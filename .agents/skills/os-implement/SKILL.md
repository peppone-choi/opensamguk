---
name: os-implement
description: Use when implementing an approved opensamguk plan or task inside a defined file scope.
---

# OS Implement

Announce this workflow, then read `.claude/commands/os-implement.md` and the routed implementation documents completely. Treat the approved plan and task as `$ARGUMENTS`. Map Claude file and shell tools to Codex equivalents, register ownership, use tests first, preserve parity invariants, and stop on scope expansion. Run the required verification before claiming completion; never commit, push, merge, or deploy without explicit approval.
