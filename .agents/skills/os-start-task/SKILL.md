---
name: os-start-task
description: Use when beginning a non-trivial opensamguk task that needs contract, ownership, document routing, plan, and approval checks before implementation.
---

# OS Start Task

Announce this workflow, then read `.claude/commands/os-start-task.md` and `docs/agent/README.md` completely. Treat the current request as `$ARGUMENTS`. Inspect task contract and ownership, route only necessary documents, and produce an executable plan with verification commands and approval points. Do not implement during this workflow.
