---
name: os-e2e
description: Use when validating an opensamguk user flow or acceptance criterion through a live browser, DOM, and network observations.
---

# OS E2E

Announce this workflow, then read `.claude/commands/os-e2e.md` and `docs/agent/lifecycle-testing.md` completely. Treat the requested flow as `$ARGUMENTS`. Use the available Codex Playwright/browser capability after probing it. If the service or browser is unavailable, report `채점대기`; never convert missing observation into a pass.
