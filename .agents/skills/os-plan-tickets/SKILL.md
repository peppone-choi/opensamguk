---
name: os-plan-tickets
description: Use when converting an opensamguk specification, plan, or backlog into Jira Epics, Stories, Sub-tasks, and testable acceptance criteria.
---

# OS Plan Tickets

Announce this workflow, then read `.claude/commands/os-plan-tickets.md` and `docs/agent/lifecycle-planning.md` completely. Treat the source document and optional project key as `$ARGUMENTS`. Probe the Atlassian MCP before use. If authentication is unavailable, produce the local Markdown plan and report `채점대기`; do not claim that tickets were created.
