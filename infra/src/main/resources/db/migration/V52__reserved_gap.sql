-- V52 는 의도된 빈 번호다(PR 비평 S7). V51(gateway 공지) 뒤 V53(게시판 확장)~V56(작전) 이 먼저 번호를 받아
-- 이 자리가 비었고, Flyway 기본 `outOfOrder=false` 라 나중에 다른 브랜치가 V52 를 들고 오면 이미 V53+ 가 적용된
-- DB 에서 거부된다. 그래서 번호를 자리표시자로 못 박는다 — 스키마 변경 없음. 새 마이그레이션은 V57 부터.
SELECT 1;
