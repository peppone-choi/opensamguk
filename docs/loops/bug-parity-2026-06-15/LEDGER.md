# LEDGER — bug-parity-2026-06-15 루프

목적: "ulw 버그 확인 및 수정 및 QA 및 패러티 확보" — opensamguk 패러티/로직/크래시/와이어링
버그를 하나씩(1바퀴=1버그) 잡고 고정 게이트 시험지로 회귀 0을 확인하며 닫는다.

행 형식: `| 바퀴 | 가설 | 점수 전→후 | 채점자 | 판정 | 원인 한 줄 |`
(0바퀴 = 베이스라인. 채점자 칸 공란/"본인" = 무효. 폐기 가설 변형은 원가설 바퀴 링크.)

| 바퀴 | 가설 | 점수 전→후 | 채점자 | 판정 | 원인 한 줄 |
|---|---|---|---|---|---|
| 0 | (베이스라인) | BE `:common:test`+`:logic:test` `--rerun-tasks`=BUILD SUCCESSFUL (전 스위트 green) + FE `tsc --noEmit` web/game·web/gateway 둘다 EXIT=0/0에러 | gate(결정적) — ctx_execute gradle grep BUILD SUCCESSFUL + tsc 직접 | 기준선 | branch loop-parity-2026-06-14-c @ cde6abca, 2026-06-15 KST. Docker down → infra/engine/api IT skip(채점 제외) |
| 22 | 라이브 장수생성 서버별 라우팅 — s1 레지스트리 `gameUrl=/game`을 신뢰해 로비가 `/game/join`으로 보내던 것을 `s1→/game/s1` 정규화 + 쿼리형 서버 child-path 보존(`/game/join?server=id`)으로 수정 | live `/api/servers` s1 `/game` + `/game/s1/join` 404 → local `/api/servers` s1 `/game/s1`, bbae `/game?server=bbae`; `serverGameUrl` base/path 케이스 PASS; web/gateway `tsc --noEmit` PASS; `pnpm build` PASS | fresh: codex-ultrawork-reviewer BLOCK→수정→PASS | 채택 | 원인: 멀티서버 env가 gameUrl을 공용 `/game`으로 내려도 FE가 서버 id를 경로에 합성하지 않음. 부수 수정: `/game?server=bbae` 뒤에 `/join`을 단순 concat하던 잠재 회귀 차단 |
| 1 | 로비맵 재난아이콘 회귀: MapPreviewController `state=city.frontState`→`city.state` + DTO doc + 신규테스트 (disaster-wave 07b3badb가 WorldMapController만 고치고 누락) | green→green + MapPreviewControllerTest 5/5(신규 'surfaces city.state not front_state' 포함)·WorldMapControllerTest 4/4 | fresh: :app:game-api:test XML(결정적) | 채택 | func_map.php:145-147 tuple[2]=state. 두 맵뷰어 불변식 복원. BE헌트#8/FE헌트#2 |
| 2 | troop 페이지 계약 정합: BE TroopsResponse에 myGeneralId/permission/멤버 cityName·npc/리더헤더 추가 + FE TroopInfo 실와이어 정렬 (크래시 + 모든 부대 mutation 버튼 dead 수정) | green→green + F4ReadControllersTest 29/29 + tsc green | fresh: game-api XML + tsc(결정적) | 채택 | FE가 BE 미발신 shape 소비→reservedCommandBrief.map undefined 크래시·myGeneralId 항상0. 멤버 cityName(#11)·부대명변경 permission≥4(#18) 동반. FE헌트#3/#4/#6/#11/#18 |
| 3 | diplomacy 서신 발송/회수/파기 인테이크 가드: isIntakeQueued/isIntakeDenied 적용 + 죽은 2단계 state 분기 제거 | green→green + tsc + 65 vitest green | fresh: tsc + vitest(결정적) | 채택 | 무조건 성공토스트(P0-04/06류) 제거. IntakeOutcome에 state 없어 파기 2단계 분기 dead. FE헌트#5/#10 |
| 4 | join 장수등록 4프리셋(랜덤/통무/통지/무지형) generalStats.ts 포팅 + 전콘(pic) 체크박스 | green→green + tsc green | fresh: tsc(결정적) | 채택 | legacy PageJoin.vue+generalStats.ts. pic=Join.php rule(boolean). 폼편의=RNG게이트 밖. FE헌트#13 |
| 5 | 맵 도시 툴팁 포맷 두 뷰어 동시정정: `【region|level】 name` + 국가명 2줄 (불변식) + types.ts state 주석 | green→green + tsc 양쪽 green | fresh: tsc web/game+gateway(결정적) | 채택 | legacy CityBasicCard.vue:10. MapViewer↔MapPreview byte-동일 유지. FE헌트#20/#12 |
| 6 | generals 페이지 컬럼 라벨 정정(명성→레벨, 명성칭호→명성) + 무국적 '무소속'→'-' | green→green + tsc green | fresh: tsc(결정적) | 채택 | sibling rankings/generals 라벨 + legacy '-' 표기. FE헌트#14/#16 |
| 7 | city 통솔보너스(+N) 색 limegreen→cyan | green→green + tsc green | fresh: tsc(결정적) | 채택 | legacy GeneralBasicCard.vue:32 style color:cyan. FE헌트#15 |
| 8 | rankings/npcs 종능/통무지/명성/계급 천단위콤마 제거→raw int | green→green + tsc green | fresh: tsc(결정적) | 채택 | legacy a_npcList.php:122-127 raw. FE헌트#17 |
| 9 | history 연감 드롭다운 라이브월(현재) 옵션 추가 + 다음달 가드 완화 | green→green + tsc green | fresh: tsc(결정적) | 채택 | legacy PageHistory.vue generateYearMonthList. FE헌트#21 |
| 10 | inherit ResetStat 검증 하드코딩 165→currentStat 합계 derive | green→green + tsc green | fresh: tsc(결정적) | 채택 | gameConst FE 미노출→currentStat sum 사용(GameConst defaultStatTotal=165 동치). FE헌트#23 |
| 11 | login next 파라미터 open-redirect 차단(내부 '/' 만 허용, '//' 차단) | green→green + tsc green | fresh: tsc(결정적) | 채택 | 보안: 프로토콜-상대 '//evil.com' 리다이렉트 차단. FE헌트#22 |
| 12 | lobby '입장' 행 장수 초상(picture) 64x64 렌더 (BE가 주는데 드롭) | green→green + tsc green | fresh: tsc(결정적) | 채택 | legacy getIconPath.ts(imgsvr→d_pic/d_shared). 표시-only. FE헌트#8 |
| 14 | RaiseDisaster city.trust 곱 누락 (재난=trust*ratio 무캡 / 호황=least(trust*ratio,100)) — 커밋 11b9ad75 | green→green + DisasterTrustApplyTest 3/0 + RaiseDisasterTest 10/0 | fresh: :app:game-engine:test+:logic:test XML(결정적) | 채택 | RaiseDisaster.php:129/154. trust=FLOAT raw 곱(round 없음). 엔진 applyDisaster postLogic+meta. BE헌트#2/#11 |
| 15 | flush-delta 3컬럼 누락: nation.power(#9)/general.officer_city(#17)/statisticInserts(#10) — 커밋 5be70e13 | green→green + JdbcFlushExecutorIT 6/0(power 1000→1234·officer_city 5→0·statistic step-12) | fresh: :infra:test XML(Docker IT, 결정적) | 채택 | process_war.php:705-708·func_gamerule.php:322-333. RowMapper는 방출하나 SET/payload 누락. BE헌트#9/#10/#17 |
| 13 | turn-loop CRITICAL: runTick 꼬리(applyKillturnDecrement+updateTurnTime) 미실행 + dueGenerals strict-< (PHP TurnExecutionHelper.php:153-230/:237 turntime<date) — 커밋 e8576473 | green→green + :app:game-engine:test 372/0(신규 DrainTailAdvance 5 + 기존 8 fixture 재정합) | fresh: :app:game-engine:test XML(결정적, Docker ITs 포함) | 채택 | 꼬리 미배선=killturn 미감소·kill/환생/유체이탈·lived_month·turntime advance 미발동. 8 fixture가 inclusive<=·killturn0 가정 → strict-<·killturn>0·E2E at=runTime·meta 결정적순서로 재정합(약화 0). AiSelectionGate 불변. BE헌트#1/#6 |
| 21 | 로비 시나리오 이름 표시(라이브 버그 3) — `scenario_1010`(코드) 대신 시나리오 title `【역사모드1】 황건적의 난`. 신규 `ScenarioTitleResolver` 가 config/meta title 미시드 시 커밋된 scenario 리소스에서 read-time 해석(legacy ResetHelper `getTitle` 동일 출처), 폴백=코드 | game-api 307→311 green(ScenarioTitleResolverTest 4/0 byte-exact, ServerBasicInfoControllerTest 3/0 불변) | fresh: game-api XML(결정적) + 배치 적대리뷰(ship 전) | 채택 | 라이브 s1 config title 미시드 → read-time 리소스 해석으로 **재시드 불요**. ScenarioImporter seed-time config title 미기록은 그대로(read-time이 상위 폴백) |
| 20 | 로비 미등록 진입 3-버튼 패러티(라이브 버그 1+2 UI) — 단일 '장수등록'→`/game/` 덤프를 entrance.ts 게이팅으로 교체: canCreate=`!(blockGeneralCreate&1)`→장수생성/game/join · canSelectNpc=`npcMode==1`→장수빙의/game(CharacterClaim) · canSelectPool=`npcMode==2`→장수선택/game/select-pool. gameUrl `/game` 베이스 정규화 | web/gateway tsc EXIT=0/0err | fresh: tsc(결정적) + 배치 적대리뷰(ship 전) | 채택(조건부) | entrance.ts:51-58,274-276. **라이브 s1 config npcmode 미시드(=0)라 빙의 버튼 실제표시는 config 의존** → 버그2 완결엔 npcmode 시드/어드민편집(버그4) 필요(백로그) |
| 19 | che_감축 level 제약 패러티 — 누락된 2번째 `ReqDestCityValue(level>origCityLevel)`(origCityLevel=`CityConst.byId(capital).level` 정적레벨) 추가 + **커널갭** `cityIntField "level"→c.level` 수정(미지원이라 감축 `>4,>orig`·증축 `>3,<8` 4 제약 전부 런타임 throw=latent-broken이었음) | logic 2154→2157 green (CheGamchukConstraintTest 3/0, GamchukJeungchuk 3/0, AI/selection 골든 불변) | fresh: **parity-reviewer**(적대적 che_감축.php byte + XML) | 채택 | che_감축.php:63-71. 정적레벨 출처 정확(dynamic 아님), errMsg byte-exact, op `>`. cityIntField 단일 case 추가(타 key 불변)로 증축도 co-fix. 행동 테스트(deny@level==orig / allow@level>orig). #14 |
| 18 | InvaderEnding 엔진배선 — dispatched no-op 닫음: `env[InvaderEndingContext.ENV_KEY]=wctx` 공급 + `WorldActionContext`가 `InvaderEndingContext`(10메서드) 구현 + `InMemoryTurnWorld.multiplyRefreshLimit`. (leaf 등록은 이미 logic `WorldActions`에 존재 — 실갭은 env 미공급) | engine 375→382 green (신규 WorldInvaderEndingContextTest 7/0), logic 2154 불변, 기존테스트 0 손상 | fresh: **parity-reviewer**(적대적 InvaderEnding.php byte-parity + XML, 제안 컨텍스트 0) | 채택 | sammo/Event/Action/InvaderEnding.php. 10메서드 전부 MATCH, 가드(isunited∈{0,2}/nationCount≥2/!needStop)+승(한족)·패(이민족 ⓞ)분기 정확. 격리 CITED: refreshLimit meta-only(컬럼flush 백로그)·setIsunited meta(Q14 선례). **RaiseInvader 미배선→LATENT live**(별도 백로그). 로그 YEAR_MONTH 접두=선존재 갭(바퀴17). off-by-one PHP cite 정정 |
| 16 | 장기-시뮬 게이트 Phase 1 — 천하통일 탐지(checkEmperior) 포팅: Q14 detection(level>0 국가수==1 && 전도시소유 → isunited=2 + 전토통일 국가사 로그). logic `CheckEmperior.kt`(pure)+`postUpdateMonthlyTail` Q14 콜백 + 엔진 `WorldCheckEmperiorContext`+`InMemoryTurnWorld.setIsunited`. 격리(전부 CITED): 1회성 부수효과 5종+checkStatistic+DB영속(컬럼flush/boot-load)+로그 YEAR_MONTH 접두 | logic 2148→2154·engine 372→375 green (신규 CheckEmperiorTest 6/0 + WorldCheckEmperiorContextTest 3/0) | fresh: **parity-reviewer**(적대적 PHP byte-parity + XML 직독, 제안 컨텍스트 0) | 채택 | func_gamerule.php:696-769(:430 호출). no-rng Q14(draw 스트림 불변). 인바리언트 1-6 전부 MATCH, 격리 전부 CITED, 회귀 0. 장기-시뮬 Phase 1 닫음(이전 세션 un-gated 원복분 재포팅+게이트화). 스코어러 P1 2건=선존재 엔진-광역 로그포맷 갭(아래 백로그) |

| 23 | ScenarioImporter가 world_state.config에 `npcmode`/`block_general_create` 시드 — entrance 3버튼 게이트에 사용되는 값을 미기재 시 FE 오판하지 않도록 명시 기록 | infra `:infra:test` ScenarioImporterIT green (Docker IT); config에 `npcmode`/`block_general_create` 키 단언 PASS | fresh: `:infra:test` XML(결정적, Docker IT) | 채택 | legacy install.php 기본값(npcMode=0, blockGeneralCreate=0)과 동일. 기존 라이브 서버는 별도 어드민 config 편집 필요(백로그). |
| 24 | `che_해산` handler tombstone seam — PHP `deleteNation`처럼 방랑국 해산 시 `nation`/`nation_turn`/`diplomacy` 삭제와 `ng_old_nations` 보존 스냅샷을 daemon flush에 전달 | prod s1: `nation` 90개 중 `level=0` 88개 + 최근 `멸망` 로그 다수 → engine `FoundingHandlerSeamTest`/`KillTombstoneTest`/`TombstoneEmitterTest` XML green | fresh: engine XML(결정적) + PHP oracle line 증거 | 채택 | `CheHaesan.lastDeletedNationId`를 `ReservedTurnHandler`가 소비하지 않아 dead level-0 nation row가 남음. 추가로 `ChangeRecorder.markNationDeleted` 스냅샷이 world dirty state에 기록되지 않아 `ng_old_nations` flush payload가 비어 있었음. |
| 25 | 잘못된 예약 인자가 resolver까지 도달해 런타임 예외를 내는 경계 누락 | `ReservedTurnHandlerTest` RED 14건 중 1 failure(`ClassCastException`) → GREEN 14/14, failures/errors/skips 0 | fresh: Java 21 대상 XML + PHP `BaseCommand.php:64-86,377-411` | 채택 | 정의별 `parseArgs`를 제약/resolve 전에 실행하지 않아 invalid map이 바인딩된 resolver까지 도달했다. 필수 schema key 검증 실패 시 PHP 문구로 로그 후 `휴식` 폴백하며 world mutation은 0으로 고정했다. |
| 26 | `RaiseNPCNation` 후보 도시가 Kotlin `Random.Default`에 의존해 같은 world seed에서도 순서가 달라진다 | `WorldActionContextRngTest` RED 1/1 failure → GREEN 1/1, failures/errors/skips 0 | fresh: Java 21 대상 XML + PHP `RaiseNPCNation.php:199-232`, `Util.php:411-428` | 채택 | PHP 후보 셔플은 action `RandUtil` 밖의 ambient stream이다. action draw cursor를 보존하면서 canonical action seed의 별도 fresh stream으로 Kotlin 비결정성을 격리했다. PHP turn-for-turn 셔플 캡처는 Phase 2/3 과제로 유지한다. |
| 27 | 빈 토너먼트 패턴이 Kotlin `Random.Default`에 의존해 동일 world seed에서 다른 대회가 선택된다 | `MonthlyPostUpdateHookTailWiringTest` RED 1/1 failure(`6m22s`) → GREEN 1/1, failures/errors/skips 0(`24s`) | fresh: Java 21 대상 XML + PHP `func.php:1280-1304` | 채택 | PHP `shuffle()`는 Q15 monthly `RandUtil` 밖의 ambient stream이다. canonical monthly seed의 별도 fresh stream으로 패턴만 결정화해 전달된 monthly RNG cursor를 보존했다. engine/logic production `shuffled()`/`Random.Default` 검색은 0건이다. |
| 28 | B1이 `CreateManyNPC(10,10)`의 생성 수와 RaiseNPCNation 결과를 모두 10으로 가정해 정상적인 11건을 회귀로 오판한다 | `ScenarioBlankUnificationIT` RED free NPC 10→11, 다음 RED NPC국 10→11 → GREEN 1/1, failures/errors/skips 0(`33s`) | fresh: Java 21 Testcontainers IT + PHP `CreateManyNPC.php:65-94` | 채택 | fillCnt는 군주국별 `npc<4` 부족분을 추가하므로 literal 10이 아니다. pre-event world에서 PHP 공식을 계산하고, canonical deterministic 후보 셔플의 거리 필터 결과 11개국은 별도 회귀값으로 고정했다. |
| 29 | 교차 호출 게이트가 필수 인자 명령 `che_헌납`을 빈 payload로 실행해 새 PHP argTest 경계를 정상 제약 실패로 오판한다 | full backend RED 514건 중 2 failure(`인자가 올바르지 않습니다.`) → `PrecheckFullCrossCallSiteTest` GREEN 11/11, failures/errors/skips 0(`1m3s`) | fresh: Java 21 대상 XML + PHP `che_헌납.php:26-52` | 채택 | API PRECHECK의 빈 args와 daemon의 빈 예약 payload를 동일시한 오래된 fixture가 원인이다. daemon에는 유효한 `{isGold:false,amount:1000}`을 전달해 양쪽 모두 동일한 쌀 제약 분기를 비교하도록 바로잡았고 production invalid-arg 거부는 유지했다. |
| 30 | PHP의 월 단위 `killturn`을 3삭/월 엔진에서 그대로 감소시켜 NPC 수명이 1/3로 줄어든다 | lifecycle/mapper 회귀 GREEN + V26 Testcontainers 1/1 GREEN(기존 72→216, `killturn_unit=phase`) | fresh: Java 21 XML + PHP `Scenario/GeneralBuilder.php:656-667` | 채택 | 절대 출생·사망년도는 유지하고 월 기간만 ×3 한다. 신규 장수는 phase 마커를 기록하며 V26은 기존 `npc_state>=2` 행을 마커 기준으로 한 번만 변환한다. |
| 31 | `ScenarioImporter`가 미성년 449명을 시작부터 INSERT해 PHP보다 일찍 등장시키고 사망 카운터도 일찍 감소시킨다 | 181년 229 즉시·449 지연·77 birth 이벤트; boot 등장/flush/restart GREEN; V26 기존 미성년 행→RegNPC 이벤트 복구 GREEN | fresh: Java 21 Testcontainers + PHP `Scenario.php:421-469,557-568`, `Scenario/GeneralBuilder.php:575-591`, `Event/Action/RegNPC.php:14-59` | 채택 | `birth+14` 절대년도 1월에 등장한다. V26은 기존 세계의 14세 미만 NPC와 turn/rank를 제거해 이벤트로 복구하고, 런타임 RegNPC는 멸망국을 0으로 내리며 picture/ego/special/text를 보존한다. |
| 32 | schema key만 확인해 fractional int·음수 징병량·불완전 건국/선양 payload가 resolver까지 도달한다 | 임관 `2.5`, 징병 `-1`, 건국 빈/과장명·fractional/범위밖 색·잘못된 국가형, 선양 누락 id 회귀 GREEN | fresh: Java 21 XML + PHP `che_임관.php:28-48`, `che_징병.php:45-77`, `che_건국.php:40-69`, `che_선양.php:33-56` | 채택 | 선언 schema 타입 검증에 명령별 PHP argTest를 결합했다. 건국은 `mb_strwidth<=18`·실재 국가형·색 index를 요구하고 선양은 `destGeneralID:int` schema를 명시하며 invalid는 mutation 없이 `휴식`으로 폴백한다. |
| 33 | 징병 병종 해금이 현재 도시만 보아 자국 다른 도시의 레벨을 무시한다 | API precheck와 daemon FULL 모두 `ownCities={cityId:level}`를 전달; 다중 도시 회귀 GREEN | fresh: `CommandPrecheckServiceTest`, `ReservedTurnHandlerTest`, full backend XML | 채택 | PHP 국가 전역 해금 계약을 두 호출 경계에 동일하게 반영했다. |
| 34 | 유니크 복권 의도가 daemon에서 소비되지 않고, 비매매 유니크를 일반 장비 상점 카탈로그로 조회한다 | 카탈로그 멤버십·이미 보유/경매/보관 수량·PHP break 추첨 순서 회귀 GREEN | fresh: `ReservedTurnHandlerTest`, PHP `hwe/func.php:1487-1702` | 채택 | 유니크 정의는 `InheritCatalog`로 단일화하고 실패 시 추가 draw를 소비하지 않도록 break를 보존했다. |
| 35 | PHP `storage` 논리 상태를 없는 테이블처럼 조회해 재시작 후 유니크 점유량을 잃는다 | `game_kv namespace LIKE 'ut_%'` 집계 + 진행 중 경매만 로드; archive IT GREEN | fresh: `WorldSnapshotLoaderArchiveIT`, `GameKvFlushIT` | 채택 | Kotlin 물리 저장 계약에서 PHP 논리 storage 네임스페이스를 복원했다. |
| 36 | 시나리오 importer 회귀가 PHP가 보존하는 명시 군주 level 12까지 일괄 level 1로 오판한다 | 잘못된 전체 level=1 단언을 제거하고 장각/하진 level 12 보존을 명시 단언; ScenarioImporterIT GREEN | fresh: `ScenarioImporterIT`, PHP `Scenario/GeneralBuilder.php:651-653` | 채택 | 소스 officer=0/신규 성인만 level 1로 정규화하고 명시 officer는 보존한다. |
| 37 | ActionLogger 저장 접두를 추가한 뒤 현대 UI의 별도 연월 열과 중복 표시된다 | 중원 정세·지도 하단·메인 3피드에서 저장 text만 인라인 렌더; RED 2/148 → target GREEN | fresh: web/game `WorldLogPageTest`, `MainRecordZoneTest` + PHP `PageHistory.vue:41` | 채택 | PHP `formatLog(item)` 표면처럼 날짜를 텍스트에서 한 번만 표시한다. |
| 38 | 로컬 compose와 `.env.example`이 알려진 관리자 기본 비밀번호를 시드한다 | 평문 기본값 제거; 빈 `ADMIN_PASSWORD`는 관리자 시드를 건너뜀; production compose는 여전히 필수 env | fresh: secret scan + `docker compose config --quiet` | 채택 | 실서버 비밀번호는 서버 env에만 설정하고 저장소에 전파하지 않는다. |
| 39 | 통일 아카이브가 현재 프로세스의 로그·통계만 사용해 재시작 전 역사와 과거 최대 국가/장수 수, 실제 서버 기수를 잃는다 | snapshot에 `statisticRows`·최신순 `nationHistory`·`serverCount` 적재; 현재 recorder 행과 병합; focused engine + Testcontainers GREEN | fresh: `WorldActionCheckEmperiorContextTest`, `WorldSnapshotLoaderArchiveIT`, PHP `func_gamerule.php:803-904` | 채택 | PHP는 전체 `statistic`, `world_history`, `ng_games`를 다시 질의한다. Kotlin도 DB snapshot과 현재 월 pending write를 결합해 같은 첫 최대행·최신행·역사 순서를 보존한다. |
| 40 | 프론트 lockfile에 패치 가능한 `form-data` high, `js-yaml` moderate, `esbuild` low advisory가 남아 있다 | 이전 web/game·web/gateway `pnpm audit` 각각 0건; Next 15계열 최신 `15.5.20`에서 37/148·1/3 tests, typecheck, production build GREEN | fresh: registry version 확인 + 양쪽 test/typecheck/build; 최종 audit endpoint HTTP 410 | 채택 | transitive dependency를 패치 버전으로 고정하고 `@types/node`를 Vite peer 범위로 올렸다. 최종 재감사는 npm audit endpoint 폐기(410)로 결과를 만들 수 없어 성공으로 주장하지 않는다. |
| 41 | 징병 FULL 제약 보강 뒤 드러났던 모순 성공 픽스처 22건이 최종 통합 상태에서 재발할 수 있다 | forced 5-module gate `BUILD SUCCESSFUL in 30m 44s` 481 suites/4,406 tests, failures/errors 0, skip 1; 최종 canonical backend gate `BUILD SUCCESSFUL in 20m 51s`, 동일 481/4,406 GREEN | fresh: `--rerun-tasks` 전체 + `tools/parity/gate.sh backend` XML 원본 재합산 | 채택 | 22건과 후속 assertion은 모두 0건이며, 새 lifecycle·통일·archive·command 회귀를 포함한 전체 XML에서 재발하지 않았다. |
| 42 | 엔진의 `last_turn` 운반용 meta 봉투가 flush 시 전용 `general.last_turn`과 `general.meta`에 중복 저장된다 | `VerticalSliceE2EIT` RED 1/1 → `GeneralRowMapperTest` + E2E GREEN; 최종 481/4,406 GREEN | fresh: 실제 Testcontainers DB byte-compare + PHP `General.php:704-717` | 채택 | row mapper가 typed `lastTurn`과 meta 봉투를 모두 그대로 직렬화했다. 전용 JSON 컬럼 키를 meta 직렬화 경계에서 제거해 PHP처럼 한 컬럼에만 저장한다. |
| 43 | NPC 상태 2인 랜덤임관 fixture가 유산 `active_action` 적립을 기대한다 | engine 전체 RED 1건 → `ReservedTurnHandlerTest` GREEN; 최종 481/4,406 GREEN | fresh: PHP `InheritancePointManager.php:258-270` + 대상 XML | 채택 | PHP는 owner가 있어도 `npc>=2`이면 유산 적립을 건너뛴다. production 조건은 유지하고 오래된 기대값만 바로잡았다. |
| 44 | 국가 삭제 뒤 분쟁 JSON이 `8.0`을 보존해야 한다는 fixture가 PHP 재인코딩과 어긋난다 | engine 전체 RED 1건 → `TombstoneEmitterTest` GREEN; 최종 481/4,406 GREEN | fresh: PHP `process_war.php:508-522`, `Json.php:14-27` + 대상 XML | 채택 | PHP `json_decode` 후 `JSON_PRESERVE_ZERO_FRACTION` 없이 encode하면 `8.0`은 `8`이 된다. production canonical 값은 유지하고 기대값을 정정했다. |
| 45 | 무작위 건국이 군주와 선택 도시가 같아도 수하 전원을 재배치한다 | `GeongukTest` 표적 gate GREEN | fresh: PHP `che_무작위건국.php:158-168` + 대상 XML | 채택 | PHP는 같은 도시 분기에서 `setCity()`만 호출하고 국가 전체 UPDATE는 else에서만 수행한다. Kotlin도 같은 도시에서는 follower cascade를 만들지 않는다. |
| 46 | 임관 `last_turn.arg`에 실행 보조값 `relYear`/`actorNpcType`까지 저장된다 | `JoinTest` 표적 gate GREEN | fresh: PHP `che_임관.php:28-50,177` + 대상 XML | 채택 | PHP `argTest()`가 `destNationID` 하나로 arg를 정규화하므로 LastTurn도 그 키만 저장한다. |
| 47 | 랜덤임관 `last_turn.arg`에 실행 보조값이 남는다 | `RandomImgwanTest` 표적 gate GREEN | fresh: PHP `che_랜덤임관.php:31-34,281` + 대상 XML | 채택 | PHP `argTest()`가 arg를 null로 덮어쓰므로 Kotlin LastTurn도 null을 저장한다. |
| 48 | 해산이 전 장수를 먼저 재야화한 뒤 로그를 몰아서 적재한다 | `GeongukTest` 표적 gate GREEN | fresh: PHP `func.php:1753-1778` + 대상 XML | 채택 | PHP foreach처럼 각 멤버의 재야화와 action/history 로그를 같은 반복에서 처리하고 군주는 마지막에 처리한다. |
| 49 | LEDGER 26/27의 결정적 ambient divergence 채택 뒤 `RaiseNPCNation`에만 fail-fast가 남아 시나리오 이벤트를 중단한다 | compile 경계 복원; 추가 ambient 전용 게이트는 사용자 지시로 생략 | fresh: PHP `RaiseNPCNation.php:199-232`, `func.php:1278-1305` + 사용자 결정 | 채택(명시적 divergence) | 저장되지 않는 PHP 전역 native RNG 순열은 byte parity로 주장하지 않는다. action/monthly RandUtil 커서를 보존하는 별도 결정적 스트림을 유지하고, 사용자는 이 ambient 순열의 추가 테스트 없이 진행하도록 지시했다. |

## 진행 현황

- **Wave 1 완료**(바퀴 1–12, 커밋 520ab0e4·a8a83432·6648bbfc·a42304dc): FE 표시/패러티 + 로비맵 회귀 + troop 계약 + diplomacy 가드. 전부 채택(green).
- **Wave 2 진행중**(Docker on): 바퀴 13 turn-loop CRITICAL(turnTime/killturn 미진행 + dueGenerals strict-<), 바퀴 14 RaiseDisaster trust, 바퀴 15 flush 3컬럼(power/statisticInserts/officer_city). 게이트 후 행 추가.
- 헌트 워크플로: BE `wf_de48944f-a6c`(17확정), FE `wf_644eafb5-0f1`(23확정). 전체 출력 /private/tmp/.../wnebzposs.output·wnanb7z4g.output.

## 최상위 이니셔티브 — 장기-시뮬 패러티 게이트 (공백지→천하통일)

"완벽한 게임"의 진짜 완성 게이트. 계획: `docs/superpowers/plans/2026-06-15-long-sim-parity-gate-plan.md`.
- **Phase 1 ✅ (바퀴 16)**: 천하통일 탐지 포팅 완료 — checkEmperior(func_gamerule.php:696-769, :430 호출) 국가수==1 && 전도시소유 → isunited=2 + 전토통일 로그. 엔진 월틱 Q14(no-rng). logic `CheckEmperior.kt` + 엔진 `WorldCheckEmperiorContext`. detection+in-memory 전이+로그까지 닫음(영속/부수효과/로그접두는 아래 백로그). 다음=Phase 2(PHP run_long_sim.php 캡처 하네스).
- Phase 2: PHP 풀게임 캡처 하네스 run_long_sim.php (TimeUtil mock + executeAllCommand 루프 + 턴별 draw/상태/로그). 비결정 차단원 중립화.
- Phase 3: Kotlin LongSimReplayGateTest (시나리오 부팅 → N턴 → turn-for-turn byte-compare).
- Phase 4: bounded 결정적 윈도 green → 차단원 중립화하며 천하통일까지 확장(각 1바퀴).
- **Phase 4a 런타임 불변식 green (2026-07-13, PHP replay와 별도):** `scenario_0` fresh DB에서 6개 플레이어 국가·60명 사용자 소유 역할군을 실제 `거병/임관/건국`으로 만들고, production 이벤트 구성요소 기반 결정적 월간 하네스·외교관 임명·`che_선전포고`·24개월 외교 전이·군량매매/이동/징병/훈련/사기/출병으로 94개 도시를 점령했다. `checkEmperior`의 `isunited=2`와 통일 로그를 JDBC flush/restart 뒤 재검증한다. 표적 engine 3건, fresh 커맨드 계약 189건, 전체 logic 2,377 tests, engine 510 tests의 XML gate가 green이다. 이는 Kotlin runtime start-to-end gate이며 Redis/`TurnRunService`/API/SSE 운영 경로나 Phase 2/3 PHP turn-for-turn divergence를 닫았다는 뜻은 아니다.
- 비결정 차단원: TimeUtil::now(PHP wall-clock), ORDER BY RAND GeneralAI.php:3324/3345(do선양/do국가선택, Q1격리). RaiseNPCNation event shuffle과 tournament pattern의 Kotlin ambient RNG는 바퀴 26/27에서 별도 deterministic stream으로 격리했지만 PHP turn-for-turn 캡처/동일 대체는 Phase 2/3에 남아 있다.

## 백로그 (Docker 게이트 필요 — 골든 신규캡처 / 실DB IT)

- BE 명령-패러티(골든캡처 후 1바퀴씩): #7 선동 trustAmount 로그 소수1자리(number_format), #3 급습/#12 이호경식 외교 term 가산식(state IF), #13 약탈발동 float 포맷, #14 감축 2번째 제약, #15 집합 ReqTroopMembers 스텁.
- RNG-draw 경로(draw-for-draw 골든 필수): #4 do선전포고 officer_level<12+TechLimit 게이트 누락(여분 draw desync), #5 preprocessCommand(부상회복+병량소비) 데몬 루프 미실행.
- #16 C3Strategic 비교 역전 — unreachable/latent(주입처 0), P7 staging seam 배선 시 1바퀴.
- FE BE-coupled: #1 외교 서신 승인/거부(respond 엔드포인트 신설), #9 my-boss b_myBossInfo 전 섹션(read 컨트롤러 확장), #19 vote voteReward 안내(DTO 확장).
- **(라이브 버그 2+4 연결) 서버 config npcmode/block_general_create 미시드 + 어드민 편집 부재:** legacy ResetHelper.php:294-295 는 npcmode/block_general_create 를 게임생성 어드민 파라미터로 config 에 기록. opensamguk `ScenarioImporter` 는 config 에 startyear/turnterm 만 쓰고 npcmode/block_general_create 미기록 → `ServerBasicInfoController:74` 기본 0 → 로비 빙의/선택 버튼 영구 비활성(라이브 버그 2의 config 근본). **버그4(어드민 서버설정 편집)** = legacy `_admin1.php` 7개 설정 편집 + write 엔드포인트 부재(read-only). 둘 묶음: (a) ScenarioImporter 가 config 에 npcmode/block_general_create 시드(기본값 결정 필요), (b) 어드민 config write 엔드포인트+폼. 라이브 s1 은 이미 시드됨 → 어드민 편집으로만 npcmode=1 설정 가능(재시드=게임 리셋). 별도 plan.
- bbae(scenario_1030) 보급 동결 근본원인(doNPC구출발령 empty supplyCities → RandUtil.choice throw; 상류 보급계산 발산) — 별도 바퀴.
- **(바퀴 16 파생) 로그 formatText 접두 갭 — 선존재 엔진-광역 + FE 2계층 divergence (바퀴 17 측정결과: 단일 바퀴 부적합, 별도 계획 필요):** PHP `ActionLogger`는 push 시점에 push-site별 formatType 으로 접두를 stored-text 에 굽는다 — pushNationalHistoryLog/pushGlobalHistoryLog=YEAR_MONTH `<C>●</>{y}년 {m}월:`(`ActionLogger.php:241-243`), pushGeneralActionLog/pushGlobalActionLog=MONTH `<C>●</>{m}월:`(:249-251), battle=RAWTEXT/PLAIN 등(:233-238). opensamguk 엔진은 `LogEntryDraft.text` verbatim 저장(`DatabaseHooks.toLogRow`, `LogEntryDraft.format` 미소비) → 월 파이프라인 history 로그(개전/종전 Q6/Q7 `func_gamerule.php:360,384` + 전토통일 :733) 접두 누락. **추가로 FE 도 divergence**: legacy `PageHistory.vue:41`는 `v-html=formatLog(item)`로 접두-포함 text 를 인라인 렌더(행별 날짜컬럼 없음)인데, opensamguk `web/game/.../world-log/page.tsx:95`는 `{year}년 {month}월`을 **별도 컬럼**으로 렌더. ⇒ 백엔드만 접두 추가 시 **날짜 이중표시 회귀**. 패러티-정답=stored-text에 formatType별 접두 굽기 + FE는 인라인 렌더(날짜컬럼 제거). **단일 바퀴 부적합**(엔진 formatText 포팅 + push-site별 formatType 배선 + 접두-없음 가정 기존 엔진 테스트 일괄 정정 + FE 4-surface + PHP stored-bytes 골든). **spec 작성 완료**: `docs/superpowers/specs/2026-06-15-log-format-prefix-parity.md`(9 formatType 인벤토리 + push-site 맵 + 단계 P0-P3). **§3 패러티-계약 결정(A: byte-match stored text+FE 인라인 / B: 구조 divergence+정규화 게이트, 추천 A) = 유저 승인 대기.** 장기-시뮬 Phase 4 full-stored-bytes 게이트 전제.
- **(바퀴 16 파생) isunited DB 영속 갭 — CLOSED 2026-07-13:** `DatabaseHooks`가 `meta.isunited`를 flush payload에 싣고, `JdbcFlushExecutor`가 `world_state.isunited`를 갱신하며, `WorldSnapshotLoader`가 전용 컬럼을 `meta.isunited`의 정본으로 복원한다. `ScenarioBlankUnificationIT`가 `isunited=2`와 통일 로그를 flush/restart 뒤 재검증한다.
- **(바퀴 18 파생) 침략자-이벤트 family START 미배선 — InvaderEnding 도달성 prereq:** `RaiseInvader`(isunited=1 세팅)/`RaiseNPCNation` start-event + 트리거 조건이 엔진에 미배선(P3 "9 leaves" 밖). 바퀴 18이 InvaderEnding **종료 seam**(dispatched no-op)은 닫았으나, START가 없어 live 에선 isunited 가 1이 되지 않아 종료가 **발동 불가(LATENT)**. family START 배선 = 별도 이니셔티브(reachability). 배선 후 InvaderEnding 라이브 검증 + 장기-시뮬에서 이민족 엔딩 경로 게이트화.
- **(바퀴 16 격리, Phase 4) checkEmperior 1회성 부수효과:** checkStatistic(:725)·유니크경매 종료(:735-743)·상속 unifier+2000(:745-753)·United 이벤트(:755)·상속 merge/apply(:757-760)·refreshLimit*100(:763)·CheckHall(:765-767). 천하통일 최종턴에서만 발동 — 장기-시뮬 Phase 4 윈도 확장 시 PHP 라인대로 포팅.
- FE 채점 = `tsc --noEmit`(web/game·web/gateway) + `web/game` vitest(65). BE = gate.sh 모듈 XML(결정적).
