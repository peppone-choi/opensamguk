# 2026-09-26 프론트 전면 재구축 — 결정 반영과 캔버스 v3 1묶음

## 범위

- 사용자 결정 7건(메타 `reports/opensamguk/tasks/2026-09-26-frontend-full-audit.md` §7)과 모바일 작전실 결정을 ADR-LITE-049 개정과 로드맵에 반영한다.
- 캔버스 v3 생성 원본 `docs/design/ui-v3/`를 추가한다. 1묶음은 셸·메뉴·작전실(데스크톱·태블릿·모바일)·시스템 보강 8장이다.
- 제품 코드는 바꾸지 않는다.

## 바뀐 것

- `.ai/decisions.md` ADR-LITE-049 Amendment (2026-09-26):
  · 정본 교체
  · 2026-09-17 개정의 월드 분기·대응표·「기존 라우트 바로 교체」 조항과 규칙 (5) 삭제
  · 셸·메뉴 한 벌, 새 경로 + 308, 모바일 같은 게임, 표기 규칙, 삼모 화면 삭제, 소유
- `docs/design/roadmap.md` 「세계와 지도」: 화면 정본 문장 교체, 모바일 문장 추가, 검토일 갱신.
- `docs/design/ui-v3/`: README, 공용 부품, 1묶음 생성기, 생성된 `project/V3*.dc.html` 8장.

## 검증

- `python3 boards_v3_shell.py` → 8장 생성.
- 보드별 여닫는 태그 균형 검사 통과.
- 한자는 시스템 보드의 「쓰지 않는 말」 표에만 있다(의도).
- 캔버스 아티팩트 버전 19에 8장과 인덱스(새 줄 y=-3500, 제목 노트)를 반영했다.

## 미검증·위험

- 보드 렌더는 눈으로 확인하지 않았다(Design 아티팩트 규칙상 사용자 요청 없이 렌더 검증을 하지 않는다).
- 새 경로 이름(`/retinue`, `/stratagem`, `/territory`, `/corps`, `/court`, `/records`, `/council`, `/mail`)은 1묶음 승인 때 함께 확정한다.
- 입력 원장 표시 이름에 삼모 용어가 남아 있다: 숙련전환, 군량매매. 원장 소유 레인에서 고쳐야 한다.

## 인계 뒤 갱신 (2026-09-26 총괄 조율 세션)

- 이 PR과 캔버스 아티팩트는 총괄 조율 세션이 이어받았다. 와룡전풍 지도 설계(메타 `docs/map-design-plan.md`)는 지도 설계 세션이 계속 맡는다.
- main `f8a228878`을 병합했다(충돌 없음).
- 지도 설계 결정 중 화면에 닿는 것을 ADR-LITE-049 개정 「지도 표시」와 로드맵 「세계와 지도」에 반영했다: 아이소 폐기·탑다운 타일, 세력색은 경계 띠·깃발·城 지붕·1칸 거점 아이콘, 城·부대 깃발 모양.
- ADR-LITE-044 개정의 「아이소 타일 렌더러」 문구는 렌더 방식만 대체했다. 격자·좌표·province 계약은 그대로다. 城 크기·성문·강·땅 피복 같은 지도 설계 층 세부는 지도 설계 세션이 ADR-LITE-044 개정으로 올린다.
- 검증: `python3 tools/ci/naming_lint.py` 4종 모두 기준선과 같다.
