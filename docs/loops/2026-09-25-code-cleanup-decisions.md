# 코드 이름 규칙과 삼모 은퇴 결정

기준 커밋: `origin/main` 88ee3f61f. 사용자 결정(2026-09-25)에 따라 ADR-LITE-042·043·057·062·065를 정정하고 새 이름 규칙을 ADR-LITE-066으로 기록한다. 구현·운영 전환은 아직 하지 않았다.

## 착수 게이트

- 2026-09-25 확인: 0단계 PR #914(b19546c)·#913(4c03ced)·#915(88ee3f6)이 모두 main에 머지됐다. #915의 범용 예외 격리 등 잔여 범위는 후속 과제로 남았다. 이 결정 문서 worktree는 선행 PR의 변경을 보존했다.
- #905는 draft로 열려 있고 head `1b2ce3c`의 변경 파일이 넓다. 개명 전에 머지되게 하거나 `docs/development/rename-map.md`에 리베이스 대응을 기록한다. 이 PR에서는 #905의 코드·지도를 수정하지 않는다.
- pep C단계의 저장·통신 식별자 개명 및 삼모 은퇴 선행 조건을 관리자 런북 첫머리에 명시했다. 운영 실행은 하지 않았다.

## 이슈 반영 초안

- GH #891 / Jira OPENSAM-290: 「한 시즌 뒤 스위치 제거」 계획을 새 코드 정리 추적으로 대체한다. 삼모 전용 코드·골든·도구·롤백 스위치의 제거, 공유 동작 감사, 세계 형식 fail-closed, 순수 개명·저장 식별자 개명 게이트를 새 추적에 연결한다. 대체 추적을 만든 뒤 옛 #891을 대체 사유와 링크를 남기고 닫는다.
- GH #249 / Jira OPENSAM-106: C단계는 저장·통신 식별자 개명과 삼모 은퇴 뒤에 수행한다고 명시한다. 기존 「삼모 골든 보존」「스위치 복귀」「추가 승인 없이 운영 초기화」 조건은 폐기하고 컷오버 전 이미지 태그와 백업 복원을 적는다.
- GH/Jira 수정과 종료는 외부 반영이므로 대상별 승인 후 수행한다. 이 문서는 게시된 이슈 본문이 아니다.

## 정한 값

- 다음 ADR 번호는 저장소의 마지막 번호 065 다음인 **066**으로 정했다.
- 패키지 예시는 `input`, `turn`, `court`, `domestic`, `war`, `encounter`, `siege`, `march`, `movement`, `supply`, `warehouse`, `economy`, `retinue`, `person`, `card`, `prestige`, `vision`, `stratagem`, `map`, `npc`다. 실제 클래스 충돌과 책임 분리는 의존 감사·개명표에서 확정한다.

## 검증과 미완료

- 문서 변경의 `git diff --check`를 통과했다. 런타임·DB·API·웹 동작은 이 PR 범위에서 변경하지 않았다.
- CI·리뷰·main 병합 검증은 별도로 진행한다.
