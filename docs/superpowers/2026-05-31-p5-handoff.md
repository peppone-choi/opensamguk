# P5 — GeneralAI NPC Autorun — Handoff (2026-05-31)

**Branch:** `peppone-choi/pull-main-continue-build` → PR to `main`.
**Merge state:** `HEAD = d8fde2c`, 47 commits ahead of `origin/main` (`b5d4a9d` = P0–P4).
**Build (shippable HEAD):** `:common:test` + `:logic:test` (~1661) + `:app:game-engine:test` (~135) — **BUILD SUCCESSFUL, 0 failures** (only cosmetic Windows-char test-name warnings). `DaemonNoEntityManagerTest` green (the ONE daemon-write rule holds).

---

## TL;DR

P5 ports the PHP `GeneralAI.php` (4,293-line NPC-AI monolith) to a pure-`:logic` `ai/` package + an
engine seam, on the memory-centric CQRS framework. **The P5 CORE gate is CLOSED:** the live Kotlin AI,
run over a byte-faithful scenario-1010 world snapshot, reproduces the real PHP AI **draw-for-draw for all
174 due generals** (667/667 draws, value + `stateIdx`/`bufferIdx` cursor), non-vacuous. Remaining work
(crafted-fixture coverage for under-exercised families, long-sim, a few follow-ups) is a documented
backlog — the same way P3/P4 closed.

---

## What landed (the merge)

### `:logic` — the `ai/` package (pure, no Spring/DB)
- `AutorunGeneralPolicy` / `AutorunNationPolicy` — the 4-layer policy merge (default → server → nation →
  aiOptions). **The KV `priority` override is LIVE for BOTH policies** (the B1 parity-fatal correction:
  PHP `property_exists` matches the bare-action-name `static` props → valid override REPLACES default,
  typo DROPS). The merged `priority` ORDER **is** the dispatch = log = draw order.
- `AiSeed` — the per-general `RandUtil(LiteHashDrbg(serializeSeed(hidden,"GeneralAI",year,month,generalId)))`
  (5-component, `hiddenSeed`-direct) + the `getStatValue` flavor table (calcGenType/reward/promotion sigs).
- `AiUtils` — stable `usort`/`uasort`/`arsort` (no secondary comparator), count-idx weight,
  `choiceUsingWeightPair` builder.
- `AiInstanceState` — `updateInstance` (reqUpdateInstance regime), `calcGenType` (the decision's FIRST,
  conditional `nextBool`), `calcDiplomacyState` (`joinYearMonth(-1)` early-gate, `last_attackable`
  ChangeRecorder delta reading the pre-turn snapshot).
- `AiWorldView` — `categorizeNationCities`/`categorizeNationGeneral` (PK-ascending buckets,
  first-match-wins ladder, `calcRecentWarTurn` 12000 sentinel, cities-before-generals by-ref) +
  `calcWarRoute`.
- `bfs/AiDistance` — REUSES the GREEN `CityConst.path` name-order adjacency + P4
  `SearchDistanceListToDest`; adds `searchDistance` (FIFO finalize-at-dequeue), Floyd-Warshall,
  `isNeighbor`.
- `CandidateBridge` — `candidateAllowed` = argTest gate THEN `evaluateConstraints(FULL)` (the IDENTICAL
  call execution makes). AI emits RAW args; single internal canonicalization.
- `GeneralAI` — the two LIVE dispatcher spines (`chooseGeneralTurn`/`chooseNationTurn`), pre-loop
  branches, reserved-honor asymmetry, `do중립` terminal fallback. `chooseInstantNationTurn` is a
  `@ParityQuarantine` stub (zero PHP call-sites, off-gate).
- `GeneralAiFactory` / `GeneralAiContext` — the assembly seam + per-general AI input bundle.
- `ai/families/*` — the 7 leaf families' `do<한글>` bodies (draw-order primitives + world-driven
  candidate-set assembly): NationDeploy (11× 발령), NationReward (포상/몰수), NationDiplo
  (불가침/선포/천도 — SELECTION only), GenDomestic (내정/금쌀/징병), GenWarMove (전투/워프/헌납/집합/방랑),
  GenFound (거병/건국/선양/국가선택/사망대비/중립), RatesPromo (tax/bill/promotion).
- New `che_*` defs (9) + the `che_출병` FULL-pack repair + 5 diplomacy/pathfinding presets.

### `:app:game-engine` — the seam
- `AiTurnAdapter` — builds the per-general "GeneralAI" `RandUtil` (threaded by reference, the SOLE
  decision stream) + the read-only world view; materializes the live `InMemoryTurnWorld` (real
  generals/cities/tech/diplomacy/derived scalars) into the AI context. READ-ONLY over game entities;
  meta-KV via ChangeRecorder delta.
- `ProcessNationCommand` — the nation-command resolve path (new), nation pass BEFORE general pass.
- `TurnDaemonLifecycle` — threads ONE per-general "GeneralAI" decision rng across the nation + general
  passes (production matches the gated behavior).

### Golden harness (`tools/php-golden` + test resources)
- `capture_ai.php` — live 1010 due-general autorun draw-stream + chosen command + reason (174 turns,
  year 181 month 1), npc-census proof (174/0/0, aborts if non-zero), live-pinned `hiddenSeed`
  `71adaa4df4012a20c0883beba4810681`.
- `capture_ai_world.php` — the pre-turn `world-1010.json` snapshot (174 generals / 94 cities / 2
  nations / diplomacy / nation_env), byte-identical across two runs.
- `capture_ai_crafted.php` — GT2 crafted goldens for the under-covered families (already captured:
  `ai-crafted-diplo-{불가침제의,선전포고,천도}`, `ai-crafted-genfound-선양`, `ai-crafted-nation-pass-{m3,m6,m12}`).
- `AiReplayGateTest` (`:logic`) — RNG-kernel replay (174/174, 667/667 draws).
- **`AiSelectionGateIT` (`:app:game-engine`) — THE CORE GATE: live-selection draw-for-draw, 174/174
  turns matched (chosen actionCode + RAW args + reason + live-pulled draw stream value+cursor),
  non-vacuous (seed-corruption probe → 2/174).**

---

## Quarantines (held, proven)
- **ORDER BY RAND** (`do선양` npc==5, `do국가선택`-오랑캐 npc==9): UNREACHABLE in scenario 1010
  (census 0/0, asserted at capture + replay). Deterministic `min(no)`/`min(nation)` substitute, 0 DRBG
  draws. Crafted fixtures byte-match the non-id bytes only.
- **`chooseInstantNationTurn`**: no live PHP call-site → off the gate; structural stub, sibling
  `chooseNationTurn` byte-match.
- **Diplomacy downstream delta (m10)**: `do불가침제의`/`do선전포고`/`do천도` — only the SELECTION +
  boolean gate + draw stream are P5; the `che_*` state-mutation/logs are P6.

---

## Backlog — NOT in this merge (resume here)

### 1. Crafted-fixture live replay (WIP — preserved, BROKEN)
- The crafted GOLDENS are captured + committed (`ai-crafted-*.json`). The live-replay wiring
  (extending `AiSelectionGateIT` to replay them) is **incomplete and does not compile** — it is
  **stashed** (`git stash list` → "p5-crafted-replay-WIP") **and pushed to branch
  `peppone-choi/p5-closure-wip`**.
- Compile errors to fix: `AiSelectionGateIT.kt` ~line 409 `Unresolved reference 'canonicalizeArgs'`
  + 2 type-inference errors ~line 423.
- The WIP also contains **real diplomacy divergence fixes** (`CheCheondo`, `CheSeonjeonpogo`,
  `NationDiploFamily`) surfaced by the first crafted replay — re-apply + finish these.
- Resume: `git checkout peppone-choi/p5-closure-wip` (or `git stash pop`), fix the compile errors
  (add/route `canonicalizeArgs` via the F-BRIDGE canonicalization, do NOT double-canonicalize),
  run the crafted gate, fix any further Kotlin divergences (never the golden), commit.

### 2. Long-sim multi-turn timeline (gate dimension c) — not started
- Current golden is a single-turn (year 181 month 1) cohort. Needs a multi-turn capture + advancing
  the Kotlin world turn-for-turn (full monthly tick + turn execution), asserting per-turn selections
  stay byte-identical across N turns.

### 3. Smaller follow-ups
- **G12** — the NATION reserved-fail deny-log string: not exercised by the month-1 window (both lords'
  reserved = 휴식); needs a crafted nation fixture.
- **Nation-type income module**: `do불가침제의`/`do선전포고` income uses an identity fold (neutral/default
  type only); a nation carrying a special `che_*` type needs its `onCalcNationalIncome` modifier
  threaded.
- The untracked `tools/php-golden/probe_*.php` are throwaway investigation scripts (not committed).

---

## Process notes (for the next phase)
- **Foundation-first + the draw catalog worked:** the unit-confidence bodies hit **0 divergences** on
  the first live-selection pass. Decompose every shared extension point into a Tier-0 wave; pin every
  RNG draw + candidate-set order from the PHP source before porting.
- **Verify by output tail, never exit code** (gradle UP-TO-DATE false-greens — use `--rerun-tasks`).
- **PHP source is grand truth — verify, don't trust the paraphrase.** The B1 parity-fatal inversion
  (the "dead-KV" claim) was caught only by reading `AutorunGeneralPolicy.php:7-39` directly.
- Java 21 LTS required (`JAVA_HOME=$(/usr/libexec/java_home -v 21)`). `legacy/` is a symlink to the
  canonical clone, git-ignored.

## Phase docs
- Research: `docs/superpowers/research/2026-05-30-p5-research.md` + `…-p5-ai-decision-draw-catalog.md`.
- Plan + review: `docs/superpowers/plans/2026-05-30-p5-npc-ai.md` + `…-REVIEW.md`.
- Divergence catalog + pins: `.context/p5-research/` (git-ignored: GATE-DIVERGENCES.md, H-HELPERS.md,
  MATERIALIZE-SPEC.md, R-*.md, G*.md).
