# OPENSAM-218 parity ADR contract review

Scope: `.agents/skills/`, `.ai/{decisions.md,known-issues.md,handoff.md}`, `.claude/`, `.coderabbit.yaml`, `CLAUDE.md`, `docs/agent/`, `docs/superpowers/{MILESTONES.md,SESSION_HANDOFF.md}`, root `build.gradle.kts`/`settings.gradle.kts`, and active source/config/help guidance in `*.{kt,kts,java,ts,tsx,js,mjs,cjs,py,sh,yaml,yml}`. Application scope is every module under `app/`, including `app/board-api`, `app/game-api`, `app/game-engine`, and `app/gateway-api`, with module `build.gradle.kts`, `src/main`, and `src/baseline` where present. The same inventory covers `common/` (`common/src`), `infra/` (`infra/src`), `logic/` (`logic/src`), `web/` (`web/game`, `web/gateway`), `tools/`, and `scripts/`. Hook/check scope includes `scripts/agent/protect-sensitive-files.sh`, `scripts/agent/codex-bash-guard.sh`, `scripts/agent/codex-post-tool-use.sh`, `scripts/agent/test-codex-agent-os.sh`, and `tools/agent-system/check.py`; historical-tool scope includes active guidance and help under `tools/php-golden/`, `tools/golden-dump/`, `tools/rng-dump/`, and repository generators such as `tools/gen_cities_seed.py`.

Independent reviewer: `/root/ticket218/op218_contract_review` (read-only `fable-deep-reasoner`).

Evidence boundary: this committed artifact is a bounded methodology and source critique. It does not claim command execution, test success, hosted-CI success, or validation of any exact commit SHA. Exact command, exit, output, SHA, and CI evidence belongs in the external task report.

The first pass found that a context-free phrase list could not prove policy direction, the added regexes were vulnerable to negation and paraphrase, and the ADR section match accepted a decoy heading. The implementation replaced that design with exact, H2-bounded machine markers: ADR-LITE-042 records the retired and retained policy state IDs, while the CLAUDE section records stable identities for the preserved six numbered rules.

The second pass found one defect in the negative fixture: it tried to corrupt both marker forms by replacing `=`, but the CLAUDE marker has no equals sign. The fixture now mutates the shared `replay_determinism` state ID, proving that both contracts fail closed. It also rejects a non-exact ADR heading.

The fresh-context pass then found active stale authorities outside the first scope: the file hook required PHP-only capture, implementation/debug prompts absolutely forbade justified expectation updates, the parity reviewer cited a deleted CLAUDE section and treated draw/log/PHP-only golden rules as universal, and the PHP harness README still declared PHP the grand truth. ADR-LITE-010 and ADR-LITE-018 also mixed still-valid product/topology decisions with authority clauses retired by ADR-LITE-042. Their status now records partial supersession so the independent decisions remain approved while only parity-authority clauses are retired. Active guidance treats PHP capture as opt-in historical comparison and permits approved expectation changes only with a reason and regression evidence.

Adversarial iterations exposed and fixed marker-preserving contradictions, conflicting duplicate markers/status lines, a prose-mutation fixture that failed for the wrong reason, negation false positives, duplicate findings, a duplicate dictionary key, and a mismatch between advisory golden-write handling and downstream enforcement. Composition probes prove that an allowed historical/negation clause cannot mask an active claim after an adversative conjunction on the same line; contextual classification is bounded to the matched clause. Retained invariant prose and the unique machine marker remain independent positive contracts, and explicit weakening fails. The strict checker requires every changed golden path to have newly added, path-keyed meaningful reason, a regression runner with an executable task/goal, concrete `PASS` result, and `CLEARED` critique; placeholder, whitespace-only reuse, generic/self-pass, unbound evidence, discovery/help/list/dry-run/no-run/skip-test modes fail. Neither evidence nor review can authorize deleting an existing test. The apply-patch and shell-aware Bash hooks block deletion of tracked test sources/fixtures and moves outside recognized test directories; golden moves remain inside the golden surface. Bash probes cover wrappers, command lists, relative `cd`, `git -C`, recursive directories, and multiple move sources. The strict checker independently consumes complete Git name-status entries and rejects both `D` and boundary-crossing `R*`. New tests and valid within-surface renames remain permitted. Static review syntax cannot independently prove that a claimed command was truthfully executed; the gate therefore combines known non-execution rejection, concrete result provenance, independent critique, and CI.

The final exact-SHA security pass replaced token-anywhere Bash inference with command-position analysis. It normalizes absolute command basenames; parses bounded `env`/`sudo`/`command`/`exec`/`nice`/`time`/`timeout` option arity and `--`; follows viable `cd`, command lists, nested shell `-c` clusters, and `git -C`; blocks unresolved mutation expansion, `find -delete`, `git update-index --force-remove`, and unknown execution wrappers around tracked-test mutation while preserving proven read-only `rg`/`grep`/`echo` controls. Contract tests independently pin literal ADR headings, markers, retained prose, and partial-supersession headings instead of importing them from the checker. Structured retained-rule aliases plus clause-level weakening predicates reject replay, one-writer, flush-delta, insertion-order, frozen-baseline, and evidence-integrity reversals even when markers remain. The stale prompt-pack reference now names `Product and regression discipline`.

The follow-up security pass closed root-operand and wrapper-working-directory gaps. `env -C`/`--chdir` and `sudo --chdir` now propagate an independently resolved working directory into the protected-path analysis. A shared tracked-descendant query makes recursive `rm` and `find -delete` reject any root containing an existing test or golden, rather than requiring the root itself to look test-like. Expanded command verbs fail closed. Git global parsing consumes or rejects operand-bearing `--git-dir`, `--work-tree`, `--namespace`, `--config-env`, and related separated/equals forms before identifying `rm`/`mv`/`update-index`. Exact inert regressions cover all of these forms.

The substitution/interpreter follow-up moved dynamic shell syntax to the trust boundary: active command/backtick/process substitutions are rejected from the raw command while single-quoted inert text remains readable. Unknown or alternate shell-style `-c` interpreters are recursively analyzed, direct `unlink` joins the destructive category, and `find -exec`/`-execdir`/`-ok`/`-okdir` fail closed for non-read-only actions over protected roots. Git rejects dynamic alias configuration, opaque `rm --pathspec-from-file`, and mutation against a noncanonical git-dir/work-tree, while retaining read-only `--version`/`--help`/`--exec-path`, `-P`/`-p status`, and `env --argv0` controls. Exact fixtures cover substitution nested under an otherwise read-only command, parameter defaults, both process-substitution directions, dynamic command verbs, alternate interpreters, find actions, alias/config-env variants, and alternate repository context.

The worktree-boundary follow-up resolves destructive operands through `realpath` and rejects anything outside the current repository root, including absolute sibling paths and in-root-looking symlinks to external trees. Git mutation additionally proves the effective `-C` top-level and absolute git-dir are the current worktree. Unknown interpreters or commands that mention a tracked protected path/ancestor fail closed, covering Perl/Python mutation APIs without enumerating them. Unquoted newlines are command separators, combined shell flags containing `c` are recursively parsed, every destructive `find` action is inspected, and persistent or environment-injected Git aliases are rejected. A synthetic external Git repository plus symlink makes sibling coverage deterministic in CI; benign interpreter, Git config read, and read-only find controls remain allowed.

The final architecture pass makes that boundary finite rather than interpreter-blacklist-dependent. The pre-hook builds one protected baseline from the union of HEAD and index, recognizes literal protected ancestors and outside-repository references, grants access only to a narrow proven-read family, and treats programmable `sed`/`awk`, unknown commands, unresolved syntax, and non-allowlisted shell scripts as opaque. Shell syntax-only checks remain available, while execution is limited to tracked official test/verify entrypoints; opaque language inline/module/stdin execution remains denied. `git worktree list` is the sole allowed worktree API. An immediate PostToolUse invariant parses NUL-delimited `git diff --find-renames --name-status -z` for base-to-HEAD, staged, and unstaged state; it rejects protected deletions and rename-outs even when an opaque program constructed the path dynamically. The strict checker uses the same NUL-safe name-status shape, while a synthetic repository E2E proves dynamic deletion and staged rename-out are caught.

The preservation follow-up denies opaque interpreter inline/module/stdin/heredoc modes before execution, including obfuscated Python and Perl path construction that contains no literal protected operand; normal repository-contained script-file execution remains available. The active product-authority surface now also includes CodeRabbit guidance, the two Kotlin compatibility comments, and the Codex user manual. Those texts retain their historical behavior references while explicitly limiting them to opt-in frozen regression evidence under ADR-LITE-042; independent required phrases and stale Korean/English authority mutations prevent a coordinated return to PHP-as-current-authority wording.

Git handling uses a finite command contract as well: destructive worktree/history rewrites such as reset, clean, switch, checkout, restore, rebase, merge, and related commands are denied before execution in the current or any sibling worktree; `GIT_CONFIG_PARAMETERS` injection and mutating `git config` are denied. Global context options are interpreted sequentially, and current-worktree proof is run with the effective `-C`/git-dir/work-tree context. Before add or commit can absorb index/worktree state, a NUL-safe prospective diff rejects protected deletion or rename-out. Fetch/push require a configured remote with an unchanged, explicit non-helper network URL; remote-helper and URL-rewrite operands fail closed. Read commands and the already structured protected-aware rm/mv handling remain explicit.

The regression-evidence parser rejects pytest discovery, fixture/marker introspection, and setup-plan modes because those commands do not execute the asserted tests. Narrow literal prose pins are intentionally retained only for the ADR authority direction and preserved invariants: semantic contradiction mutations complement them, while the exact clauses prevent checker-and-document coordinated weakening of the ticket's core policy.

The active known-issues ledger now preserves open ticket/quarantine facts as frozen-regression and product-test dispositions without mandating a full PHP port, golden capture, or byte parity. The review lifecycle attacks current approved requirements and retained architecture invariants; PHP evidence is reviewed only for an explicitly selected historical comparison. Both active surfaces have independent scope pins and stale-authority mutations.

The inventory is auditable beyond a curated file list: active `kt/kts/java/ts/tsx/js/mjs/cjs/py/sh/yaml/yml` source, config, and executable help surfaces are discovered from an independently pinned glob contract. Frozen tests, legacy source, generated/build trees, and the two scanner fixture files are explicit exclusions. Bare `PHP grand truth`, `GRAND TRUTH = PHP`, `PHP wins`, `PHP 정본`, `PHP oracle`, byte-parity, `패러티 대상`, `골든으로 잠긴`, and parity-driven-v2 forms are classified line by line with adversative-bounded historical/negation handling. Existing implementation comments retain the historical behavior they document but label it as ADR-LITE-042 frozen evidence rather than current authority. The web pages and command-log executable help receive the same classification as Kotlin. Mutation fixtures prove every bare obligation form fails in a newly discovered active TSX file while test and scanner fixtures remain explicitly outside active guidance.

The Bash boundary rejects runtime Git configuration (`-c`, `--config-env`, every `GIT_CONFIG_*` assignment), interpreter autoload environment/options, and every executing/mutating `find` action. This prevents helpers from running inside otherwise read-only Git/find commands; print-only find, syntax checks of repository-contained shell files, and tracked official test/verify shell entrypoints remain available.

The final boundary critique treats Git pathspec interpretation and process context as untrusted. Colon-prefixed magic pathspecs, case-insensitive pathspec mode, every `GIT_*_PATHSPECS` assignment, and `GIT_DIR`/`GIT_WORK_TREE` environment selectors fail closed before mutation analysis. Git editor, pager, askpass, SSH, external-diff, and proxy execution-control environments are denied. Current-worktree top-level/git-dir proof applies to every supported mutating workflow verb, including add, commit, fetch, and push, whose options follow explicit allowlists; alternate `-C`, git-dir/work-tree, bare, namespace, and super-prefix contexts cannot reuse the local workflow allowance. The strict name-status gate applies the shared protected-golden predicate to deletion as well as rename, including `src/main/resources/golden`, so review evidence cannot authorize fixture loss. Product-authority classification covers bounded PHP/legacy/Vue/FE/hwe oracle, authority, source-of-truth, parity-target, `정본`, and `오라클` forms, including PHP-to-grand-truth claims split across up to three adjacent comment lines. Historical/negated context remains match-bounded so an adversative active clause is still reviewed; unrelated multi-line parity descriptions do not become candidates.

The release-boundary follow-up parses `mv -t`/`--target-directory` before protected-source evaluation and classifies the effective `directory/basename` destination. A retained test may therefore move either into a recognized test directory or to a recognized test-source filename, while directory-only fixtures still cannot escape. Shell startup injection (`BASH_ENV`, `ENV`, `KSH_ENV`, `ZDOTDIR`) is denied before official shell entrypoints are considered. `git fetch --multiple` expands and validates every configured remote/group member; helper operands cannot hide after a valid first remote. Custom regression-runner fallback now requires an existing executable resolved inside the repository. The documented macOS Java 21 assignment is the sole command-substitution exception and is normalized before the remaining command is analyzed; other substitutions remain fail-closed.

The final input-boundary follow-up rejects `git update-index` stdin/index-info/path-file modes before any path-dependent removal decision. Attached `mv -tDIR` is parsed, unknown options fail closed, and filename-based test destinations are limited to verified single-file sources; protected directory/ancestor moves still require a recognized test directory. Interpreter-named regression evidence resolves Python/Bash/sh script operands inside the repository before accepting them, while direct custom runners additionally require executable permission. Handoff freshness is intentionally bounded to the text before the first history separator: both current sections identify the OPENSAM-206~220 integration wave and OPENSAM-218/ADR-LITE-042 lane, while the preserved OPENSAM-43 body is explicitly historical and excluded from current-authority classification.

Review method: inspect the policy markers and required prose, trace each guard/check predicate to its independently pinned mutation fixture, classify every active source/config/help hit under the declared inventory, and compare protected-path rules across apply-patch, Bash, PostToolUse, and strict name-status handling. Reproduction commands and exact outputs are intentionally omitted here; they belong in the external task report. The stable PR checks page is <https://github.com/peppone-choi/opensamguk/pull/499/checks>.

Verdict: cleared
