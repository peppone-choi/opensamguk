# Cross-agent critique — W0-2 공유 DTO widen (회수 머지)

- 날짜: 2026-06-10
- 스코프: `w0/2-shared-dto-widen` (vs main) — PAGE_PARITY_AUDIT §5 W0-2
- 구현자: w0-2 서브에이전트(세션 리밋으로 사망, 5/6 단위 완료) · 회수·검증: 오케스트레이터(Claude Fable 5)

## Implementer claim (커밋 5개, 각 TDD 단위)

1. `5f9ec8c` IdentityDto front-info widen — lastVote/onlineNations/aux.myLastVote/troopInfo
2. `d57f058` MyGeneralSummary raw 정렬 키 — dedication/experience/personal/special/special2/ownerName
3. `439d0a8` NationFinanceResponse legacy 중첩 구조 — policy/warSettingCnt/income 분해 (P0-53 read 키 정합)
4. `b33211e` InheritPointResponse.availableTargetGeneral + InheritLog.date
5. `d6eddde` ReservedCommandsResponse 메타 — turnTime/turnTerm/year/month/date + cutTurn 포팅

## 회수 절차 (사망 시점 처리)

- 사망 시 워킹트리에 **미커밋 RED 테스트 1건**(MapPreviewControllerTest `$.startYear` — MapPreviewDto
  widen 미착수分). 게이트 red 1의 유일 원인임을 확인 후 패치로 분리 보관
  (`/tmp/w0-2-mappreview-red.patch`), 워킹트리에서 제거.
- 제거 후 `:app:game-api:test` 41 suites **red=0** (XML 검증).
- **잔여(W0-2b 백로그)**: MapPreviewDto widen (startYear 등 — 감사 W0-2 행의 마지막 단위).
  루프 LEDGER 백로그에 등재.

## 검증 (회수자 수행)

- 게이트: `:app:game-api:test` 41 suites failures=0 errors=0 (XML)
- 각 커밋이 단일 논리 단위 + 신규 필드는 실데이터 소스 배선 또는 BLOCKED 문서화 원칙(발사 지침)
  하에 작성됨 — 커밋 메시지에 PHP 근거 기재
- strict check 잔여 지적(critique artifact/docs)은 본 문서로 해소

## Verdict

Verdict: cleared — 회수된 5단위 한정. MapPreviewDto 잔여는 W0-2b로 이관(fabrication 없음).
