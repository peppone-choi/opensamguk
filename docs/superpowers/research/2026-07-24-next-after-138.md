# Next ticket after OPENSAM-138 PR #314 (main tip `74ec5aad`)

Date: 2026-07-24
Role: research only (no production code edits)
Worktree: active (`peppone-choi/op-139-minversion-barrier` tip == main tip)
Tracker: GitHub jira-mirror issues (Atlassian MCP unavailable this run)
Do-not (held): prod deploy/cutover, golden fabricate, force-merge, edit `.codex/config.toml` / `max_threads`

---

## 0. Executive recommendation

**Process OPENSAM-139 / #285 (`ARCH-S5-T3`) first.**

| Option | Verdict |
|--------|---------|
| **OPENSAM-139 / #285 committedWorldVersion + minVersion primary-read barrier** | **Do next (implement)** |
| Finish OPENSAM-138 residual 3-run JFR heap proof before any coding | **Defer as hygiene residual** — not a hard gate for S5-T3 |
| OPENSAM-140 / #286 cutover | **Blocked** — plan deps include S5-T2 **and** S5-T3 |
| #271 / OPENSAM-125 capacity | **Parallel later / activation** — gates prod activation language, not S5-T3 coding |
| S6 shadow/canary | **After** S5-T3 + S6-T1 deps |

Proof below: tip has #314; S5-T3 depends on S4 durability foundations (closed build-only), not on JFR residual; plan wave W4 allows S5-T3 in parallel with/after S5-T2 build slice; worktree branch name already targets 139.

---

## 1. Confirm OPENSAM-138 on tip — commit/PR evidence + residual AC

### 1.1 Git / PR evidence (this worktree)

| Fact | Value |
|------|--------|
| `HEAD` / `origin/main` | `74ec5aade7a78d610dd976d4bb8604ff7ef8d0ab` |
| Merge commit subject | `Merge pull request #314 from peppone-choi/peppone-choi/op-138-s5-t2-bounded-boot` |
| Feature commit | `f325944bf0074b3051f9c55c7a29fbd196c829a3` — `fix(OPENSAM-138): bound boot archive reads` |
| Parent tip before merge | `3d432bfc` — Merge PR #313 (OPENSAM-137) |
| PR | https://github.com/peppone-choi/opensamguk/pull/314 — **MERGED** `2026-07-24T03:10:23Z` |
| PR title | `fix(OPENSAM-138): bound boot archive reads` |
| Independent review | `docs/superpowers/reviews/2026-07-23-opensam-138-bounded-boot-review.md` → **Verdict: cleared** |
| Design | `docs/superpowers/plans/2026-07-23-opensam-138-bounded-boot-design.md` |
| Catalog stamp | `logic/.../HotColdCatalog.kt:4` → `version = "ARCH-S5-T2-2026-07-23"` |

**What landed (build-only S5-T2 slice):**

| Area | Path (tip) | Role |
|------|------------|------|
| Boot loader | `app/game-engine/.../boot/WorldSnapshotLoader.kt` | Drop eager cold full-history boot; strip cold meta keys (`statisticRows`/`nationHistory`/`generalHistory`/`globalLogs` ~L500) |
| On-demand cold | `infra/.../read/ArchiveHistoryReader.kt`, `StatisticSnapshotReader.kt` | Bounded archive/statistic reads |
| Flush-time history | `infra/.../persistence/JdbcFlushExecutor.kt` | Pending-history merge + exact-key SQL in flush txn |
| Catalog + guard | `logic/.../memory/HotColdCatalog.kt`, `.../HotColdWorldCatalogGuardTest.kt` | S5-T2 catalog + architecture guard (10 tests green per PR) |
| Archive IT | `.../WorldSnapshotLoaderArchiveIT.kt` | 2 tests green |

PR verification claim (from #314 body): focused Java 21 gate BUILD SUCCESSFUL ~9m; XML HotColdWorldCatalogGuardTest 10/0/0/0; WorldSnapshotLoaderArchiveIT 2/0/0/0; agent-system check Errors 0 / Warnings 0.

### 1.2 GH issue #284 AC (GWT) vs landed code

Source: GH jira-mirror **#284 OPEN** / plan `ARCH-S5-T2` in
`docs/superpowers/plans/2026-07-18-cqrs-memory-consistency-hardening-plan.md`.

| GWT / verify claim | After PR #314? | Notes |
|--------------------|----------------|-------|
| No unbounded full-history **boot** SQL for cold statistic/history/global-log | **YES (build)** | Loader no longer eagers those; cold readers + flush-time SQL |
| Cold reads use keyset / bounded projection / aggregate + stable order | **YES (build)** | e.g. `StatisticSnapshotReader` LIMIT/pick pattern; `ArchiveHistoryReader` exact-key |
| Same hot cardinality, 1× vs 10× cold-history → retained heap delta ≤ ~5% after full GC | **NO (residual)** | Not measured in the #314 worker pass |
| **3-run heap/JFR comparison** (ticket verify method) | **NO (residual)** | Review residual: “Literal 3-run JFR/full-GC retained-heap comparison from GH #284 was not run” |
| Broad `verify-changes.sh --run` full matrix green | **NO (tool residual)** | Terminated exit 143; last tail `:infra:compileKotlin` — tooling stall, not counted green |
| Phase-hot auction/tournament live prefetch | **N/A to 138** | Still 137 residual (`activationReady = false`) |
| Prod deploy / cutover | **NO (forbidden)** | Explicit non-goal |

### 1.3 Closeability ruling for 138

**CLOSEABLE as build-only complete — yes** (S4 hygiene pattern: foundation + focused tests + cleared review on main).
**CLOSEABLE as full GWT including ops heap proof — no** until 3-run JFR residual is recorded or explicitly waived with ADR.

Treat tip `74ec5aad` as **coding-complete for S5-T2 boot-scan removal**. Residual is **measurement/hygiene**, not missing boot-scan code.

---

## 2. #283 / #284 hygiene-close recommendation (comment text only)

### 2.1 Clarification: these are issues, not open PRs

| Tracker ID | Kind | Title | State (research time) | Code PR already merged |
|------------|------|-------|----------------------|------------------------|
| **#283** | GitHub **issue** (`jira-mirror`) | [OPENSAM-137] Hot/cold catalog… | **OPEN** | PR **#313** MERGED |
| **#284** | GitHub **issue** (`jira-mirror`) | [OPENSAM-138] Remove full-history boot scans… | **OPEN** | PR **#314** MERGED |
| Open **PRs** | — | (empty list this run) | — | — |

There is no open code PR #283/#284 to merge. Residual is **tracker hygiene**: jira-mirror issues still OPEN after build PRs merged.

### 2.2 Should they hygiene-close as build-only?

| Issue | Hygiene-close as build-only? | Residual that must be named |
|-------|------------------------------|-----------------------------|
| **#283 / OPENSAM-137** | **Yes (recommend)** | Phase-hot runtime prefetch still `activationReady=false` (open auctions / tournament KV); CI “activation” language still soft-gated by **#271 / S1-T3** |
| **#284 / OPENSAM-138** | **Yes (recommend)** | **3-run JFR / full-GC retained-heap 1× vs 10× proof** not attached; broad verify-changes matrix stall (tooling); do not claim ops capacity certified |

S4 precedent already closed: #279/#280/#281/#282 (OPENSAM-133…136) **CLOSED** as build-only after code PRs.

### 2.3 Recommended close comment text (do not auto-post unless coordinator asks)

**For #283:**

> Closed as **build-only complete** on main by PR #313 (`3d432bfc`). Evidence: `HotColdCatalog` + `HotColdWorldCatalogGuardTest`, review Verdict: cleared. Residual **not** closed: phase-hot runtime prefetch for open auctions / tournament KV (`activationReady=false`); CI activation language remains gated by #271. Full-history boot scan removal was **#284 / S5-T2** (now on tip via PR #314). No deploy/cutover.

**For #284:**

> Closed as **build-only complete** on main by PR #314 (`74ec5aad` / feature `f325944b`). Evidence: bounded boot archive path (`WorldSnapshotLoader` cold strip + `ArchiveHistoryReader`/`StatisticSnapshotReader` + flush-time history), `HotColdWorldCatalogGuardTest` 10/0/0, `WorldSnapshotLoaderArchiveIT` 2/0/0, review Verdict: cleared. Residual **not** closed: **3-run JFR/full-GC retained-heap comparison** (1× vs 10× cold fixtures, ≤5% or S1-T3-approved threshold) not executed in the implement pass; broad `verify-changes.sh` matrix stall is tooling residual. No deploy/cutover/golden changes.

**This research worker did not mutate GH** (prefer recommend-only).

---

## 3. Ranked next implement tickets (path:line map + ARCH plan dependency)

Plan source: `docs/superpowers/plans/2026-07-18-cqrs-memory-consistency-hardening-plan.md`
Contract source: `docs/superpowers/specs/2026-07-18-cqrs-consistency-failure-contract.md`

### Rank 1 — **OPENSAM-139 / #285 / `ARCH-S5-T3`** (recommended next implement)

| Field | Value |
|-------|--------|
| User value | Post-command reads must not succeed with stale state |
| Plan deps | `ARCH-S4-T3`, `ARCH-S3-T2`, `ARCH-S2-T2` — S4 durability tickets **CLOSED** as build-only (#281 etc.); scoping/CAS foundations already on main |
| Wave | Plan §6 W4: S5-T1 and **S5-T3 are separable after durability gate**; S5-T2 boot-scan build is on tip |
| Blocks | S6-T1 (#286) lists deps **including S5-T3** |

**Path:line implementation surface (tip inventory):**

| Seam | Path:line | Gap for 139 |
|------|-----------|-------------|
| Engine stamps commit watermark on results | `app/game-engine/.../run/TurnRunService.kt:243` | intake path `committedWorldVersion = state.worldVersion + 1` |
| Tick path commit watermark | `TurnRunService.kt:365-368`, `:551`, `:584` | EXECUTION/intake rows carry version |
| Writer CAS fence (existing S3) | `TurnRunService.kt:525-528` | `expected_world_version` / `writer_epoch` already stamped |
| Flush bumps `world_state.world_version` | `infra/.../JdbcFlushExecutor.kt:363-420` | CAS UPDATE already exists |
| Persist `committed_world_version` | `JdbcFlushExecutor.kt:2116`, `:2132-2143`; row type `:2374` | outbox/result rows |
| Command result repository | `infra/.../CommandResultRepository.kt:89` | insert binding |
| API admission terminal still **zeros** version | `app/game-api/.../reserve/CommandTerminalResultFactory.kt:41` | `committedWorldVersion = 0` — not a substitute for engine terminal |
| Read entity **missing world_version column** | `app/game-api/.../read/WorldStateReadRepository.kt:21-63` | `WorldStateReadEntity` has no `world_version` field — **must add** for primary barrier |
| Process-world read | same file `:70-83` | `findProcessWorld()` is primary-scoped read entry |
| RYW post-command screen (contract) | `docs/.../cqrs-consistency-failure-contract.md:140-151`, GA-040 MyController | classify RYW; `minVersion` not implemented |
| Command result AUTHORITATIVE (no minVersion) | contract GA-075 `CommandController` | durable result lookup ≠ state RYW |
| Eventual ranking example | `app/game-api/.../rank/RankReadService.kt:53+` | EVENTUAL class when no minVersion |
| Cold readers (reuse primary pool, no new SoT) | `infra/.../read/ArchiveHistoryReader.kt`, `StatisticSnapshotReader.kt` | logical read/write pool split later; both stay on PostgreSQL primary for now |
| Spec UNKNOWN for HTTP shape | contract L144 | HTTP status/wait/response schema for `VERSION_NOT_VISIBLE` is ticket design work |

**Acceptance (GWT abbreviated):**

1. Authoritative/RYW read with `minVersion=V` succeeds only when primary scoped `world_state.version >= V`; else bounded wait → `VERSION_NOT_VISIBLE` + current/required + retry hint.
2. EVENTUAL endpoints without minVersion remain allowed; precheck advisory ≠ engine final.
3. No physical replica; logical pool boundary only; no async projector as new source of truth.

**Verify:** concurrent write/read IT; version timeout contract tests; endpoint consistency classification; primary routing assertion.

### Rank 2 — OPENSAM-138 residual measurement (hygiene, not next coding epic)

| Work | Why ranked #2 not #1 |
|------|----------------------|
| 1×/10× cold fixture + 3-run full-GC/JFR heap delta ≤5% | Completes full GWT for #284 but does not unblock RYW user value; can run in parallel ops window |
| Optional attach artifact under `docs/superpowers/evidence/` | Coordinator/ops; not production code path for 139 |

### Rank 3 — OPENSAM-137 residual phase-hot prefetch

| Seam | Catalog | Note |
|------|---------|------|
| Open auctions month | `HotColdCatalog` PHASE_HOT ~L240 `activationReady=false` | Separate from 139 |
| Auction expiry / tournament KV | PHASE_HOT ~L276 / ~L336 | Soft-gated by #271 for “activation complete” language |

### Rank 4 — OPENSAM-140 / #286 / `ARCH-S6-T1` cutover

| Field | Value |
|-------|--------|
| Deps | `ARCH-S2-T4`, `S3-T3`, `S4-T4`, **`S5-T2`**, **`S5-T3`** |
| Verdict | **Do not start** until 139 lands (and 138 build residual is accepted) |
| Why not next | Expand/backfill/point-of-no-return is ops-adjacent; plan forbids cutover without S5 complete |

### Rank 5 — #271 / OPENSAM-125 / `ARCH-S1-T3` capacity

| Field | Value |
|-------|--------|
| Role | Heap warning/critical, boot admission, Micrometer metrics including version lag / minVersion wait |
| Verdict | Valuable parallel **after or beside** 139 metrics hooks; **not** preferred next implement ticket for RYW barrier |
| Coupling | 138’s 5% threshold may later be refined by S1-T3; coding of 139 does not require it |

---

## 4. Explicit recommendation with proof

### Recommendation

**Implement OPENSAM-139 / #285 (`ARCH-S5-T3`) next.**

### Proof chain

1. **Tip is post-138:** `74ec5aad` = merge of PR #314; catalog version string is `ARCH-S5-T2-2026-07-23`.
2. **S5-T2 coding critical path is done for boot scans:** cold full-history boot removed; residual is measurement (JFR), which S4-style hygiene does not block the next build ticket.
3. **S5-T3 plan deps are satisfied at build-only level:** S4-T1…T4 issues #279–#282 are **CLOSED**; CAS fence + `committedWorldVersion` stamping already exist on the engine/flush path.
4. **Plan wave W4** explicitly separates S5-T3 from waiting on full S5-T2 ops heap ceremony; S6-T1 **requires** S5-T3.
5. **Gap is concrete and mapped:** no `minVersion` / `VERSION_NOT_VISIBLE` Kotlin implementation; `WorldStateReadEntity` omits `world_version`; contract already designates RYW (GA-040) vs EVENTUAL vs AUTHORITATIVE.
6. **Worktree already named** `peppone-choi/op-139-minversion-barrier` at tip — session intent aligns with this pick.
7. **Alternatives lose:**
   - #140 cutover: hard-deps S5-T3.
   - #271 capacity: activation/ops, not RYW product path.
   - JFR residual alone: no user-visible command-after-read safety.

### Suggested first implement slice (for next worker; not executed here)

1. Design HTTP/error body for `VERSION_NOT_VISIBLE` (contract currently UNKNOWN shape).
2. Map `world_version` on `WorldStateReadEntity` + primary reader helper `currentWorldVersion()`.
3. Shared `MinVersionPrimaryBarrier` (bounded wait poll on primary) used by RYW endpoints; start with GA-040 `GET /api/my-page` (or the contract’s designated post-command state screen).
4. Ensure engine terminal results (not API admission `committedWorldVersion=0`) are what clients use as `minVersion`.
5. Concurrent IT + classification matrix tests.
6. Independent adversarial review; no deploy.

---

## 5. Do-not list

| Forbidden | Why |
|-----------|-----|
| **Production deploy / cutover** | S6 + human GO only; S5 build-only closes exclude deploy |
| **Golden fabricate / weaken golden tests** | Parity discipline; PHP capture only |
| **Force-merge** | No override of failed durability/parity gates |
| **Edit `.codex/config.toml`** (incl. `max_threads`) | Pre-existing Agent OS WIP; baseline-separated; guard fails if touched |
| **Prod multi-world cutover / second-world admission** | OPENSAM-140 / S6; blocked on S5-T3 |
| **Quiet-close #283/#284 without residual text** | Phase-hot + JFR residuals are real |
| **Treat 3-run JFR numbers as inventable** | Measure with fixtures or leave residual explicit |
| **Introduce async projector / replica as new source of truth** | S5-T3 GWT forbids; replica ADR is S6-T3 only |
| **Pull/reset/hard main further without need** | Task constraint; tip already at expected `74ec5aad` |

---

## 6. Sources

| Kind | Ref |
|------|-----|
| Main tip | `74ec5aad` Merge PR #314 |
| Feature | `f325944b` fix(OPENSAM-138) |
| PRs | #314 MERGED (138), #313 MERGED (137) |
| GH issues | #283 OPEN, #284 OPEN, #285 OPEN, #286 OPEN, #271 OPEN; #279–#282 CLOSED |
| Plan | `docs/superpowers/plans/2026-07-18-cqrs-memory-consistency-hardening-plan.md` (S5-T2/T3, S6-T1, §6 waves) |
| Contract | `docs/superpowers/specs/2026-07-18-cqrs-consistency-failure-contract.md` (RYW / VERSION_NOT_VISIBLE) |
| 138 design/review | `docs/superpowers/plans/2026-07-23-opensam-138-bounded-boot-design.md`, `docs/superpowers/reviews/2026-07-23-opensam-138-bounded-boot-review.md` |
| Prior pick | `docs/superpowers/research/2026-07-23-next-after-137.md` (recommended 138; now done) |
| Catalog | `logic/.../HotColdCatalog.kt` |
| Loader / readers | `WorldSnapshotLoader.kt`, `ArchiveHistoryReader.kt`, `StatisticSnapshotReader.kt` |
| Engine version stamp | `TurnRunService.kt` |
| Read gap | `WorldStateReadRepository.kt` (no `world_version`) |

---

## 7. Bottom line

Main tip **`74ec5aad` has OPENSAM-138 / PR #314 merged** as a **cleared build-only** S5-T2 slice; **residual AC** is primarily the **3-run JFR heap proof** (plus 137 phase-hot residual). Hygiene-close **#283 and #284** as build-only with residual comments (recommend only). **Next implement ticket: OPENSAM-139 / #285 primary-read barrier** — path:line map above; plan deps met at build-only; unblocks S6 cutover later. Do not deploy, fabricate goldens, force-merge, edit `.codex/config.toml`, or start prod cutover.
