# Next ticket after OPENSAM-137 PR #313 (main tip `3d432bfc`)

Date: 2026-07-23
Role: research only (no production code edits)
Branch context: `peppone-choi/op-138-s5-t2-bounded-boot` tracks `origin/main` at merge tip `3d432bfca68393e31eb5a414e927bc12f5cac914`
Tracker source: GitHub jira-mirror issues (Atlassian MCP unavailable this run)
Do-not (held): prod deploy/cutover, golden fabricate, force-merge, edit `.codex/config.toml` / `max_threads`

---

## 0. Executive recommendation

**Process OPENSAM-138 / #284 (`ARCH-S5-T2`) first this session.**

Treat OPENSAM-137 / #283 as **build-only foundation complete on main after PR #313**, with an **explicit residual checklist** (phase-hot runtime prefetch migration + later CI activation), **not** as full GWT-closed “prefetch live.” Do **not** block S5-T2 on finishing auction/tournament phase-hot cutover first: the catalog already classifies those as `PHASE_HOT` with `activationReady = false`, and design §D gates true activation/cutover on still-open `ARCH-S1-T3` / #271.

| Option | Verdict |
|--------|---------|
| Finish 137 residual (phase-hot prefetch) before any 138 | **Defer** — valuable but not the dependency critical path for boot-scan removal; activation soft-gated by #271 |
| Start 138 now using `LEGACY_FULL_SCAN_PENDING_S5_T2` worklist | **Do this session** |
| Close #283 silently with no residual note | **No** — residual must be named |

---

## 1. Is OPENSAM-137 / #283 CLOSEABLE as build-only complete after PR #313?

### 1.1 What landed on main (PR #313)

| Fact | Evidence |
|------|----------|
| PR | https://github.com/peppone-choi/opensamguk/pull/313 — **MERGED** `2026-07-23T13:48:01Z` |
| Merge commit / main tip | `3d432bfca68393e31eb5a414e927bc12f5cac914` |
| Title | `feat(OPENSAM-137): hot/cold catalog and architecture guard (build-only)` |
| Catalog | `logic/src/main/kotlin/opensamguk/logic/memory/HotColdCatalog.kt` (`version = ARCH-S5-T1-2026-07-23`) |
| Guard | `app/game-engine/src/test/kotlin/opensamguk/engine/boot/HotColdWorldCatalogGuardTest.kt` |
| Focused proof | `HotColdWorldCatalogGuardTest` XML: **9 tests, 0 fail / 0 error / 0 skip** (`BUILD SUCCESSFUL`) |
| Independent review | `docs/superpowers/reviews/2026-07-23-opensam-137-hot-cold-catalog-review.md` → **`Verdict: cleared`** |
| Gate re-review | `.omo/evidence/opensam-137-gate-review.md` → **APPROVE / cleared; AC-2 resolved** |
| PR self-scope | “Closes nothing for #283 … build-only slice only”; non-goals: no production prefetch, no full-history boot scan removal, no deploy |

Also landed with the PR: design `docs/superpowers/plans/2026-07-23-opensam-137-hot-cold-prefetch-design.md`, inventory research `docs/superpowers/research/2026-07-23-opensam-137-hot-cold-catalog.md`, prior triage `docs/superpowers/research/2026-07-23-ticket-triage-next.md`, `.ai/*` session notes.

### 1.2 Ticket AC (GWT) vs landed code

Source: GH jira-mirror **#283** (OPEN) / plan `ARCH-S5-T1` — body matches plan GWT.

| GWT claim | Landed after #313? | Notes |
|-----------|--------------------|-------|
| **always-hot / phase-hot / query-only cold 데이터가 명시** | **YES** | `HotColdCatalog.snapshotAccesses` + `runtimeReadSeams` + temperatures |
| **phase-hot 데이터는 bounded keyset/limit로 phase 시작 전에 stable order prefetch** | **NO (residual)** | Three seams tagged `PHASE_HOT`, `activationReady = false`; still live repository calls mid-boundary |
| **RNG draw / entity iteration 내부 lazy SQL 0** | **Partial / inventory only** | Guard prevents *uncataloged* new reads; does **not** yet force open-auction reads out of month/command phase bodies |
| **Catalog-missing new access → architecture test fails** | **YES (AC-2)** | Default-deny repository + typed JDBC detection; review **cleared** |
| Verify via inventory + query-count + ordering + RNG/log golden | **Inventory + guard only** | No phase-hot migration parity gate run as part of #313 (non-goal) |

### 1.3 Closeability ruling

**CLOSEABLE as build-only complete — yes, under the S4 hygiene pattern used for #279/#280.**
**CLOSEABLE as full GWT / “prefetch done” — no.**

S4 precedent (e.g. #279 close comment): close when the **build foundation + tests + cleared review** are on main; record **activation / ops residuals** explicitly; forbid deploy/cutover.

Apply the same pattern to #283:

- **Closeable now** with a comment that cites PR #313, tip `3d432bfc`, review `Verdict: cleared`, focused guard XML 9/0/0, and residual checklist below.
- **Must not** claim “deterministic phase-boundary prefetch is production-true” or “zero lazy SQL for open auctions.”
- PR #313 already stated it does not auto-close #283; a **human/coordinator close** (or residual-ticket split) is still required.

**Must remaining phase-hot prefetch finish first?**
**No**, not as a hard gate for *build-only close* or for *starting S5-T2*.
**Yes**, if the goal is *full GWT green for S5-T1 title string* including live prefetch. That residual is real but **separable**.

Design §D non-goals (design doc) reinforce:

- Activation/cutover of production call sites and merge-blocking gate enablement wait on **`ARCH-S1-T3` / #271** (still **OPEN** at research time).
- Full-history boot scan removal is **S5-T2**, not S5-T1.

---

## 2. If 137 residual remains: minimal remaining slice vs deferral

### 2.1 Minimal remaining S5-T1 residual (do not invent scope)

From `HotColdCatalog` `PHASE_HOT` + design §E step 4:

| Seam | File / call | Catalog note |
|------|-------------|--------------|
| Open auctions (month) | `MonthlyPostUpdateHook` → `auctionRepository.findByFinishedFalse` | followUp: move to ordered `PhaseHotPrefetchStep` |
| Open auctions (expiry) | `AuctionExpiryDaemon` → same open set | share ordered phase cache |
| Tournament KV | `TournamentDaemon` → `gameKvRepository.findByTable` | bounded tournament KV projection |

**Minimal code slice (when executed later):**

1. Ordered open-auction reader (explicit `ORDER BY id ASC` or equivalent stable history order; active `finished = false` bound).
2. `PhaseHotPrefetchStep` + short-lived phase cache populated immediately before month/phase body (`TurnRunService` before `pipeline.runMonth(...)` per design §B).
3. `registerAuction` / expiry path read cache only — no mid-body repository open-set scan.
4. Optional tournament KV bounded projection (same ticket residual or immediately subsequent micro-slice).
5. Focused architecture tightening if any allowlist still exists; **parity gate** for month-phase RNG/log byte-identity after behavior-touching migration.
6. **Do not** claim CI merge-blocking activation complete while #271 is open (design §0 / §D / §E.6).

### 2.2 Explicit deferral (recommended disposition)

| Residual | Owner after this research | Why defer this session |
|----------|---------------------------|------------------------|
| Phase-hot open-auction + tournament prefetch migration | **S5-T1 residual checklist** on #283 (or child comment), not S5-T2 | S5-T2 is boot retention/heap; mixing doubles parity surface |
| Guard/tests as merge-blocking “activation” language | Wait **#271** / S1-T3 capacity policy | Design hard non-goal |
| Full-history boot scans | **S5-T2 / #284** | Catalog already marks them `LEGACY_FULL_SCAN_PENDING_S5_T2` |
| Prod deploy / cutover | S6 / ops | Standing do-not |

**Suggested tracker wording (coordinator, not executed by this research worker):**

> Closed as **build-only complete** on main by PR #313 (`3d432bfc`). Evidence: `HotColdCatalog` + `HotColdWorldCatalogGuardTest` (9/0/0), review `Verdict: cleared`. Residual **not** closed: phase-hot runtime prefetch for open auctions / tournament KV (`activationReady=false`); CI activation remains gated by #271. Full-history boot scan removal is **#284 / S5-T2**, not residual code missing from the catalog foundation.

---

## 3. OPENSAM-138 / #284 (`ARCH-S5-T2`) readiness

### 3.1 Dependency on S5-T1

| Source | Dependency statement | Interpretation after #313 |
|--------|----------------------|---------------------------|
| Plan `ARCH-S5-T2` | 의존성: **`ARCH-S5-T1` 완료** | Catalog + guard foundation is the S5-T1 artifact S5-T2 consumes |
| Design §A.3 / §D | S5-T1 **records** `QUERY_ONLY_COLD` + does **not** delete boot scans; S5-T2 **removes** them | Inventory is the handoff |
| GH #284 | Same GWT + “ARCH-S5-T1 완료” | Treat **build-only S5-T1 foundation** as sufficient to **start** S5-T2 coding; optional formal #283 close can land in parallel hygiene |

**Readiness: YES to start S5-T2 implementation this session**, provided implementers:

1. Consume catalog `LEGACY_*` / followUp fields as the worklist (single source of truth).
2. Do not require live phase-hot auction prefetch first.
3. Keep parity: no golden fabricate; fix Kotlin if byte mismatch; prefer existing game-api cold readers over inventing daemon dual truths.

### 3.2 Map: `LEGACY_FULL_SCAN_PENDING_S5_T2` from `HotColdCatalog`

Boot snapshot entries (primary S5-T2 removal/bound targets):

| `methodName` | `relation` | Catalog followUp (abridged) | Loader evidence (`WorldSnapshotLoader`) |
|--------------|------------|-----------------------------|----------------------------------------|
| `loadStatisticRows` | `statistic` | Replace full-history boot scan with bounded history projection | `SELECT … FROM statistic ORDER BY id ASC` (no LIMIT) |
| `loadNationHistory` | `log_entry:NATION/HISTORY` | Prefetch only deterministic history window needed by turn logic | `FROM log_entry` nation history, ordered, **unbounded** |
| `loadGeneralHistory` | `log_entry:GENERAL/HISTORY` | Same windowing | general history, **unbounded** |
| `loadGlobalLogs` | `log_entry:SYSTEM/HISTORY,ACTION` | Bounded cold window | system history/action, **unbounded** |
| `loadInheritancePoints` | `game_kv:inheritance` | Key by owner / bounded active owners | inheritance KV full table scan pattern |

Related catalog follow-ups often co-touched in S5-T2 (not all bound=`LEGACY_*` but called out for later binding):

| Entry | Why relevant to S5-T2 |
|-------|------------------------|
| `loadRankValues` (`rank_data`, ALWAYS_HOT / HOT_KEYSET) | followUp: bind to loaded general ids before removing full-history patterns |
| `loadGameEnv` / `loadNationEnv` | followUp: world-scoped bounded projection when cohort is bounded |
| Runtime `RehydrateService` direct SQL | Cold recovery boundary; convert to cataloged bounded projections before activation language |
| Runtime KV table-wide scans in command/tournament ports | Named bounded projections (S5-T2 followUp on several seams) |
| Boot allocator `auctionRepository.findAll` | Replace max-id `findAll` with aggregate reader |

Guard test already asserts every `LEGACY_FULL_SCAN_PENDING_S5_T2` snapshot entry has an S5-T2 followUp string — the machine-readable backlog is in-tree.

### 3.3 S5-T2 AC (GWT) reminder (#284 OPEN)

1. Same hot cardinality, 1× vs 10× cold-history fixtures → retained heap delta ≤ ~5% (or S1-T3 approved threshold); **no unbounded full-history boot SQL**.
2. Cold reads use keyset / bounded projection / aggregate; **stable ordering + PHP byte parity**.

### 3.4 Risks

| Risk | Detail | Mitigation |
|------|--------|------------|
| **Parity** | History order, log feed shape, inheritance maps, rank_data attachment order are parity-sensitive if any turn/AI/path still reads boot-copied `state.meta` history | Prove no RNG/entity-iteration consumer of nation/general/global history meta before delete (design greps already claim MonthlyPostUpdateHook / GeneralAI / ProcessWarNG / AiTurnAdapter do not); run `tools/parity/gate.sh backend` after each removal |
| **game-api cold paths already exist** | `LogFeedReadRepository`, `AdminGeneralLogReadRepository` already query `log_entry` for read API — **do not** invent a second daemon oracle; re-point or delete daemon eager copy so one cold truth remains | Prefer reuse/adapt of game-api read SQL shapes; keep daemon write path JDBC flush-only rule |
| **Hidden consumers** | Command/admin paths or future code may assume `InMemoryTurnWorld` meta always holds full history | Catalog + guard + call-site audit before delete; fail tests if uncataloged access appears |
| **Heap fixture / S1-T3 threshold** | Absolute 5% may be refined by still-open #271 | Use plan default 5% unless S1-T3 publishes another approved number; do not block coding on #271 for **removal of scans**, only for capacity *admission policy activation* language |
| **Scope bleed into 137 residual** | Implementing auction phase-hot cutover inside 138 | Keep 138 focused on boot/history retention; leave phase-hot to 137 residual |
| **Dual dirty truth** | Accidental JPA writes while reshaping loaders | Preserve one-daemon-write rule (`ChangeRecorder` → `JdbcFlushExecutor` only) |

---

## 4. Explicit session choice

| Priority this session | Ticket | Why |
|-----------------------|--------|-----|
| **1 — implement** | **OPENSAM-138 / #284 `ARCH-S5-T2`** | Main tip has catalog worklist; branch name already `op-138-s5-t2-bounded-boot`; story value is bounded boot heap; S5-T1 foundation + cleared review landed |
| **2 — tracker hygiene (coordinator)** | OPENSAM-137 / #283 | Close **build-only** with residual checklist **or** leave OPEN with same residual text; either is fine so long as residual is not forgotten |
| **3 — later / gated** | 137 phase-hot prefetch residual | After or parallel to 138 micro-slices; full activation language after #271 |

**Do not** open S6 coding or prod cutover as “next after 137.”

---

## 5. Do-not (reaffirmed)

| Forbidden | Why |
|-----------|-----|
| Production deploy / cutover | S6 / ops GO only; build-only closes exclude deploy |
| Golden fabricate / weaken tests | Parity discipline; PHP capture only |
| Force-merge | No override of failed durability/parity gates |
| Edit `.codex/config.toml` or `max_threads` | Pre-existing Agent OS concurrency WIP; baseline-separated from S5 work |
| Quiet-close #283 without residual text | Phase-hot residual is real |
| Fabricate S5-T2 heap numbers without fixtures | Measure with 1×/10× cold fixtures + full GC protocol |

---

## 6. Sources

| Kind | Ref |
|------|-----|
| Main tip | `3d432bfc` Merge PR #313 |
| PR | https://github.com/peppone-choi/opensamguk/pull/313 |
| GH jira-mirror | #283 OPENSAM-137 OPEN; #284 OPENSAM-138 OPEN; #271 S1-T3 OPEN |
| Catalog | `logic/.../HotColdCatalog.kt` |
| Guard | `app/game-engine/.../HotColdWorldCatalogGuardTest.kt` |
| Review | `docs/superpowers/reviews/2026-07-23-opensam-137-hot-cold-catalog-review.md` (`Verdict: cleared`) |
| Design | `docs/superpowers/plans/2026-07-23-opensam-137-hot-cold-prefetch-design.md` |
| Plan | `docs/superpowers/plans/2026-07-18-cqrs-memory-consistency-hardening-plan.md` (`ARCH-S5-T1`/`T2`) |
| Prior inventory | `docs/superpowers/research/2026-07-23-opensam-137-hot-cold-catalog.md` |
| Loader | `app/game-engine/.../boot/WorldSnapshotLoader.kt` |
| Cold read examples | `app/game-api/.../LogFeedReadRepository.kt`, `AdminGeneralLogReadRepository.kt` |
| Session | `.ai/task.md`, `.ai/current-state.md` (S5 catalog section) |
| S4 close pattern | #279 build-only close comment after PR #312 |

---

## 7. Bottom line

PR #313 delivered a **cleared build-only S5-T1 foundation** (catalog + default-deny guard) on main tip `3d432bfc`. Full ticket GWT still lists **phase-hot prefetch residual**, but that residual is **not** a prerequisite for starting **S5-T2 boot-scan removal**. **Next coding ticket this session: OPENSAM-138 / #284.** Close or annotate #283 as build-only with residual; do not deploy, fabricate goldens, force-merge, or touch `.codex/config.toml`.
