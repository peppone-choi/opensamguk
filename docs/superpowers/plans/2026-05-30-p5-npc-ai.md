# P5: GeneralAI NPC Autorun — the one-RNG-per-general decision spine, the three dispatcher chains, 4-layer policy merge, derived-state cache, ~50 `do<한글>` command-selection families, the constraint bridge (precheck == daemon), and the fixed-seed full-game draw-for-draw Gate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`. Execute each task as a checkbox step (`- [ ]`), **TDD red→green** (write the failing test first, then the impl), **one logical commit per task**, ending every commit message with the `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` trailer. The orchestrator (not you) creates branches and commits after review; treat the inline `git`/commit lines as the intended commit boundaries, not as instructions to run git yourself. **Verify build success by tail/grep of the gradle output, never by exit code** (project memory: `task-notification` exit 0 is unreliable — pipe `... 2>&1 | tail -40` and grep for `BUILD SUCCESSFUL` / the test count). Every gradle invocation MUST be prefixed `JAVA_HOME=$(/usr/libexec/java_home -v 21)` — Gradle 8.x fails to parse Java 25. Run from repo root `/Users/apple/conductor/workspaces/opensamguk/bogota` via `./gradlew`. **Do NOT re-port the P0-B/P1/P2/P3/P4 substrate** — `:common` (`rng/RandUtil` + `rng/LiteHashDrbg` byte-exact + `rng/SeedSerializer`, `util/PhpRound` half-away, `constants/GameConst`+`GameUnitDetail`+`CityConst`, `log/*`+`josa/Josa`), `:logic` (`stats/ActionPipeline` 12-source fold + `stats/GetStatValue` with-injury flags, `actions/CommandRegistry`+`GeneralActionDefinition`+`GeneralActionResolveContext`, `constraints/EvaluateConstraints`+`Presets`+`Comparators`, `statview/MemoryStateView`+`WorldEnvBuilder`, `war/*` battle engine P4-GREEN incl. `war/SearchDistanceListToDest` name-order BFS, `tick/MonthScopedRng`, domain `LogicEntities`/`NpcType`/`LastTurn`/`NationTurn`/`Diplomacy`/`GetNationColors`), `:infra` (`JdbcFlushExecutor` JDBC-only + row mappers), `:app:game-engine` (`turn/InMemoryTurnWorld`+`turn/ChangeRecorder`+`turn/ReservedTurnHandler`+`turn/TurnDaemonLifecycle`+`turn/WorldStateViewAdapter`+`run/TurnRunService`+`MonthlyPipeline`+`ReservedTurnRepository`), `tools/php-golden` (PROVEN Docker capture) are GREEN (~1543 tests, byte-parity gates closed); this plan **EXTENDS** them. **PHP `legacy/devsam-core/hwe/sammo/GeneralAI.php` is GRAND TRUTH; `legacy/devsam-core2026` TS (`app/game-engine/src/turn/ai/`) is a draw-for-draw STRUCTURAL twin used ONLY as a 2nd oracle — PHP WINS every divergence (`aiUtils.roundTo` half-up vs PhpRound half-away; the dropped `do선전포고` re-target abort draw; `do징병` deterministic-armType vs PHP `choiceUsingWeight`; the dropped `do천도` cooldown/sticky/BFS-restriction; `last_attackable` module-static-Map vs flush-delta; `buildSeedBase` vs `hiddenSeed`-direct; collapse `*0.9` vs `addExperience(-x*0.1)`).**

**Goal (design §11 P5, lines 262-266):** Lift the proven **per-general single-tick** framework of P0–P4 up to the **deterministic NPC AI** — the `GeneralAI` autorun that *replaces* a due general's reserved `(actionCode, args)` with an AI-chosen one BEFORE the existing `ReservedTurnHandler` executes it. The AI **mutates no GAME ENTITIES** — it CHOOSES the command; the existing handler resolve path (→ `ChangeRecorder` → `JdbcFlushExecutor`) executes it. BUT the AI DOES write general/nation **meta-KV deltas** (`last_attackable`, `resp_assist_try`, `last천도Trial`, `use_auto_nation_turn`, `movingTargetCityID`, `killturn`, `defence_train`) — these route through `ChangeRecorder` as AI-side deltas, NOT inline, NOT a module-static Map (M4). The read-only-over-entities isolation + the meta-KV-via-delta discipline is the whole point. Scope:
- **AI command-selection parity.** *Which* `do<한글>` fires, *which* `che_*` command + RAW args it emits, every RNG draw it makes (method + args + ORDER), every early-/null-return, every sort comparator, every short-circuit.
- **The dispatcher spines** — `chooseGeneralTurn`/`chooseNationTurn` (LIVE) — ordered fallback chains over `do<한글>()` where **first non-null wins**, the merged `priority` array IS the dispatch order, and the dispatch order IS the log order IS the draw order. **`chooseInstantNationTurn` has ZERO PHP call-sites (R-SEAM §3) → it is QUARANTINED as a no-gate-path, NOT wired into the live turn loop, NOT on the gate (B3).**
- **Derived state** — `updateInstance` → `calcGenType`/`calcDiplomacyState`/`calcWarRoute`/`categorizeNationCities`/`categorizeNationGeneral` — the per-AI-instance snapshot every `do<한글>` reads, with TWO distinct cache regimes (the `reqUpdateInstance` boolean vs the per-field null-guards — never coupled).
- **~50 `do<한글>` command families** across nation (발령/포상/몰수/불가침제의/선전포고/천도) and general (내정/금쌀/징병/전투준비/출병/워프류/헌납/거병/건국/방랑군이동/해산/선양/국가선택/사망대비/중립) sides + the 7 rate/promotion helpers.
- **Policy 4-layer override cascade** producing the merged `priority` list (**KV override is LIVE for BOTH policies** — B1) + the `can*` gates + the value-key set.
- **The constraint bridge** — the AI's accept/reject gate is the *literally identical* `evaluateConstraints(...FULL...)` call the daemon executor + precheck UI already make, fronted by a NEW `isArgValid`/argTest gate (M2/M9).
- **The nation-command resolve path** — does NOT exist in the Kotlin daemon today (R-SEAM §4); the nation pass (officer_level≥5, runs BEFORE the general pass — R-SEAM §2) requires porting `processNationCommand` into the daemon, reusing the GREEN general `processCommand` while-loop as the structural template.

**THE #1 PARITY RISK (design §13, the desync spine):** a whole AI **decision** runs on **ONE** `RandUtil(LiteHashDrbg(seed))` built once per general in the ctor from `serializeSeed(hiddenSeed,'GeneralAI',year,month,generalId)` (5 components, `hiddenSeed` DIRECT — `GeneralAI.php:153-159`) and threaded by reference through `calcGenType` and every `do<한글>` — **never re-seeded.** Byte-parity reduces to **did the Kotlin port pull draws off this single stream in the identical ORDER, COUNT, and METHOD.** A single conditional draw that appears/disappears (the `calcGenType` near-balance `nextBool` — the decision's FIRST draw; a `&&`/`||`-short-circuited `nextBool` whose left operand diverges; a `nextBool(prob)` crossing the `>=1`/`<=0`/`===0.5` no-draw/bit boundary; a variable-count BFS `choice` loop; the `do전쟁내정` double-`nextBool(0.3)`; `choosePromotion`'s structurally-present-but-never-drawing `nextBool(newChiefProb)`) shifts the entire downstream stream and silently desyncs the rest of the decision. **This `'GeneralAI'` AI-decision stream is DISTINCT from the per-command EXECUTION streams** (`'nationCommand'` then `'generalCommand'`, each re-seeded at execution — R-SEAM §2); the AI decision picks the command, then execution re-seeds its own RNG to resolve it. **This is the same discipline as P4's `processWar_NG`, but the stream is per-general-per-decision instead of per-battle, and the draw spine is the dispatcher priority loop instead of the phase machine.**

**P5 GATE (design §11 P5, line 266):** **fixed-seed full-game (scenario `1010`) replay — NPC selection + downstream log turn-for-turn parity.** The `tools/php-golden` Docker capture (generalized from the P4 `capture_battle`/`capture_conquercity` draw-recording seam) captures, per due general per turn, the full ordered AI **draw STREAM** `{seq, method, args, result, stateIdxBefore, bufferIdxBefore}` tagged by the firing `do<한글>` reason + the chosen `(actionCode, RAW args)` + the resulting `ChangeRecorder` delta + the per-turn log; the Kotlin AI replays it draw-for-draw on the same per-general seed. Three dimensions:
- (a) **AI-selection draw-for-draw** — the chosen `do<한글>` reason, the emitted `(actionCode, RAW args)`, and the ordered per-general draw stream (value-for-value AND `stateIdx`/`bufferIdx` cursor-for-cursor) match at the FIRST divergent draw.
- (b) **Downstream log/delta turn-for-turn** — the executed command's `ChangeRecorder` delta + the per-turn log byte-equal (the AI choice feeds the EXISTING P2–P4 resolve gates, already GREEN — so a correct choice ⇒ a correct downstream by construction). **EXCEPTION (m10): for the diplomacy-emitting families (do불가침제의/do선전포고/do천도) the downstream delta/log is EXPLICITLY EXCLUDED from the P5 gate (the `che_불가침제의`/`che_선전포고`/`che_천도` state-mutation/logs are P6); the P5 gate asserts only the AI SELECTION + the boolean gate + the draw stream for those families.**
- (c) **Long-sim timeline non-divergence** — N turns of full-game replay (scenario 1010) stay byte-identical across PHP and Kotlin; the first turn that desyncs is reported.

**DIPLOMACY RESULT is DEFERRED to P6 (design lines 262-266), but the BOOLEAN gate is a P5 control-flow input.** P5 ports *whether* to emit and *which* `do<한글>` fires; P6 ports the diplomacy/천도 state-mutation + logs internals. BUT `hasFullConditionMet()`'s boolean outcome is a P5 control-flow input (a `false` makes `do*` return null → the next priority action fires → a *different* downstream draw stream), and the KV side-effects written *below* the gate (`resp_assist_try`, `last천도Trial`, `reqUpdateInstance`) only fire when it returns true. So P5 calls the gate and branches on it; the FULL constraint packs of the commands the AI emits must be ported **now**, not deferred (GAP G5).

**Architecture — EXTEND the P0–P4 framework, never reshape it. The AI is SEQUENTIAL / draw-order-preserving; there is NO parallelism inside a single general's decision.** Each `do<한글>`'s draws feed the next read; the priority loop short-circuits on first non-null; `categorizeNationGeneral` mutates `nationCities['important']`/`['generals']` by reference (cities MUST exist first). The only fan-out P5 admits is across PLAN families (nation-deploy vs nation-reward vs nation-diplo-SELECTION vs gen-domestic vs gen-war-move vs gen-found vs rates-promo), NEVER across the per-general decision sequence. Module touch-points:
- **`:logic`** — NEW `ai/` package (PURE — no Spring/DB): `ai/GeneralAI.kt` (the two LIVE dispatcher spines + the quarantined instant stub + `ChosenCommand`); `ai/AutorunGeneralPolicy.kt` + `ai/AutorunNationPolicy.kt` (the 4-layer merge, LIVE KV override); `ai/AiInstanceState.kt` (`updateInstance` + `calcGenType` + `calcDiplomacyState` + `maxResourceActionAmount`); `ai/AiWorldView.kt` (the categorize/derive read-only facade over the world); `ai/AiUtils.kt` (order-then-pick helpers); `ai/AiSeed.kt` (the `"GeneralAI"` lineage); `ai/CandidateBridge.kt` (the `candidateAllowed` helper + the argTest gate); `ai/bfs/AiDistance.kt` (the 4 BFS/distance helpers, REUSING the GREEN name-order `CityConst.path` adjacency — R-BFS); then the `ai/families/*` `do<한글>` method bodies. EDITS: `constraints/Presets.kt` (5 diplomacy/pathfinding presets), `constraints/CheChulbyeong.kt` (FULL-pack repair, R-BRIDGE §2), `actions/CommandRegistry.kt` (the 9 missing emitted `che_*` defs + argTest), `statview/WorldEnvBuilder.kt` (the AI env superset).
- **`:app:game-engine`** — the turn-loop interpose seam: `ReservedTurnHandler`/`TurnDaemonLifecycle`/`reservedActionOf` widen from `(Int)->String` to carry `(actionCode, argJson)` (R-SEAM §1) and gain an AI hook that, for AI-controlled generals, replaces the reserved command with `ai.chooseGeneralTurn(...)` BEFORE `handle`; PLUS the officer_level≥5 **nation-turn pass** running BEFORE the general pass (R-SEAM §2), which requires a NEW `processNationCommand` resolve path ported into the daemon (R-SEAM §4). The engine adapter supplies the AI's read-only world view + the `"GeneralAI"`-seeded `RandUtil`.
- **`tools/php-golden`** — `capture_ai.php` (drive due-general autorun over installed scenario_1010, recording the per-general AI draw stream + chosen command + reason + delta) reusing the P4 `RandUtilDrawRecorder` seam; the symmetric Kotlin `AiReplayGateTest`; plus crafted-fixture sibling-byte-match goldens for the under-covered L-DIPLO/L-GENFOUND families (R-GATE §3).

**Tech stack (unchanged from P0–P4):** Kotlin 2.1 / Spring Boot 3.4 / JPA read-only / JDBC batch (daemon write — NO `EntityManager`) / SHA-512 `LiteHashDrbg` / Testcontainers (Docker Desktop macOS quirk pinned: `api.version=1.44`, `DOCKER_CONTEXT=default`, `RYUK_DISABLED`). **No new RNG primitive** — every AI draw (`nextBool`, `choice`, `choiceUsingWeight`, `choiceUsingWeightPair`, `nextRangeInt`, `nextFloat1`) already exists on `common/rng/RandUtil.kt`.

**Parity discipline (non-negotiable, carried from P0–P4):** RNG draw-for-draw byte parity off the ONE shared `"GeneralAI"` decision stream (value + `stateIdx`/`bufferIdx` cursor); `nextBool` short-circuit semantics (`prob>=1`→true NO-draw, `===0.5`→`nextBit` 1-bit, `<=0`→false NO-draw, else `nextFloat1()<prob`) reproduced exactly; `choice`/`choiceUsingWeight*` walk over **insertion-ordered** candidate lists (BFS/DB-row/sort order is itself a parity target); `Util::round`=`PhpRound` half-away (NEVER `Math.round` half-up — the TS `aiUtils.roundTo` is WRONG everywhere; PHP wins); `Util::toInt`/`intdiv`/`intval` truncate-toward-zero; `ceil` distinct (getDedLevel); `floor` distinct (getTechLevel/TechLimit); PHP 8 sorts are unconditionally STABLE — no secondary comparator on ties; jsonb/conflict-map/bucket key INSERTION order (`LinkedHashMap`, never re-keyed by id); the daemon write path NEVER uses a JPA `EntityManager`; the AI is READ-ONLY over GAME ENTITIES (chosen command's resolve runs the EXISTING delta path) but WRITES meta-KV via ChangeRecorder delta. **The draw ORDER and the side-effect ORDER are themselves parity targets, not just the numbers.**

**AI RNG lineage (PHP grand truth, `GeneralAI.php:153-159`):**
```
GeneralAI seed:  serializeSeed(hiddenSeed, "GeneralAI", year, month, generalId)     # 5 components, hiddenSeed DIRECT (NOT TS buildSeedBase)
                 RandUtil(LiteHashDrbg(seed))                                         # ONE per general-per-DECISION; built ONCE in ctor; threaded by reference; NEVER re-seeded
                 the "GeneralAI" token is a SEPARATE stream from execution's "nationCommand"/"generalCommand" streams (R-SEAM §2)
ORDER BY RAND():  do선양 / do국가선택-오랑캐 — MySQL-side RAND(), NOT on the DRBG stream (0 draws consumed → cursor unaffected). QUARANTINED (G4); not reachable in 1010 (R-GATE §1: 0 npc==5, 0 npc==9).
```
**`hiddenSeed` provenance (R-GATE §2, source-verified):** legacy is the config-file static `d_setting/UniqueConst.php::$hiddenSeed`, REGENERATED fresh-random on every install (`ResetHelper.php:96`) — NOT a stable constant; opensamguk threads it from `world_state.hidden_seed` (P3 F4, GREEN, intentional config-file→DB-column divergence). The capture commits the EXACT live hex (`^[0-9a-f]{32}$`) the PHP golden ran under as the shared golden FIXTURE INPUT. **NEVER hardcode a placeholder/example hex — byte-0 desync (m5).**

**Reference sources (read before transcribing):**
- P5 RESEARCH (the consolidated mining + the area decomposition + dependency graph): `docs/superpowers/research/2026-05-30-p5-research.md`.
- P5 DRAW CATALOG (the per-turn draw-stream oracle, every draw cited `file:line`): `docs/superpowers/research/2026-05-30-p5-ai-decision-draw-catalog.md` — the F-DISPATCH/F-INSTANCE freeze spec AND the GATE's expected-stream oracle.
- **The 6 source-read research pins (`.context/p5-research/`): `R-SEAM.md` (the daemon seam contract — handle() sig, pass order, instant quarantine, nation-resolve gap), `R-NATIONPOL.md` (the nation policy end-to-end + the B1 confirmation), `R-FACADE.md` (PK-ascending DB order + helper bodies + joinYearMonth formula), `R-BFS.md` (REUSE the GREEN name-order adjacency), `R-BRIDGE.md` (the 35 emitted codes → Kotlin pack map, 9 missing defs + 1 divergent pack + 5 presets + the typo verdict), `R-GATE.md` (the npc 5/9 census + hiddenSeed provenance + coverage map).** These pin the facts the blockers demanded.
- GAP files (`.context/p5-research/`): `G1-general-policy.md`, `G2-nation-policy.md`, `G3-bfs-helpers.md`, `G4-rand-quarantine.md`, `G5-constraint-bridge-reqs.md`, `G8-genstat-flavor.md`, plus `GAPS.md` and the 13 facets `A1`–`A13`.
- PHP grand truth: `legacy/devsam-core/hwe/sammo/GeneralAI.php` (the 4293-line body), `sammo/AutorunGeneralPolicy.php` (215 lines, READ IN FULL — B4), `sammo/AutorunNationPolicy.php` (291 lines, READ IN FULL — B4), `func.php` BFS/distance helpers, `func_converter.php`/`func_time_event.php` rate helpers, `sammo/CityConstBase.php`, the `Command/{General,Nation}/che_*.php` FULL packs, `TurnExecutionHelper.php:260-364` (the turn loop, the pass order + nation resolve), `General.php` (getStatValue flavors + calcRecentWarTurn), `Util.php` (joinYearMonth/simpleSerialize).
- 2nd oracle (NOT truth): `legacy/devsam-core2026/app/game-engine/src/turn/ai/`.
- P4 plan (FORMAT + substrate + the draw-recording seam): `docs/superpowers/plans/2026-05-30-p4-battle-engine.md`.
- Design roadmap §11 P5 / §13: `docs/superpowers/specs/2026-05-29-devsam-opensamguk-kotlin-migration-design.md:262-266`.

---

## Resolved decisions (cited; freeze BEFORE the fan-out)

1. **The `"GeneralAI"` seed is 5-component, `hiddenSeed`-DIRECT (catalog §1).** `serializeSeed(hiddenSeed, "GeneralAI", year, month, generalId)` — NOT the TS `buildSeedBase(world)`. ONE `RandUtil` per general per DECISION, built ONCE, threaded by reference, NEVER re-seeded. The `"GeneralAI"` decision stream is DISTINCT from execution's `"nationCommand"`/`"generalCommand"` streams (R-SEAM §2). Pinned in F-SEED.
2. **The two cache regimes are NEVER coupled (research §"Two distinct cache regimes").** `reqUpdateInstance` boolean gates the `updateInstance` block (city/nation/policy/genType/dipState/maxResource) — re-runs when a command set it dirty (dirty triggers at `GeneralAI.php:1308/1509/1972/1990/2113`). The per-field null-guards gate `categorizeNationCities`/`categorizeNationGeneral`/`calcWarRoute` — computed once, NEVER re-invalidated even if `reqUpdateInstance` later flips. Do NOT share one flag. Pinned in F-INSTANCE.
3. **`chooseInstantNationTurn` is a QUARANTINED no-gate-path — NOT wired, NOT on the gate (B3, R-SEAM §3).** A repo-wide grep finds ZERO live callers of `chooseInstantNationTurn` in `legacy/devsam-core` (its own TODO at `:3620` confirms it is an unwired stub); the live turn loop (`TurnExecutionHelper.php:306`) calls ONLY `chooseNationTurn` for officer_level≥5. No PHP golden can exercise the instant path. **P5 wires ONLY `chooseGeneralTurn` + `chooseNationTurn`.** A structural-only port of `chooseInstantNationTurn` MAY be written (kept deliberately divergent from `chooseNationTurn` — gate-first, no reason strings, 2-guard loop, `reservedCommand` by-ref) but it is marked `@ParityQuarantine("R-SEAM-no-call-site")`, EXCLUDED from G-GATE, and logged to the phase backlog with the sibling `chooseNationTurn` byte-match proof. The two LIVE loops (`chooseGeneralTurn`/`chooseNationTurn`) are DELIBERATELY DIVERGENT and must NOT be collapsed.
4. **Reserved-honor asymmetry (the biggest control-flow trap — A13 §5 #1).** The GENERAL path honors a non-휴식 reserved command **WITHOUT** re-validating (`chooseGeneralTurn` step 5, no gate) and **WITHOUT logging**; the NATION path DOES gate (`hasFullConditionMet()` then a `getFailString()` fail-log on deny, then FALLS THROUGH to the loop). Port both verbatim. The existing `ReservedTurnHandler` execution gate is the backstop for an honored-but-illegal general reservation (it 휴식-rests at execution) — that double-layer IS the PHP behavior. Do NOT "normalize" the general path to add a check.
5. **KV `priority` overrides are LIVE for BOTH the GENERAL and the NATION policy (B1 — INVERTED from the prior draft; source-verified R-NATIONPOL + `AutorunGeneralPolicy.php:7-39,118-147`).** Each policy class declares one **`static`** property per bare action name (`static $귀환='귀환'`, `static $출병='출병'`, …; nation: `static $부대전방발령`, `static $불가침제의`, …). `property_exists($this, $priorityItem)` checks **both instance AND static** properties, so a valid bare-action-name KV `priority` item returns TRUE → it is KEPT → the filtered list (if non-empty) **REPLACES** `$this->priority`. The guard's real purpose is to **drop genuinely-unknown names** (a typo like `'귀한'`) with an `E_USER_NOTICE`, NOT to drop everything. **NET: a valid-name KV `priority` override REPLACES the default; a typo/unknown name is DROPPED (falls back to keeping the others, and if the whole filtered list is empty the default is kept); the override mechanism is identical for both policies — there is NO asymmetry, NO "dead config".** Port identically for both. (The nation policy applies the whole-array replace at construction `:197-220` then the consumer loop additionally validates `can<name>`; the general policy filters at construction `:118-147`. Both reach "valid replaces, typo drops".) Delete every "dead-KV" framing.
6. **`ORDER BY RAND()` is QUARANTINED with a deterministic `min(no)` substitute + proof (G4, R-GATE §1).** `do선양` (`GeneralAI.php:3324`) and `do국가선택`-오랑캐 (`:3345`) are MySQL-side RAND(), NOT on the DRBG stream → 0 draws consumed (cursor unaffected). Substitute: deterministic `min(no)`/`min(nation)` over the same WHERE-filtered candidate set (insertion = general.no order, G13). Neither path is reachable in the gate scenario (R-GATE §1: scenario_1010 has **0 npc==5 and 0 npc==9** generals out of **678** total; `can선양` requires npc==5; 오랑캐임관 requires npc==9; the 2 officer_level==12 rulers are both npc==2). Quarantine with the crafted-fixture sibling-byte-match proof (G4 §4) — DO NOT fabricate an id, weaken a test, or seed npc 5/9 into 1010. GT1 EMITS the actual census `SELECT npc, COUNT(*) FROM general GROUP BY npc` against the installed fixture as the quarantine proof artifact (aborts if non-zero).
7. **The getStatValue flavor table is RESOLVED (G8).** `calcGenType` = `getX(false)` = `getStatValue(stat, withInjury=false, withIActionObj=true, withStatAdjust=true, useFloor=true)` (strength/intel then `clamp(v, 1.0)`=`valueFit(v,1)`, leadership NO floor). Reward math = `(false,true,true,true)`. Promotion stat-gate = `(false,false,false,false)`. Build the per-call-site → 4-arg-signature table in F-SEED and verify against the GREEN `stats/GetStatValue.kt`. **Do NOT re-round the result — `useFloor=true` already truncated.**
8. **BFS visit order is `CityConst.path` name-list order — REUSE the GREEN adjacency, do NOT rebuild (G3, R-BFS — DEFINITIVE).** Both PHP `CityConstBase::_generate()` and the Kotlin `CityConst.generateCities()` build `path` as a `LinkedHashMap<Int,String>` in `$initCity` connection-NAME source order; **no numeric sort exists anywhere**. The P4 `war/SearchDistanceListToDest.kt` already iterates `city.path.keys` in this name-order (byte-identical to PHP `array_keys(...->path)`). **The AI MUST REUSE `CityConst.byId(x).path` + the P4 `SearchDistanceListToDest` primitive — it must NOT build a separate name-order adjacency LinkedHashMap.** The `[cityID=>dist]` insertion = dequeue order = BFS visitation order seeds every `choice`/`choiceUsingWeightPair` candidate list. `searchDistance` finalizes at DEQUEUE (allows duplicate enqueues, first-dequeue wins); `searchAllDistanceByCityList` is Floyd-Warshall (row key order = self → path-order neighbors → transitive-append discovery order). PHP BFS wins (CLAUDE.md findNextCapital law). **F-BFS must complete BEFORE F-FACADE (sequential — F-FACADE's `categorizeNationCities`/`calcWarRoute` are strict consumers of the name-order output; R-BFS §4).**
9. **`do전쟁내정` has TWO `nextBool(0.3)` — the single biggest collapse-into-one trap (catalog §5.K).** `:2271` conditional (only if `nation.rice < baserice`) THEN `:2279` ALWAYS-drawn (no guard). Port both. Mirror traps: `choosePromotion`'s `:4102` `nextBool(newChiefProb)` is structurally present but NEVER draws (prob always 1 or 0 → short-circuits) — a naive port that always calls it adds a phantom draw; `do금쌀구매` has ZERO draws (fully deterministic ladder — any accidental RNG breaks parity); `do요양`/`do집합` are GATE-EXEMPT (no `hasFullConditionMet`) but NOT draw-free (`do집합` npc==5 draws `nextRangeInt(2,4)` + writes `killturn` delta — m4).
10. **The constraint bridge is the IDENTICAL `evaluateConstraints(...FULL...)` call execution makes, fronted by an argTest gate (A13/G5/M2/M9, R-BRIDGE §4/§5).** `candidateAllowed(actionCode, RAWargs, ctx, view) = (argTest gate passes) && evaluateConstraints(def.buildConstraints(ctx.copy(args=canonicalized, mode=FULL)), ctx, view) is Allow`, FULL mode (PRECHECK's `Unknown` phantom-denies), the `isArgValid`/argTest arg gate FIRST (BaseCommand.php:377 order: isArgValid → testAll → testPostReqTurn) — **the AI emits RAW args; the bridge canonicalizes ONCE inside argTest and rejects on a missing required key with `'인자가 올바르지 않습니다.'`; do NOT double-canonicalize (M2)**, `testPostReqTurn` term-stack tail AFTER, FRESH ctx per candidate. Gate-EXEMPT: `do집합`/`do요양` + the `사망`/`재야유저`/`do예약턴` reserved-returns (G14). The 9 missing `che_*` defs + the 1 divergent pack (che_출병) + 5 diplomacy/pathfinding presets (G5, R-BRIDGE §2/§3) are P5 work, NOT a P6 deferral.
11. **Diplomacy RESULT internals deferred to P6 (design lines 262-266); only the SELECTION + the boolean gate + the draw stream are P5.** `do불가침제의`/`do선전포고`/`do천도` port *which* `do*` fires + the draw stream + the emit; the `che_*` command-class state-mutation/logs are P6. The `che_불가침제의`/`che_선전포고`/`che_몰수` FULL constraint packs (needed for the boolean) ARE P5 (decision #10). **The G-GATE downstream-delta/log assertion (dimension b) EXCLUDES these three families (m10).**
12. **`calcDiplomacyState` WRITES meta-KV via ChangeRecorder delta — it is NOT a pure function (M4).** `last_attackable=yearMonth` (`:266/271/275`), and the sibling KV writes across the AI (`resp_assist_try`, `last천도Trial`, `use_auto_nation_turn`, `movingTargetCityID`, `killturn`, `defence_train`) route through `ChangeRecorder` as AI-side deltas, NOT inline, NOT a module-static Map (TS `last_attackable` static-Map is WRONG — PHP wins). The read at `:276` observes the **pre-turn snapshot** value; the writes queue as deltas visible NEXT turn (after the chosen command's resolve flushes). The AI is read-only over GAME ENTITIES; the meta-KV deltas are the ONLY AI-side writes. Pinned in F-INSTANCE.

---

## File Structure

All paths relative to repo root. `[NEW]` = create, `[EDIT]` = modify existing P0–P4 substrate. **Do NOT re-create** the RNG kernel / PhpRound / ActionPipeline fold / getStatValue / CommandRegistry / EvaluateConstraints / MemoryStateView / ChangeRecorder / TurnDaemonLifecycle / CityConst.path adjacency / SearchDistanceListToDest — EXTEND/REUSE them. The AI is read-only over GAME ENTITIES; it returns `(actionCode, RAW args)` and queues meta-KV deltas.

```
logic/src/main/kotlin/opensamguk/logic/ai/
  GeneralAI.kt                       # [NEW] F-DISPATCH: ChosenCommand(action,args,reason); chooseGeneralTurn/chooseNationTurn control flow VERBATIM (the 2 LIVE master orders + pre-loop branches + reserved-honor asymmetry) + chooseInstantNationTurn QUARANTINED structural stub (decision #3, not wired). PURE.
  AutorunGeneralPolicy.kt            # [NEW] F-POLICY: $default_priority (14, exact order) + can* defaults (22) + doNPCState + the 4-layer cascade (default→server→nation→aiOptions) + the LIVE property_exists KV-priority override (decision #5: valid-name REPLACES, typo DROPS). NO availableInstantTurn (it lives in nation policy).
  AutorunNationPolicy.kt             # [NEW] F-POLICY: $defaultPriority (20, exact order, NO 유저장몰수) + $availableInstantTurn (12-set) + the 4-layer cascade + the chief-gate + the PhpRound(-2) derived defaults (reqNPC*/reqHuman*) + the FULL value-key set + numeric defaults + the LIVE KV-priority override (decision #5, whole-array replace + consumer-loop can<name> validation).
  AiInstanceState.kt                 # [NEW] F-INSTANCE: updateInstance (reqUpdateInstance-gated) ; calcGenType (0-or-1 conditional nextBool, the band gate, the FIRST draw); calcDiplomacyState (NO draws, joinYearMonth early-gate, last_attackable ChangeRecorder delta); the two-cache-regime split (decision #2); the meta-KV delta writes (decision #12).
  AiWorldView.kt                     # [NEW] F-FACADE: read-only categorize/derive facade over InMemoryTurnWorld — categorizeNationCities (PK-ascending sortedBy{cityId}, 4 buckets) + categorizeNationGeneral (9 buckets via first-match-wins ladder, sortedBy{no}, calcRecentWarTurn sentinel; mutates nationCities['important']/['generals'] by reference — cities FIRST) + calcWarRoute (BFS). Per-field null-guards (decision #2).
  AiSeed.kt                          # [NEW] F-SEED: RandUtil(LiteHashDrbg(serializeSeed(hidden,"GeneralAI",year,month,generalId))); the getStatValue flavor table (decision #7, G8).
  AiUtils.kt                         # [NEW] F-SEED: order-then-pick helpers (stable usort/uasort/arsort wrappers NO secondary comparator; insertion-ordered LinkedHashMap; the count-idx rank-weight pattern; the choiceUsingWeightPair candidate-build helper).
  CandidateBridge.kt                 # [NEW] F-BRIDGE: candidateAllowed(actionCode,RAWargs,ctx,view):Boolean = argTest gate THEN evaluateConstraints(FULL) is Allow; AI emits RAW, single internal canonicalization (M2); testPostReqTurn tail; fresh ctx per candidate; gate-EXEMPT path list (decision #10).
  bfs/AiDistance.kt                  # [NEW] F-BFS: REUSE CityConst.byId(x).path (name-order, R-BFS — do NOT rebuild) + REUSE P4 SearchDistanceListToDest; ADD searchDistance (FIFO, finalize-at-dequeue, distForm) wrapper + searchAllDistanceByCityList (Floyd-Warshall row-order) + searchAllDistanceByNationList + isNeighbor over the GREEN adjacency. NO draws. PHP BFS wins.
  families/
    NationDeployFamily.kt            # [NEW] LEAF nation-deploy: the 11 do발령 methods (all emit che_발령; do부대전방발령 emits the destGenaralID typo → always-null, R-BRIDGE §4)
    NationRewardFamily.kt            # [NEW] LEAF nation-reward: do유저장(긴급)포상/doNPC(긴급)포상/doNPC몰수 (che_포상/che_몰수)
    NationDiploFamily.kt             # [NEW] LEAF nation-diplo-SELECTION: do불가침제의/do선전포고/do천도 (SELECTION + boolean + draw stream only; RESULT→P6, gate-delta EXCLUDED m10)
    GenDomesticFamily.kt             # [NEW] LEAF gen-domestic: do일반내정/do전쟁내정/do긴급내정/do금쌀구매/do징병
    GenWarMoveFamily.kt              # [NEW] LEAF gen-war-move: do전투준비/do소집해제/do출병/doNPC헌납(per-resource draw)/워프트리오/do귀환/do집합/do방랑군이동
    GenFoundFamily.kt                # [NEW] LEAF gen-found: do거병/do해산/do건국/do선양/do국가선택/doNPC사망대비/do중립
    RatesPromoFamily.kt              # [NEW] LEAF rates-promo: chooseTexRate/chooseGoldBillRate/chooseRiceBillRate/choosePromotion/chooseNonLordPromotion + calcCityDevelRate/calcNationDevelopedRate

  constraints/Presets.kt             # [EDIT] F-BRIDGE: ADD existsDestNation / nearNation(isNeighbor) / hasRoute / hasRouteWithEnemy / disallowDiplomacyBetweenStatus / allowDiplomacyBetweenStatus(allowDiplomacyWithTerm) — the 5 missing presets (R-BRIDGE §3: BOTH hasRoute AND hasRouteWithEnemy are absent → 5, NOT 4), needed by the emitted diplomacy/출병 FULL packs.
  constraints/CheChulbyeong.kt       # [EDIT] F-BRIDGE: REPAIR the DIVERGENT pack (R-BRIDGE §2) — add the 5 absent FULL-pack constraints NotOpeningPart/NotSameDestCity/ReqGeneralRice/AllowWar/HasRouteWithEnemy so the AI gate boolean matches PHP che_출병.php:78-87.
  actions/CommandRegistry.kt         # [EDIT] F-BRIDGE: register the 9 missing emitted che_* defs (che_몰수 EXISTS-already? NO — 몰수 is EXISTS-GREEN; the 9 MISSING are 불가침제의/선전포고/NPC능동/귀환/인재탐색/견문/해산/요양/선양 per R-BRIDGE §2) + the argTest gate. F-BRIDGE owns this widening; no family re-touches it.
  actions/nation/CheBulgachimJeui.kt # [NEW] F-BRIDGE: che_불가침제의 def (argTest + 6mo-min AlwaysFail + BeChief/NotBeNeutral + diplomacy-term pack)
  actions/nation/CheSeonjeonpogo.kt  # [NEW] F-BRIDGE: che_선전포고 def (argTest + BeChief/NotBeNeutral/OccupiedCity/SuppliedCity/ReqEnvValue/ExistsDestNation/NearNation/DisallowDiplomacyBetweenStatus)
  actions/military/CheNpcNeungdong.kt# [NEW] F-BRIDGE: che_NPC능동 warp def (argTest optionText='순간이동'+destCityID; EMPTY fullCond; MustBeNPC permission → gate ≈ argValid)
  actions/military/CheGwihwan.kt     # [NEW] F-BRIDGE: che_귀환 def
  actions/personnel/CheInjaeTamsaek.kt # [NEW] F-BRIDGE: che_인재탐색 def
  actions/founding/CheHaesan.kt      # [NEW] F-BRIDGE: che_해산 def
  actions/founding/CheSeonyang.kt    # [NEW] F-BRIDGE: che_선양 def (BeLord gate; ORDER BY RAND quarantine note, decision #6)
  actions/develop/CheGyeonmun.kt     # [NEW] F-BRIDGE: che_견문 def
  actions/personnel/CheYoyang.kt     # [NEW] F-BRIDGE: che_요양 def (dispatcher-direct, gate-exempt; def needed for execution)
  statview/WorldEnvBuilder.kt        # [EDIT] F-SEED: emit the AI env superset (develcost/year/month/startyear/turnterm/init_year/init_month/autorun_user/npc_nation_policy/npc_general_policy) — today only year/startYear/develCost[/month].

app/game-engine/src/main/kotlin/opensamguk/engine/turn/
  ReservedTurnHandler.kt             # [EDIT] F-SEAM: WIDEN handle() to thread argJson (R-SEAM §1); interpose the AI between ring-read and resolve — for AI-controlled generals, replace (actionCode,args) with ai.chooseGeneralTurn(reserved) BEFORE resolve. AI reads the WorldStateViewAdapter (FULL). NO new entity write path.
  ProcessNationCommand.kt            # [NEW] F-SEAM: the NATION-command resolve path (R-SEAM §4 — does NOT exist today). Port processNationCommand (TurnExecutionHelper.php:72-109) reusing the GREEN general processCommand while-loop as template; 'nationCommand' 6-component seed; ChangeRecorder single-dirty-source; turn_last_{officer_level} KV write + setRawCity(null).
  TurnDaemonLifecycle.kt             # [EDIT] F-SEAM: WIDEN reservedActionOf from (Int)->String to carry (actionCode,argJson); ADD the officer_level>=5 NATION-turn pass running BEFORE the general pass (R-SEAM §2, single processBlocked gate). chooseNationTurn ONLY (chooseInstantNationTurn NOT wired — decision #3).
  AiTurnAdapter.kt                   # [NEW] F-SEAM: builds the per-general "GeneralAI" RandUtil + AiInstanceState + AiWorldView; supplies the read-only ctx to GeneralAI. The single seam owner.

logic/src/test/kotlin/opensamguk/logic/ai/...      # [NEW] per-task unit tests (one per task)
logic/src/test/kotlin/opensamguk/logic/golden/      # [NEW] AiReplayGateTest.kt (clone the P4 BattleReplayGateTest matched-count-gate-with-quarantine pattern)
logic/src/test/resources/golden/p5/                 # [NEW] committed PHP goldens: ai-turn-NN.json (live 1010 capture) + ai-crafted-{diplo,genfound}-NN.json (crafted-fixture, R-GATE §3) + ai-longsim.json

tools/php-golden/
  capture_ai.php                     # [NEW] G-GATE: drive due-general autorun over installed scenario_1010; record per-general AI draw stream + chosen (actionCode,RAW args) + reason + delta + log; npc-census proof; reuse the P4 RandUtilDrawRecorder.
  capture_ai_crafted.php             # [NEW] G-GATE: crafted-fixture captures for the under-covered L-DIPLO/L-GENFOUND families (R-GATE §3) — seed recv_assist/방랑군/재야/at-war state to reach do선전포고/do거병/do건국/do국가선택/do출병.
  manifest_ai.json                   # [NEW] G-GATE: scenario, hiddenSeed (^[0-9a-f]{32}$ from live capture), per-table ignore-list, draw-stream + reason + chosen-command + log match targets, quarantine[] (선양/오랑캐 ORDER BY RAND + chooseInstantNationTurn).
  compare-ai/                        # [NEW] G-GATE: reuse the compare-command-logs matched-count + ignore-list pattern; draw-stream value+cursor diff + reason/command/log-sequence diff; diplomacy-delta EXCLUDED (m10).
```

> **Build prerequisites (one-time, performed by the relevant foundation task):**
> - No new module deps: `logic → common`, engine → logic. Acyclic. The `ai/` package is PURE `:logic` (no Spring/DB).
> - **The 4 new `actions/` sub-package dirs (`military/`, `personnel/`, `founding/`, `develop/`) are scaffolded as an explicit FR2 step (m11)** — or, if the orchestrator prefers the flatter P2–P4 layout, collapse them into `actions/`; FR2 picks one and stays consistent.
> - The Testcontainers Docker Desktop quirk + `kotlin("reflect")` are already wired.

---

# Dependency graph & parallelization (Tier 0 → Leaf families → Gate)

The research's area decomposition (research §"Suggested P5 plan areas") is the authority. **The AI is SEQUENTIAL — the parallelism is across PLAN families, never across the per-general decision sequence.** Every shared extension point lands in a Tier-0 FOUNDATION wave that the families only CONSUME (the P2/P3/P4 anti-merge-conflict lesson: never let two parallel families co-widen the same shared file; cross-area shared artifacts build creator-then-consumer, never in parallel).

> **Two clarifying notes (m12):** (1) "dispatch order = draw order" holds only AFTER the F-POLICY merge produces the resolved `priority` list — the merge is the first thing that must be frozen. (2) Each `ai/families/*.kt` file holds MULTIPLE `do<한글>` methods; each registers into the F-DISPATCH dispatch by its **method-name** (the action name), not by file-name — a family adds its leaf methods + the by-name registrations, it never edits the dispatch loop body.

```
TIER 0 (FOUNDATIONS — shared extension points; SEQUENTIAL creator-then-consumer; DO NOT parallelize as families):
  F-POLICY    AutorunGeneralPolicy + AutorunNationPolicy (4-layer merge, LIVE KV override → merged priority = dispatch = log = draw order)  ── BLOCKS everything; resolves G1/G2/B1/B4
  F-BFS       AiDistance (REUSE GREEN CityConst.path name-order + P4 SearchDistanceListToDest; ADD Floyd/isNeighbor wrappers)              ── resolves G3/B6; consumed by FACADE/deploy/found/diplo/warroute
  F-SEED      AiSeed ("GeneralAI" lineage + getStatValue flavor table) + AiUtils (order-then-pick) + WorldEnvBuilder superset             ── resolves G8; consumed by instance + every family
  F-HELPERS   the rate/reward helper-body source-read (calcRecentWarTurn/cutTurn/getOutcome/getDedLevel/getGoldIncome/getRiceIncome/TechLimit/riceWithTech/costWithTech/joinYearMonth) with file:line + rounding/sentinel pins (B7/M5/M6, R-FACADE §2/§3/§4)  ── BLOCKS F-FACADE + F-INSTANCE + L-REWARD/L-RATES
  F-INSTANCE  AiInstanceState (updateInstance + calcGenType FIRST-draw + calcDiplomacyState joinYearMonth-gate + maxResourceActionAmount + meta-KV deltas)  ← F-POLICY,F-SEED,F-HELPERS  ── the FIRST draw site
  F-FACADE    AiWorldView (PK-ascending buckets + calcRecentWarTurn ladder + calcWarRoute; cities-before-generals by-ref)                 ← F-BFS,F-INSTANCE,F-HELPERS  (SEQUENTIAL after F-BFS — R-BFS §4)
  F-BRIDGE    CandidateBridge + argTest gate + the 5 diplomacy presets + the 9 missing che_* defs + che_출병 pack repair                   ← (CommandRegistry/EvaluateConstraints GREEN); resolves G5/M1/M2/M8/M9 (R-BRIDGE)
  F-DISPATCH  GeneralAI skeleton (2 LIVE master orders + pre-loop branches + reserved-honor asymmetry + quarantined instant stub + ChosenCommand)  ← F-POLICY,F-INSTANCE,F-FACADE,F-BRIDGE  ── the dispatcher spine
  F-SEAM      ReservedTurnHandler/TurnDaemonLifecycle widen+AI hook + ProcessNationCommand port + officer>=5 nation pass + AiTurnAdapter  ← F-DISPATCH (cross-module — sequential); resolves B2/B3
  F-QUAR      ORDER BY RAND quarantine decision (min(no) substitute + proof + backlog entry) + chooseInstantNationTurn quarantine          ← (decisions #3/#6, lands with F-BRIDGE/GenFound)

LEAF FAMILIES (PARALLEL fan-out after Tier 0; each consumes the foundations, owns its own file, never co-widens a shared file):
  L-DEPLOY    NationDeployFamily   (11 × che_발령)                     ← F-FACADE,F-BFS,F-SEED,F-BRIDGE
  L-REWARD    NationRewardFamily   (포상/몰수, usort + count-idx weight) ← F-FACADE,F-SEED,F-BRIDGE,F-HELPERS
  L-DIPLO     NationDiploFamily    (불가침/선포/천도 SELECTION→P6)       ← F-FACADE,F-BFS,F-SEED,F-BRIDGE,F-HELPERS
  L-GENDOM    GenDomesticFamily    (내정/금쌀/징병)                     ← F-INSTANCE,F-SEED,F-BRIDGE
  L-GENWAR    GenWarMoveFamily     (전투/워프/이동/헌납/집합/방랑)       ← F-FACADE,F-BFS,F-SEED,F-BRIDGE
  L-GENFOUND  GenFoundFamily       (거병/건국/선양/국가선택/사망대비/중립) ← F-BFS,F-SEED,F-BRIDGE,F-QUAR
  L-RATES     RatesPromoFamily     (tax/bill/promotion)                 ← F-FACADE,F-SEED,F-INSTANCE,F-HELPERS

TIER 2 (GATE — serial close; depends on ALL):
  G-GATE      capture_ai (+ crafted fixtures) + AiReplayGateTest (draw-for-draw selection + downstream log + long-sim timeline)  ← ALL
```

**Parallel worktree fan-out (the executor runs `{…}∥` waves):**
- **Wave 0 (Tier-0 FOUNDATIONS), sequenced by cross-edges, not co-widened:** `{F-POLICY, F-BFS, F-SEED, F-HELPERS}∥` are file-disjoint and run FIRST (F-POLICY = `ai/AutorunGeneralPolicy.kt`+`ai/AutorunNationPolicy.kt`; F-BFS = `ai/bfs/AiDistance.kt`; F-SEED = `ai/AiSeed.kt`+`ai/AiUtils.kt`+`statview/WorldEnvBuilder.kt`; F-HELPERS = a source-read research note `.context/p5-research/H-HELPERS.md`, no code). Then **F-INSTANCE** (`ai/AiInstanceState.kt`, ← F-POLICY,F-SEED,F-HELPERS). Then **F-FACADE** (`ai/AiWorldView.kt`, ← F-BFS,F-INSTANCE,F-HELPERS — SEQUENTIAL after F-BFS per R-BFS §4). Then **F-BRIDGE** in parallel with the instance/facade chain (`ai/CandidateBridge.kt` + `constraints/Presets.kt` + `constraints/CheChulbyeong.kt` repair + the 9 def files + `actions/CommandRegistry.kt` registration + argTest — F-BRIDGE owns the `CommandRegistry.resolve` widening; no family re-touches it). Then **F-DISPATCH** (`ai/GeneralAI.kt`, ← all the above). Then **F-SEAM** (the cross-module `engine/turn/` edits, sequential — never co-edited by a leaf). **F-QUAR** lands with F-BRIDGE/GenFound. Net Tier-0 sequence: **`{F-POLICY, F-BFS, F-SEED, F-HELPERS}∥` → F-INSTANCE → F-FACADE → F-BRIDGE → F-DISPATCH → F-SEAM**.
- **Wave 1 (LEAF FAMILIES, the fan-out):** `{L-DEPLOY, L-REWARD, L-DIPLO, L-GENDOM, L-GENWAR, L-GENFOUND, L-RATES}∥` are file-disjoint (each owns one `ai/families/*.kt`). The lone shared touch-point is each family REGISTERING its `do<한글>` bodies into the `GeneralAI` dispatch by action-name (m12); the dispatch BODY is F-DISPATCH-owned (a map `actionName → do-method`), and families add their leaf method + the by-name registration — they do NOT co-widen the dispatch loop. **CONSTRAINT (G5): a family that emits a `che_*` whose def is not yet in `CommandRegistry` is BLOCKED on F-BRIDGE landing that def — F-BRIDGE lands all 9 + the che_출병 repair before Wave 1 starts.**
- **Wave 2 (GATE, serial):** `G-GATE` — the capture harness (live 1010 + crafted fixtures) + the replay test; runs after all families are green.

**The shared-extension protocol (restated for P5):** every shared file is OWNED by a Tier-0 foundation task that the families only CONSUME:
> - **`GeneralAI` dispatch loop** — CREATED + owned by F-DISPATCH. The two LIVE master orders + the reserved-honor asymmetry are FROZEN there. Families register `do<한글>` bodies by name; none edit the loop.
> - **`AutorunGeneralPolicy`/`AutorunNationPolicy`** — CREATED + owned by F-POLICY. The merged `priority` ORDER is the dispatch/log/draw order — frozen. Families READ the can-flags + value-keys; never widen.
> - **`AiInstanceState` + `AiWorldView` + `AiDistance`** — CREATED + owned by F-INSTANCE/F-FACADE/F-BFS. The categorize buckets + the BFS visit order + the FIRST-draw `calcGenType` are the cross-family shared inputs (belong in the foundation, NEVER a leaf).
> - **`CandidateBridge` + the diplomacy presets + the che_출병 repair + the missing che_* defs + argTest** — CREATED + owned by F-BRIDGE. Families CALL `candidateAllowed`; never re-implement a constraint pack.
> - **`AiSeed`/`AiUtils`/the env superset** — owned by F-SEED. Families CALL the seed + the order-then-pick helpers; the `"GeneralAI"` lineage + the getStatValue flavor table are frozen.
> - **The helper-body pins** — owned by F-HELPERS (a source-read note); families CITE the file:line + the rounding/sentinel for every rate/reward/threshold constant — none re-derive.

---

# THE per-decision RNG draw order (the load-bearing parity contract — pinned from the catalog)

> **This is the ONE contract the entire P5 gate reduces to. Frozen by F-DISPATCH + F-INSTANCE; consumed by the leaf families; asserted draw-for-draw by G-GATE. Every draw comes off the ONE shared `RandUtil(LiteHashDrbg(serializeSeed(hidden,"GeneralAI",y,m,genId)))` built once in the ctor and threaded by reference. The draw spine is the dispatcher priority loop — the order `do*` fire IS the draw order. Compute every probability identically to PHP to decide whether `nextBool` draws at all (and a BIT vs a FLOAT).**

**SETUP (ctor, NO draws):** build the SOLE `rng`; cache `leadership/strength/intel` (with-injury) + `fullLeadership/fullStrength/fullIntel` (`getX(false)`). **HARD WARNING (m8):** the very next thing — the `calcGenType` draw — is CONDITIONAL on the near-balance band (weaker stat ≥ stronger×0.8); its presence/absence shifts EVERY later draw; the getStatValue flavor mapping (G8) MUST be GREEN (F-SEED) before this is implemented, and the exact `Double` float arg is a byte-parity target.

**`updateInstance` → `calcGenType` (the FIRST draw site, `GeneralAI.php:175-204`):** 0 OR 1 draw. `if strength>=intel` → 무장, and **IF `intel >= strength*0.8`** → DRAW `nextBool(intel/strength/2)` (`:185`); else 지장, and IF `strength >= intel*0.8` → DRAW `nextBool(strength/intel/2)` (`:193`). Exactly one of {1a,1b} can fire, only inside the near-balance band; outside the band ZERO draws. `calcDiplomacyState`/`calcWarRoute`/`categorizeNationCities`/`categorizeNationGeneral` make ZERO draws (they only set candidate ORDER + queue meta-KV deltas). **The danger is purely the presence/absence of this one draw + its exact `Double` float arg (decision #7, G8).**

**The dispatcher draw spine (the order `do*` fire):**
- **`chooseGeneralTurn` prologue:** `updateInstance` (calcGenType) → `:3719` `nextBool(npcMessageFreqByDay*term/(60*24))` ONLY if `npcmsg` truthy (`&&` short-circuit, side-effect no return) → 선양/집합/거병/국가선택/방랑 pre-loop branches → **priority loop** `foreach generalPolicy->priority → do{X}()` first-non-null → `do중립` fallback.
- **`chooseNationTurn` prologue:** `updateInstance`→`categorizeNationGeneral`→`categorizeNationCities` → npcType≥2 promotion/tax/bill side-effects (`choosePromotion`/`chooseNonLordPromotion` may draw; the rate helpers do NOT; `use_auto_nation_turn` delta) → reserved-honor (GATED, falls through on deny — decision #4) → **priority loop** `foreach nationPolicy->priority → do{X}($lastTurn)` (4 guards incl. the npcType<2 availableInstantTurn restriction) → neutral fallback.
- **`chooseInstantNationTurn`:** QUARANTINED (decision #3) — NOT in the live draw spine; not on the gate.

**PER-METHOD draw tables (the exhaustive map — `docs/superpowers/research/2026-05-30-p5-ai-decision-draw-catalog.md` §5 is the verbatim spec). The highest-risk callouts the leaves MUST reproduce:**
- **발령 (§5.A/B):** general-pick `choice` BEFORE city-pick `choice`/`choiceUsingWeight` (array-literal L→R). `do부대전방발령` draws a VARIABLE number of `choice($nextCityCandidate)` per ambiguous BFS hop AND emits the `destGenaralID` typo → its argTest ALWAYS fails → it ALWAYS returns null (R-BRIDGE §4 — port the latent bug verbatim, the draws still happen before the null). `do유저장구출발령`/`doNPC구출발령` draw a `choice` per qualifying lostGeneral INSIDE the loop (draws = #qualifying + 1, even for discarded args). Insertion = BFS/DB order (F-BFS/F-FACADE).
- **포상/몰수 (§5.C/§7):** ONE `choiceUsingWeightPair`, weight = `count - idx` (positional rank after a STABLE `usort`; `max(warCnt,civilCnt)-idx` for doNPC포상; `takeAmount` for 몰수). A non-stable sort or a secondary comparator reorders ties → different idx → different weight → different single draw. The two 긴급 methods set `reqUpdateInstance=true` AFTER the gate.
- **선전포고 (§5.D):** up to 3 draws — `nextBool(trialProp**6)` → conditionally `nextBool(1/count(lowTargetNations))` in the empty-`nations` fallback (**TS DROPS THIS — PHP wins, 3 vs 2 — confirm Kotlin emits 3, M3**) → `choiceUsingWeight(nations)` weight `1/sqrt(power+1)`. `reqUpdateInstance=true`.
- **거병 (§5.E):** up to 4 draws — `nextBool(0.5)`=nextBit if cityLevel∉{5,6} → per-dist-3-candidate `nextBool()`=nextBit (VARIABLE count, BFS-order-dependent) → `nextFloat1()*70` → `nextBool(0.0075*more)`.
- **국가선택 (§5.F):** `ORDER BY RAND()` 오랑캐 branch (quarantined, unreachable in 1010) → `nextBool(0.3)` → {early `nextBool(pow(...))` or post-early `nextBool()`=nextBit} → sibling-or `nextBool(0.2)`→`choice(paths)`.
- **건국 (§5.I):** `choice(availableNationType)` (13-elem → nextInt(12)) THEN `choice(array_keys(GetNationColors()))` (33-elem LIST → nextInt(32) over INDICES — G6/the existing `domain/GetNationColors.kt`; assert `.size==33`, m6). Order type-THEN-color, do not swap.
- **내정 (§5.K):** `do전쟁내정` TWO `nextBool(0.3)` (decision #9). `do일반내정` one conditional `nextBool(0.3)` + terminal `choiceUsingWeightPair`. `do긴급내정` up to 2 conditional `nextBool`. `do금쌀구매` ZERO draws.
- **징병 (§5.L):** conditional `nextBool(remainPop/maxPop)` → `choiceUsingWeight(availableArmType)` (4-entry FOOTMAN/ARCHER/CAVALRY/WIZARD insertion order; throw unreachable — G7) → `choiceUsingWeight(pickScore)`. `round(crew-49,-2)` PhpRound.
- **워프/이동 (§5.M/N):** `do내정워프` `nextBool(0.6)` ALWAYS → `nextBool(warpProp)` (may consume 0 draws when warpProp≥1/≤0) → `choiceUsingWeight`. `do출병` `nextBool(0.7)` via `&&` (drawn BEFORE 4 non-RNG early-returns — m1) → `choice(attackableCities)` (THROWS if none — guard traced m3). `do소집해제` `nextBool(0.75)` ALWAYS. **`doNPC헌납` `nextBool((genRes/reqRes)-0.5)` is PER-RESOURCE inside `foreach(resourceMap)` rice-then-gold → VARIABLE 0–2 draws (m2), not a single draw.**
- **천도 (§5.M):** `arsort` score-desc → top-quartile gate → `choice(candidates)` next-hop ONLY if `dist>1` (**TS DROPS the cooldown/sticky/BFS-restriction — PHP wins**).
- **중립/사망대비 (§5.H/O):** `||`-short-circuit `nextBool` (only if the `che_인재탐색` gate passes) + a `choice($candidate)` that ALWAYS draws even with 1 element. `do중립` is the TERMINAL fallback (never null).
- **promotion (§5.P/Q):** `chooseNonLordPromotion` up to 5 `choice` per empty chief slot (redraw-on-reject). `choosePromotion` exactly ONE `nextBool(0.1)` per OCCUPIED slot, ZERO per empty slot; the `:4102` `nextBool(newChiefProb)` NEVER draws (decision #9).

**THE CONDITIONAL-DRAW TRAPS (the gate's draw-stream + cursor assertion catches these at the FIRST divergent draw):** `calcGenType` band gate; every `&&`/`||`-short-circuit `nextBool`; the `do전쟁내정` double-0.3; `do출병`'s `:2720` draw-before-early-returns (m1); `doNPC헌납`'s per-resource variable count (m2); `choosePromotion` `:4102` phantom; the variable BFS loops (`do부대전방발령`/`do거병`/구출발령); the `nextBool` prob crossing `>=1`/`<=0`/`===0.5`; `choice([])` throw paths (guard traced — m3). A mis-predicted branch shifts EVERY subsequent draw → total desync.

---

# TIER 0 — Foundations

> Build the foundation wave SEQUENTIALLY (creator-then-consumer). The shared artifacts are touched by EVERY family → never parallel-worktree them. Each task is TDD red→green with a unit test; the per-decision draw-stream golden is finalized in G-GATE.

## AREA F-POLICY — AutorunGeneralPolicy + AutorunNationPolicy (the 4-layer merge → the priority spine)

> **Port target = PHP `AutorunGeneralPolicy.php` (215 lines) + `AutorunNationPolicy.php` (291 lines).** The merged `priority` array ORDER **IS** the `do*` dispatch order, the log order, AND the draw order — this is THE spine. Resolves GAP G1/G2/B1/B4. **BLOCKS everything.** **The KV `priority` override is LIVE for BOTH policies (decision #5, B1, R-NATIONPOL) — a valid bare-action-name override REPLACES the default; a typo/unknown name is DROPPED.**

### Task FP1 — `AutorunGeneralPolicy` (14-priority default + 22 can-flags + doNPCState + the cascade + the LIVE KV override)
**Files:** create `logic/ai/AutorunGeneralPolicy.kt`; test `logic/src/test/.../ai/AutorunGeneralPolicyTest.kt`.
Steps:
- [ ] **READ `legacy/devsam-core/hwe/sammo/AutorunGeneralPolicy.php:1-215` IN FULL FIRST (B4)** — confirm the bare-action-name `static` props (`static $귀환='귀환'` … `:7-39`), the `property_exists($this,$priorityItem)` guard (`:119/132`), and that a valid item is KEPT → `$this->priority=$priority` (`:128-130/141-143`). This source-read is the kickoff gate; the prior "dead-KV" claim was the proof the class was NOT read end-to-end.
- [ ] Port `$default_priority` (`:42-58`) as an ordered `List<String>` (14, EXACT order, `//NPC증여` commented out): `[NPC사망대비, 귀환, 금쌀구매, 출병, 긴급내정, 전투준비, 전방워프, NPC헌납, 징병, 후방워프, 전쟁내정, 소집해제, 일반내정, 내정워프]`. Port the 22 `can*` defaults (`:60-90`): all `true` EXCEPT `can모병=false, can한계징병=false, can고급병종=false, can집합=false, can선양=false`. Port `doNPCState` (`:94-116`, npcType≥2 path — `npc==5` flips `can집합=true`/`can선양=true` + `can국가선택=false`; `npc==1` → `canNPC사망대비=false`; nationID≠0 → `can국가선택/can건국=false`). Port the user-path baseline reset (npcType<2, `:154-179`) + the aiOptions enable map (`develop/warp/recruit/recruit_high(→recruit)/train/battle` → can-flags, `:181-213`, `assert($value)`).
- [ ] Port the 4-layer cascade (`:118-147`): default → serverPolicy (global KV) → nationPolicy (nation KV, wins) → aiOptions (flips can-flags ONLY, never reorders). **Port the LIVE KV-priority override VERBATIM (decision #5, B1): for each of serverPolicy then nationPolicy, if `key_exists('priority',$kv)`, filter the items keeping each whose name passes `property_exists($this,$name)` (TRUE for every valid bare-action-name static — `:7-39`), dropping unknowns with the `E_USER_NOTICE`; if the filtered list is non-empty, `$this->priority = filtered` (REPLACES default).** Model the guard as: keep name ∈ {declared static action-names} ∪ {declared `can*`/instance props}; drop only unknown names; replace only if non-empty. NO `availableInstantTurn` in this class (G1 §3).
- [ ] Test (B1-inverted assertions): default_priority exact 14-order; can-flag defaults; doNPCState npc==5 flips 집합/선양; user-path reset + each aiOption enables its can-set; **a server+nation KV `priority` override with VALID bare-action names REPLACES the default (result == the override list, NOT default_priority — the inverted assertion); a `priority` override containing a typo (`'귀한'`) DROPS that item (keeps the rest); an empty/all-typo filtered list leaves the default; a nation-KV override wins over a server-KV override.**
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AutorunGeneralPolicyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): AutorunGeneralPolicy (14-priority default, 22 can-flags, doNPCState, 4-layer cascade, LIVE property_exists KV-priority override: valid replaces / typo drops)`.

### Task FP2 — `AutorunNationPolicy` (20-priority default + 12-availableInstantTurn + the cascade + chief-gate + PhpRound(-2) derived defaults + LIVE KV override)
**Files:** create `logic/ai/AutorunNationPolicy.kt`; test `logic/src/test/.../ai/AutorunNationPolicyTest.kt`.
Steps:
- [ ] **READ `legacy/devsam-core/hwe/sammo/AutorunNationPolicy.php:1-291` IN FULL FIRST (B4, R-NATIONPOL)** — confirm the 23 bare-name statics (`:7-34`), the `key_exists('priority',...)` whole-array replace at `:197-199` (server) + `:212-214` (nation) then the `if(!priority) priority=defaultPriority` fallback `:218-220`, and the consumer-loop `property_exists($this->nationPolicy,'can'.$name)` validation in `chooseNationTurn` (`:3663-3666`).
- [ ] Port `$defaultPriority` (`:38-68`) as an ordered `List<String>` (20, EXACT order, `//유저장몰수` commented out): `[불가침제의, 선전포고, 천도, 유저장긴급포상, 부대전방발령, 유저장구출발령, 유저장후방발령, 부대유저장후방발령, 유저장전방발령, 유저장포상, 부대구출발령, 부대후방발령, NPC긴급포상, NPC구출발령, NPC후방발령, NPC포상, NPC전방발령, 유저장내정발령, NPC내정발령, NPC몰수]`. Port `$availableInstantTurn` (`:71-84`) as a `Set<String>` (12, key-existence map, order not significant): `{유저장긴급포상, 유저장구출발령, 유저장후방발령, 유저장전방발령, 유저장내정발령, 유저장포상, NPC긴급포상, NPC구출발령, NPC후방발령, NPC내정발령, NPC포상, NPC전방발령}`. Port the FULL value-key set + numeric defaults (`:152-180`): `reqNationGold=10000, reqNationRice=12000, reqNPCDevelRice=500, reqHumanDevelGold=10000, reqHumanDevelRice=10000, CombatForce/SupportForce/DevelopForce maps, minimumResourceActionAmount=1000, maximumResourceActionAmount=10000, minNPCWarLeadership=40, minWarCrew=1500, minNPCRecruitCityPopulation=50000, safeRecruitCityPopulationRatio=0.5, properWarTrainAtmos=90, cureThreshold=10`.
- [ ] Port the derived defaults under STRICT `=== 0` guards, in ORDER (R-NATIONPOL §3 — #3 must derive before #4/#5), all `Util::round(x,-2)=PhpRound(x,-2)` half-away EXCEPT reqNPCDevelGold (raw multiply): `reqNPCDevelGold = develcost*30` (NO round, `:223-225`); `reqNPCWarGold = PhpRound(costWithTech(tech, defaultStatNPCMax*100)*4, -2)`, `reqNPCWarRice = PhpRound(riceWithTech(...)*4, -2)` (`:228-238`); `reqHumanWarUrgentGold = PhpRound(costWithTech(tech, defaultStatMax*100)*3*2, -2)` (NOTE defaultStatMax not NPC; `*3*2`=6 written literally), `reqHumanWarUrgentRice = PhpRound(riceWithTech(...)*3*2, -2)` (`:240-250`); `reqHumanWarRecommandGold = PhpRound(reqHumanWarUrgentGold*2, -2)`, `reqHumanWarRecommandRice = PhpRound(reqHumanWarUrgentRice*2, -2)` (`:252-257`). (`costWithTech`/`riceWithTech`/`getTechCost` per F-HELPERS pins.)
- [ ] Port the cascade (`:182-289`): defaultPolicy → serverPolicy (`['values']` per-key merge with `property_exists` skip-unknown-with-NOTICE + the **LIVE** whole-array `'priority'` replace `:197-199`) → nationPolicy (same, wins, `:212-214`) → `if(!priority) priority=defaultPriority` (`:218-220`) → derived defaults → the chief-gate. Chief-gate: `if npcType≥2 → return` (`:259-261`, all can* stay true); `else if npcType<2 && !key_exists('chief',aiOptions)` (`:263-288`) → set 18 can* flags FALSE (부대전방발령/부대후방발령/부대유저장후방발령/유저장후방발령/유저장전방발령/유저장구출발령/유저장내정발령/NPC후방발령/NPC전방발령/NPC구출발령/NPC내정발령/유저장긴급포상/유저장포상/NPC긴급포상/NPC포상/NPC몰수/선전포고/천도) — **NOT disabled: can부대구출발령, can불가침제의** (R-NATIONPOL §4).
- [ ] Test: defaultPriority exact 20-order (no 유저장몰수); availableInstantTurn 12-set; the derived defaults use PhpRound(-2) NOT Math.round (reqNPCWarGold = PhpRound(reqGold*4,-2); reqHumanWarRecommandGold derives AFTER reqHumanWarUrgentGold); reqNPCDevelGold raw (no round); strict `=== 0` re-triggers derivation, non-zero override suppresses; **a nation KV `priority` override with valid names REPLACES the default (LIVE, identical to general — B1); a typo drops at the consumer-loop can<name> check;** npcType≥2 early-return keeps all can* true; npcType<2 no-chief disables the 18 flags but NOT 부대구출발령/불가침제의.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AutorunNationPolicyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): AutorunNationPolicy (20-priority default, 12-availableInstantTurn, 4-layer cascade, chief-gate 18-flag, ordered PhpRound(-2) derived defaults, LIVE KV-priority override)`.

**F-POLICY gate:** the merged `priority` ORDER drives F-DISPATCH; every family reads the can-flags + value-keys; G-GATE asserts the dispatched-do* reason sequence (a crafted golden with a valid-name override REPLACING the default confirms B1).

## AREA F-BFS — AiDistance (REUSE the GREEN name-order adjacency + BFS/distance wrappers)

> **Port target = PHP `func.php:1895` searchDistance, `2073` searchAllDistanceByCityList (Floyd-Warshall), `2125` searchAllDistanceByNationList, `2139` isNeighbor.** Resolves GAP G3/B6. **NO RNG draws.** **DEFINITIVE (R-BFS): the GREEN `CityConst.byId(x).path` (`common/constants/CityConst.kt:84-88`) is ALREADY name-order (`LinkedHashMap` in `$initCity` source order, byte-identical to PHP `array_keys(...->path)`), and the P4 `war/SearchDistanceListToDest.kt:25-67` already iterates `city.path.keys` in that order. REUSE both — do NOT build a separate name-order adjacency. F-BFS must COMPLETE BEFORE F-FACADE (sequential — R-BFS §4).**

### Task FB1 — confirm-and-reuse the name-order adjacency + `searchDistance` (FIFO, finalize-at-dequeue) wrapper
**Files:** create `logic/ai/bfs/AiDistance.kt`; test `logic/src/test/.../ai/bfs/SearchDistanceTest.kt`.
Steps:
- [ ] **FIRST inspect + ASSERT (B6): the P4 graph's neighbor ordering IS `CityConst.path` name-order, NOT numeric.** Add a test that materializes `CityConst.byId(x).path.keys` for a 2+-neighbor city and asserts it equals the `$initCity` name-list order (resolved name→id), proving no numeric sort. State definitively in the file header: "AI REUSES `CityConst.byId(x).path` + P4 `SearchDistanceListToDest`; no separate adjacency." Do NOT leave the reuse-or-rebuild decision to the implementer.
- [ ] Provide `searchDistance(from, maxDist=99, distForm=false)` over the GREEN adjacency (G3 §1): `ArrayDeque<Pair<Int,Int>>` FIFO; finalize-at-DEQUEUE (`key_exists($cityID,$cities) continue` — duplicate enqueues allowed, first-dequeue wins); record `distanceList[$dist][]` THEN `cities[$cityID]=$dist` in DEQUEUE order (LinkedHashMap); frontier at `dist>=maxDist` recorded-not-expanded; neighbors enqueued in `path.keys` (name) order skipping finalized. `distForm=false` includes `from` (dist 0); `distForm=true` drops the dist-0 bucket. **Reuse the P4 `SearchDistanceListToDest` BFS primitive where it overlaps; do NOT duplicate the graph.**
- [ ] Test: adjacency is name-order not id-sorted (the 2-neighbor assertion); searchDistance dequeue-order insertion; finalize-at-dequeue (duplicate enqueue, first wins); maxDist frontier recorded-not-expanded; distForm drops dist 0; `from` included when distForm=false.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.bfs.SearchDistanceTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/bfs): REUSE GREEN CityConst.path name-order adjacency (asserted not-numeric) + searchDistance (FIFO, finalize-at-dequeue, distForm, dequeue-order insertion)`.

### Task FB2 — `searchAllDistanceByCityList` (Floyd-Warshall row-order) + `searchAllDistanceByNationList` + `isNeighbor`
**Files:** edit `logic/ai/bfs/AiDistance.kt`; test `logic/src/test/.../ai/bfs/FloydWarshallTest.kt`.
Steps:
- [ ] Port `searchAllDistanceByCityList(cityIDList)` (G3 §4): Floyd-Warshall; `cityList` membership in INPUT order; each row `[self=>0]` then in-list neighbors (`path.keys` name order) `=>1`; the triple loop appends newly-discovered pairs to the row in discovery order (cityStop outer × cityFrom mid × cityTo inner) — **preserve the inner-map key order as a LinkedHashMap (self → path-order neighbors → transitive-append); do NOT numerically resort.** Port `searchAllDistanceByNationList(linkNationList, suppliedCityOnly=false)` (G3 §5): SQL `WHERE nation IN (...) [AND supply=1]` → DB-row order `cityIDList` (PK-ascending, R-FACADE §1) → forward to Floyd. Port `isNeighbor(n1, n2, includeNoSupply=true)` (G3 §6): boolean adjacency, `n1===n2→false`, first-hit via `path.keys`.
- [ ] Test: Floyd row key order = self → path-order neighbors → transitive-append discovery; min-relax; no numeric resort; searchAllDistanceByNationList preserves PK-ascending DB-row order; isNeighbor `n1===n2→false` + first-hit + supply filter.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.bfs.FloydWarshallTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/bfs): searchAllDistanceByCityList (Floyd-Warshall row-order append) + searchAllDistanceByNationList (PK-ascending) + isNeighbor`.

**F-BFS gate:** the `[cityID=>dist]` insertion order seeds the `choice`/`choiceUsingWeightPair` candidate lists in L-DEPLOY/L-DIPLO/L-GENFOUND/L-GENWAR + `calcWarRoute`; G-GATE asserts the resulting picks draw-for-draw. **F-FACADE may NOT start until FB1+FB2 are green (R-BFS §4).**

## AREA F-SEED — AiSeed (the "GeneralAI" lineage + getStatValue flavor table) + AiUtils + the env superset

> **Port target = PHP `GeneralAI.php:153-159` (the seed), `General.php:405-418`+`359-403` (getStatValue flavors — G8), `RandUtil.php` (GREEN), `Util.php:872-891` (simpleSerialize — GREEN as `serializeSeed`).** Resolves GAP G8. **REUSE the GREEN `serializeSeed`/`RandUtil`/`MonthScopedRng` — NO new RNG primitive.**

### Task FS1 — `AiSeed` (the 5-component lineage) + the getStatValue flavor table
**Files:** create `logic/ai/AiSeed.kt`; test `logic/src/test/.../ai/AiSeedTest.kt`.
Steps:
- [ ] `AiSeed.rng(hidden, year, month, generalId): RandUtil` = `RandUtil(LiteHashDrbg(serializeSeed(hidden, "GeneralAI", year, month, generalId)))` — 5 components, `hiddenSeed` DIRECT (decision #1; NOT TS `buildSeedBase`). The `"GeneralAI"` token is a SEPARATE stream from execution's `"nationCommand"`/`"generalCommand"`. Build ONCE per general; the caller threads it by reference.
- [ ] Pin the getStatValue flavor table (decision #7, G8) as documented constants/helpers over the GREEN `stats/GetStatValue.kt`: `calcGenType` → `getStatValue(stat, withInjury=false, withIActionObj=true, withStatAdjust=true, useFloor=true)` (strength/intel then `clamp(v, 1.0)`; leadership no floor); reward math → `(false,true,true,true)`; promotion stat-gate → `(false,false,false,false)`. **Do NOT re-round — `useFloor=true` already truncated.**
- [ ] Test: the seed string byte-matches `serializeSeed(hidden,"GeneralAI",y,m,id)` format; determinism; the `"GeneralAI"` lineage differs from a `"generalCommand"` lineage for the same (y,m,id); the 3 flavor signatures produce the expected getStatValue results against a fixture general (no double-round).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AiSeedTest' 2>&1 | tail -30`.
- [ ] Commit: `feat(logic/ai): AiSeed (5-component "GeneralAI" lineage, hiddenSeed-direct) + getStatValue flavor table (calcGenType/reward/promotion signatures)`.

### Task FS2 — `AiUtils` order-then-pick helpers + `WorldEnvBuilder` AI env superset
**Files:** create `logic/ai/AiUtils.kt`; edit `logic/statview/WorldEnvBuilder.kt`; test `logic/src/test/.../ai/AiUtilsTest.kt` + `logic/src/test/.../statview/AiEnvBuilderTest.kt`.
Steps:
- [ ] `AiUtils`: stable `usort`/`uasort`/`arsort` wrappers (PHP 8 unconditionally-stable sort, NO secondary comparator on ties — insertion-order preserved); insertion-ordered `LinkedHashMap` builders; the `count - idx` rank-weight pattern; the `choiceUsingWeightPair` candidate-list builder. These are the deterministic candidate ordering applied BEFORE each draw; every `do<한글>` consumes one.
- [ ] `WorldEnvBuilder`: ADD the AI env superset (research §9) — `develcost/year/month/startyear/turnterm/init_year/init_month/autorun_user/npc_nation_policy/npc_general_policy` (today only `year/startYear/develCost[/month]`). Keep the existing keys unchanged (precheck/full-mode must not drift).
- [ ] Test: arsort stable-DESC with insertion-order on ties (no secondary comparator); count-idx weight; choiceUsingWeightPair candidate order preserved; WorldEnvBuilder emits all AI keys + the existing keys unchanged.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AiUtilsTest' --tests 'opensamguk.logic.statview.AiEnvBuilderTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai+statview): AiUtils order-then-pick (stable sorts, count-idx weight, weighted-pair builder) + WorldEnvBuilder AI env superset`.

**F-SEED gate:** F-INSTANCE + every family consume the seed + the flavor table + the order-then-pick helpers; G-GATE asserts the seed string + the draw stream.

## AREA F-HELPERS — the rate/reward/threshold helper-body source-read (B7/M5/M6)

> **Port target = the helper bodies cited in R-FACADE §2/§3/§4.** Resolves B7/M5/M6 + GAP G9/G11. **This is a SOURCE-READ task — it produces a research note `.context/p5-research/H-HELPERS.md` with every helper's `file:line` + rounding/sentinel pinned, NOT code. It BLOCKS F-INSTANCE (joinYearMonth), F-FACADE (calcRecentWarTurn/cutTurn), L-REWARD (getOutcome/getDedLevel), L-RATES (getGoldIncome/getRiceIncome/bill).** No leaf may assert a threshold constant that is not pinned here.

### Task FH1 — read + pin every rate/reward/threshold helper body with file:line citations
**Files:** create `.context/p5-research/H-HELPERS.md` (research note); no gradle test (it gates the code tasks).
Steps:
- [ ] Pin `Util::joinYearMonth(year,month) = year*12 + month - 1` (`Util.php:709`, the **-1** variant) + `parseYearMonth(ym) = [intdiv(ym,12), ym%12+1]` (`:713`) (M6/G11, R-FACADE §3). The `calcDiplomacyState` early-gate is `ym <= joinYearMonth(startyear+2,5) = (startyear+2)*12 + 4` (`GeneralAI.php:219`). Also used at `:1830` (불가침 diplomatMonth), `:306/323`.
- [ ] Pin `General::calcRecentWarTurn(turnTerm)` (`General.php:273-296`, B7, R-FACADE §2): `recent_war` falsy → **sentinel `12*1000 = 12000`**; else `intdiv(Util::toInt(secDiff), 60*turnTerm)` trunc-toward-zero; `secDiff<=0 → 0`; **calcCache-memoized**. Consumers in `categorizeNationGeneral`: `:3544` `if recentWar >= (belong-1)*12 continue` (exclude pre-임관); `:3549` `lastWar=min(lastWar,recentWar)` (seed PHP_INT_MAX); `:3585` `if recentWar <= lastWar+12 → userWar else (dipState≠평화 && crew>=minWarCrew → userWar) else userCivil`. Pin `cutTurn` (`func.php:946-962`, anchor = (day−1) at 01:00:00, intdiv minute diff, floor to turnterm) — used in `do부대전방발령` chiefTurn/leaderTurn compare (`:305/322`).
- [ ] Pin `getDedLevel` = `valueFit(ceil(sqrt(ded)/10), 0, maxDedLevel)` (`func_converter.php:643`, **ceil** distinct from round); `getBillByLevel = dedLevel*200+400`; `getOutcome(billRate, generalList) = PhpRound(sum(getBill)*billRate/100)` (`func_time_event.php:239-249`, **half-away**); the `dedicationList` filter `npc != 5` + the **dead unused-append** `$nationGenerals[]=$this->general` (`:4216/4263` — port verbatim, it's the latent bug). Pin `getGoldIncome`/`getRiceIncome` (`:141-211`): per-city `PhpRound(onCalcNationalIncome)`, `supply==0→0`, capital `*=1+1/3/nationLevel`, officer `pow(1.05,n)`, then `*= taxRate/20` (FLOAT, no round); **caller rate diverges — `taxRate=15` for diplomacy-cost est `:1808`, `$nation['rate']` for bill `:4225/4271`.**
- [ ] Pin `TechLimit` = `getTechLevel(tech) >= valueFit(floor(relYear/techLevelIncYear)+initialAllowedTechLevel, 1, maxTechLevel)` (`func_converter.php:684`, **floor**); `getTechLevel = valueFit(floor(tech/1000),0,maxTechLevel)`; `riceWithTech/costWithTech = rice|cost * getTechCost(tech) * crew/100` (`GameUnitDetail.php:120-128`, FLOAT no round); `getTechCost = 1 + getTechLevel*0.15`. Pin the facade threshold gates (R-FACADE §4 table): user-war reqMoney `×3×1.1` (`:1260`) `+3yr` floor (`:1261`); second tier `×6×1.1` (`:1364`) `+3yr` (`:1365`); NPC half `reqNPCWar/2` (`:1431/1436`); NPC-war `×1.5` (`:1460`) `+5yr` (`:1461`); NPC second `×3×1.1` (`:1557`) `+5yr` (`:1558`); 임관 gate `< startyear+3` (`:3363`). **PIN: user gates `startyear+3`, NPC recruit gates `startyear+5`.**
- [ ] (No gradle test — this note is the citation source the dependent tasks MUST reference. F-INSTANCE/F-FACADE/L-REWARD/L-RATES tests cite H-HELPERS line numbers, never assert an unpinned constant.)
- [ ] Commit: `docs(p5-research): H-HELPERS — pin every rate/reward/threshold helper body (joinYearMonth -1, calcRecentWarTurn 12000 sentinel, getOutcome half-away, getDedLevel ceil, TechLimit floor, +3yr/+5yr gates) with file:line`.

**F-HELPERS gate:** F-INSTANCE/F-FACADE/L-REWARD/L-RATES cite this note; no threshold constant is asserted without a `file:line` pin.

## AREA F-INSTANCE — AiInstanceState (updateInstance + the FIRST-draw calcGenType + diplomacy meta-KV deltas)

> **Port target = PHP `GeneralAI.php:86-145` (updateInstance), `:175-204` (calcGenType — the FIRST draw), `:206-281` (calcDiplomacyState — NO draws, WRITES meta-KV), `:128-137` (maxResourceActionAmount).** Research §"Derived state". **The FIRST draw site (calcGenType); the two cache regimes (decision #2) MUST stay distinct; `calcDiplomacyState` is NOT a pure function — it queues meta-KV deltas (decision #12, M4).** Depends on F-POLICY, F-SEED, F-HELPERS.

### Task FI1 — `updateInstance` + `maxResourceActionAmount` + `calcDiplomacyState` (reqUpdateInstance regime, NO draws, meta-KV deltas)
**Files:** create `logic/ai/AiInstanceState.kt`; test `logic/src/test/.../ai/AiInstanceStateTest.kt`.
Steps:
- [ ] **CONFIRM the `joinYearMonth` formula against H-HELPERS (`year*12+month-1`, the -1 variant) BEFORE committing (M6/G11).** The early-gate off-by-one (`+0` vs `-1`) flips the `calcDiplomacyState` early-return → changes dipState/attackable/warTargetNation → desyncs every do불가침제의/do선전포고/do천도 boolean.
- [ ] Port `updateInstance` (`:86-145`, guarded by `reqUpdateInstance` → runs once unless re-dirtied; dirty triggers `:1308/1509/1972/1990/2113`): `baseDevelCost=develcost*12`; city row; nation row (21-col explicit ?? neutral-default `재야` nation=0/level=0); `nationStor` KV cache; `nationPolicy`/`generalPolicy` (F-POLICY); `maxResourceActionAmount = valueFit(PhpRound(max(minimumResourceActionAmount, ...prev-income-based...), -2), null, maximumResourceActionAmount)` then hard-cap `GameConst.maxResourceActionAmount` (**PhpRound -2, NOT Math.round**); `nation.aux` JSON-decode in place; `calcDiplomacyState()`.
- [ ] Port `calcDiplomacyState` (`:206-281`, NO draws, WRITES meta-KV — decision #12/M4): the early-phase gate (`yearMonth <= joinYearMonth(startyear+2,5)` per H-HELPERS) → d평화/d선포 + EARLY RETURN (leaves warTargetNation null); normal phase `attackable = !!max(front) over supplied cities`; the warTarget partition (state 0→onWar+warTargetNation=2; state 1 & term<5→onWarReady=1; else onWarYet); `warTargetNation[0]=1` sentinel; dipState from `min(term)` thresholds; war-upgrade override to d전쟁. **The `last_attackable=yearMonth` KV writes (`:266/271/275`) route through `ChangeRecorder` delta, NOT inline and NOT a module-static Map (TS WRONG). The read at `:276` observes the PRE-TURN snapshot value; the delta is visible only NEXT turn (after the chosen command flushes).**
- [ ] Test: updateInstance runs once then short-circuits unless re-dirtied; neutral-default nation when nationID==0; maxResourceActionAmount uses PhpRound(-2) + valueFit + hard-cap; calcDiplomacyState early-phase gate EARLY-RETURNs with null warTargetNation at exactly `ym == (startyear+2)*12+4` boundary; the dipState thresholds; **`last_attackable` recorded as a ChangeRecorder delta (no inline write, no static Map) and the read uses the pre-turn snapshot;** ZERO rng draws in updateInstance/calcDiplomacyState.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AiInstanceStateTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): AiInstanceState updateInstance (reqUpdateInstance regime, maxResourceActionAmount PhpRound(-2), calcDiplomacyState joinYearMonth(-1) early-gate, last_attackable ChangeRecorder delta + pre-turn-snapshot read)`.

### Task FI2 — `calcGenType` (the decision's FIRST draw — the band-gate conditional nextBool)
**Files:** edit `logic/ai/AiInstanceState.kt`; test `logic/src/test/.../ai/CalcGenTypeTest.kt`.
Steps:
- [ ] Port `calcGenType` (`:175-204`, the FIRST draw site): inputs `leadership=getStatValue(...,false,...)`, `strength=clamp(getStatValue(...,false,...),1.0)`, `intel=clamp(...,1.0)` (decision #7, G8 — `valueFit(v,1)` floor on strength/intel only). Flags `t무장=1, t지장=2, t통솔장=4` (bitwise `|=`). `if strength>=intel` → `genType=t무장`; **IF `intel >= strength*0.8`** → DRAW `nextBool(intel.toDouble()/strength/2)` (`:185`) → `|=t지장`. `else` → `genType=t지장`; IF `strength >= intel*0.8` → DRAW `nextBool(strength.toDouble()/intel/2)` (`:193`) → `|=t무장`. Then (NO draw) `if leadership >= nationPolicy.minNPCWarLeadership → |=t통솔장`. **`*0.8` and `/2` are Double float division — do NOT use integer division; the prob ∈ [0.4,0.5).**
- [ ] Test: outside the band (e.g. strength≫intel) → ZERO draws + genType=t무장; inside the band → exactly one `nextBool` with the exact Double float arg (a recording rng stub asserts the draw COUNT shifts with the predicate AND the float arg is bit-identical); tie `strength==intel` → 무장 branch (`>=`); the `t통솔장` flag is non-RNG; the FIRST draw of the decision comes off the `"GeneralAI"` stream (cursor at 0 before, advanced after only when the band holds).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.CalcGenTypeTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): calcGenType (the decision's FIRST conditional nextBool, near-balance band gate, getX(false) flavor, Double float arg)`.

**F-INSTANCE gate:** the FIRST-draw conditional is the #1 desync trap — its presence/absence shifts every later draw; G-GATE asserts it draw-for-draw + cursor-for-cursor.

## AREA F-FACADE — AiWorldView (the categorize/derive read-only facade; PK-ascending HARD requirement)

> **Port target = PHP `GeneralAI.php:3469-3513` (categorizeNationCities), `:3516-3613` (categorizeNationGeneral), `:283-292` (calcWarRoute).** Research §"city buckets"/"general buckets"/"war route". **NO draws — sets candidate ORDER. Per-field null-guards (decision #2). Cities MUST exist before generals (by-ref mutation).** Depends on F-BFS (calcWarRoute — SEQUENTIAL after F-BFS per R-BFS §4) + F-INSTANCE + F-HELPERS (calcRecentWarTurn/cutTurn).

### Task FC1 — `categorizeNationCities` (PK-ascending HARD requirement, the 4 buckets) + `calcWarRoute`
**Files:** create `logic/ai/AiWorldView.kt`; test `logic/src/test/.../ai/AiWorldViewCitiesTest.kt`.
Steps:
- [ ] **HARD REQUIREMENT (B5, R-FACADE §1): materialize the city list with `.sortedBy { it.cityId }` (ascending) and build the four maps as `LinkedHashMap` in that exact order. PK-ascending = PHP's no-`ORDER BY` default (InnoDB clustered PK). MariaDB-no-ORDER-BY ≠ Postgres/InMemory native order — the explicit sort is NOT optional and NOT a P2 note; it is a step here.** The PHP query `SELECT * FROM city WHERE nation=%i` (`:3469`) has NO `ORDER BY` (verified R-FACADE §1) → PK-ascending is the pinned fact.
- [ ] Port `categorizeNationCities` (`:3469-3513`, lazy-once null-guard, NO draws): `cities WHERE nation` in PK-ascending order → `LinkedHashMap` by cityID; each row gets `dev=(agri+comm+secu+def+wall)/(*_max sum)` float, `important=1`, empty `generals` list (filled later by-ref). Buckets: `supplyCities`=supply; `frontCities`=front; `backupCities`=supply AND NOT front (the `elseif`); `nationCities`=ALL. **Cities MUST be built before generals (the by-ref mutation).**
- [ ] Port `calcWarRoute` (`:283-292`, lazy-once, NO draws): `target = array_keys(warTargetNation ?? []); target[] = ownNation; warRoute = searchAllDistanceByNationList(target, false)` (F-BFS). Preserve `[...warTargets, ownNation]` append order (affects tie ordering).
- [ ] Test: nationCities in PK-ascending LinkedHashMap order (a fixture with shuffled InMemory insertion still yields cityId-ascending buckets — the explicit sort proven); the 4 buckets (supply/front/backup=supply-not-front/all); dev float; calcWarRoute target append order [warTargets, ownNation]; both lazy-once (NOT re-invalidated by a reqUpdateInstance flip); ZERO draws.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AiWorldViewCitiesTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): AiWorldView categorizeNationCities (PK-ascending sortedBy{cityId} HARD requirement, 4 buckets, dev float, by-ref generals) + calcWarRoute (BFS, append order)`.

### Task FC2 — `categorizeNationGeneral` (PK-ascending, the 9 buckets, first-match-wins ladder, calcRecentWarTurn, by-ref into cities)
**Files:** edit `logic/ai/AiWorldView.kt`; test `logic/src/test/.../ai/AiWorldViewGeneralsTest.kt`.
Steps:
- [ ] **HARD REQUIREMENT (B5, R-FACADE §1): load generals `.sortedBy { it.no }` (ascending) and build all 9 buckets + the per-city `generals`/`important` accumulation as `LinkedHashMap` in that order. PK-ascending = the no-`ORDER BY` default of `SELECT no FROM general WHERE nation=%i AND no!=%i` (`:3516`, EXCLUDES self).**
- [ ] Port `categorizeNationGeneral` (`:3516-3613`, lazy-once, NO draws): Pass 1: `lastWar=min(calcRecentWarTurn)` skipping pre-임관 battles (the `12*1000=12000` sentinel + trunc-div + the `>= (belong-1)*12` skip — H-HELPERS/R-FACADE §2). Pass 2: `important += 1` per officerLevel∈[2,4] on its `officer_city`; `chiefGenerals[officerLevel]` keyed by LEVEL (later overwrites on dup); city-attach + lost detection; then the **first-match-wins ladder (REORDERING changes bucketing → changes commands → changes draws):** `npcType==5`→troopLeaders; non-npc `che_집합`-leader→troopLeaders; `killturn<=5`→npcCivil; `npcType<2`→userGenerals + (userWar if `recentWar<=lastWar+12` OR (dipState≠평화 & crew≥minWarCrew) else userCivil — H-HELPERS `:3585`); `leadership(false)>=minNPCWarLeadership`→npcWar; else npcCivil. **Mutates `nationCities['important']`/`['generals']` by reference — cities MUST exist first (FC1 before FC2).**
- [ ] Test: self EXCLUDED; **general.no PK-ascending order (shuffled InMemory still yields no-ascending — the explicit sort proven, B5);** the first-match-wins ladder order (each rung asserted — a leadership≥min npc lands npcWar only if no earlier rung matched); the calcRecentWarTurn sentinel + the userWar/userCivil split at `lastWar+12` (H-HELPERS-cited, not asserted bare); chiefGenerals overwrite-on-dup-level; important += per officer 2-4; generals attached into the city buckets by-ref; lazy-once; ZERO draws.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.AiWorldViewGeneralsTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): AiWorldView categorizeNationGeneral (PK-ascending sortedBy{no} HARD requirement, 9 buckets, first-match-wins ladder, calcRecentWarTurn 12000 sentinel, by-ref into cities)`.

**F-FACADE gate:** the bucket insertion order seeds the 발령/포상/promotion candidate lists; G-GATE asserts the resulting picks. **Sequential after F-BFS (R-BFS §4).**

## AREA F-BRIDGE — CandidateBridge + argTest + the 5 presets + the 9 missing che_* defs + the che_출병 pack repair

> **Port target = PHP `BaseCommand.php:377-413` (testFullConditionMet: isArgValid → testAll → testPostReqTurn), the `Command/{General,Nation}/che_*.php` FULL packs (R-BRIDGE §1/§2), the 5 missing ConstraintHelpers (R-BRIDGE §3).** Resolves GAP G5 + M1/M2/M8/M9. **The AI's gate IS `evaluateConstraints(...FULL...)` fronted by an argTest gate — the IDENTICAL call execution makes. The 9 missing defs + 1 divergent-pack repair + 5 presets are P5 work, NOT a P6 deferral.** Reuse the GREEN `constraints/EvaluateConstraints.kt`/`Presets.kt`/`Comparators.kt` + `actions/CommandRegistry.kt`.

### Task FR0 — the emitted-code → Kotlin-pack mapping (M1) + the 4 sub-package dirs (m11)
**Files:** create `.context/p5-research/H-BRIDGE-MAP.md` (mapping note); scaffold the 4 `actions/` sub-package dirs (or pick the flat layout); no gradle test.
Steps:
- [ ] **ENUMERATE every AI-emitted `che_*` code → its Kotlin `GeneralActionDefinition` pack status (M1, R-BRIDGE §1/§2): 35 distinct emitted codes (5 nation: 발령/포상/몰수/불가침제의/선전포고/천도; 30 general). EXISTS-GREEN: 29 (incl. che_몰수). MISSING-DEF (9): che_불가침제의, che_선전포고, che_NPC능동, che_귀환, che_인재탐색, che_견문, che_해산, che_요양, che_선양. DIVERGENT-PACK (1): che_출병 (Kotlin missing NotOpeningPart/NotSameDestCity/ReqGeneralRice/AllowWar/HasRouteWithEnemy).** Flag each MISSING/DIVERGENT as a P5 blocker (FR1/FR2). 
- [ ] Decide the layout (m11): scaffold `actions/{military,personnel,founding,develop}/` OR consolidate into flat `actions/` (P2–P4 style). Pin the choice in the note so FR2 is consistent. **Note (M9): `che_발령.argTest()` requires the CORRECT `destGeneralID`; `do부대전방발령` emits the typo `destGenaralID` → always-null (port verbatim) — the bridge's argTest gate (FR3) reproduces the FALSE-on-missing-key reject.**
- [ ] Commit: `docs(p5-research): H-BRIDGE-MAP — 35 emitted che_* → Kotlin pack status (29 green, 9 missing-def, 1 divergent che_출병), CheBallyeong typo verdict, sub-package layout choice`.

### Task FR1 — the 5 missing diplomacy/pathfinding presets + the che_출병 FULL-pack repair (M8)
**Files:** edit `logic/constraints/Presets.kt` + `logic/constraints/CheChulbyeong.kt`; test `logic/src/test/.../constraints/DiploPresetsTest.kt` + `CheChulbyeongPackTest.kt`.
Steps:
- [ ] **VERIFY FIRST (M8, R-BRIDGE §3): grep `logic/.../constraints/` for `hasRoute|HasRoute|RouteWithEnemy` — confirmed NO matches → BOTH `hasRoute` AND `hasRouteWithEnemy` are absent → 5 NEW presets, NOT 4. (`Presets.kt` has `nearCity` but neither route preset.)** Do NOT add a phantom duplicate.
- [ ] ADD the 5 presets (R-BRIDGE §3): `existsDestNation`, `nearNation` (isNeighbor-backed — F-BFS), `hasRoute` (searchDistanceListToDest with allowedNationList), `hasRouteWithEnemy` (same search incl. enemy-at-war nations), `disallowDiplomacyBetweenStatus(map)`, `allowDiplomacyBetweenStatus`/`allowDiplomacyWithTerm` (`allowDiplomacyStatus` partially exists). First-deny-wins, reason strings byte-matched to PHP.
- [ ] **REPAIR che_출병 (M8/R-BRIDGE §2, PARITY-FATAL): `CheChulbyeong.buildConstraints` currently ships only `[notBeNeutral, occupiedCity, reqGeneralCrew]`; PHP `che_출병.php:78-87` FULL pack = `[NotOpeningPart(relYear), NotSameDestCity, NotBeNeutral, OccupiedCity, ReqGeneralCrew, ReqGeneralRice(reqRice), AllowWar, HasRouteWithEnemy]`. Add the 5 absent (NotOpeningPart, NotSameDestCity, ReqGeneralRice, AllowWar, HasRouteWithEnemy) in PHP order so the AI gate boolean flips identically.** (P4 trimmed the pack for the BO3 battle gate; the P5 AI bridge needs the FULL PHP pack — confirm the P4 battle tests still pass with the additions, or scope the FULL pack to the AI-bridge ctx only if P4 resolve depends on the trimmed set.)
- [ ] Test: each preset Allow/Deny matches PHP on a fixture (existsDestNation on a deleted nation → Deny; nearNation via isNeighbor; hasRouteWithEnemy via searchDistance with enemy nations; disallowDiplomacyBetweenStatus reason `'아국과 이미 교전중입니다.'` byte-equal); che_출병's FULL pack has all 8 constraints in PHP order (the 5 additions present); the P4 battle resolve still green.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.constraints.DiploPresetsTest' --tests 'opensamguk.logic.constraints.CheChulbyeongPackTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/constraints): 5 diplomacy/pathfinding presets (existsDestNation/nearNation/hasRoute/hasRouteWithEnemy/disallowDiplomacyBetweenStatus/allowDiplomacyWithTerm) + che_출병 FULL-pack repair (5 absent constraints in PHP order)`.

### Task FR2 — the 9 missing emitted `che_*` command defs + registry registration
**Files:** create the 9 def files (per the FR0 layout); edit `logic/actions/CommandRegistry.kt`; test `logic/src/test/.../actions/MissingAiCommandDefsTest.kt`.
Steps:
- [ ] Build the 9 missing defs (R-BRIDGE §2): `che_불가침제의` (argTest destNationID/year/month; 6mo-min AlwaysFail + `BeChief/NotBeNeutral`+diplomacy-term pack), `che_선전포고` (argTest destNationID; `BeChief/NotBeNeutral/OccupiedCity/SuppliedCity/ReqEnvValue(year>=startYear+1)/ExistsDestNation/NearNation/DisallowDiplomacyBetweenStatus`), `che_NPC능동` (argTest optionText='순간이동'+destCityID; **EMPTY fullCond** + MustBeNPC permission → gate ≈ argValid, R-BRIDGE), `che_귀환`, `che_인재탐색`, `che_견문`, `che_해산`, `che_선양` (BeLord gate; ORDER BY RAND quarantine note, decision #6), `che_요양` (dispatcher-direct, gate-exempt — def needed for execution). **(che_몰수 is EXISTS-GREEN — do NOT re-create it; R-BRIDGE §2.)** Register all 9 in `CommandRegistry.resolve` (F-BRIDGE owns the widening; no family re-touches it).
- [ ] Test: each def resolves from `CommandRegistry`; argTest canonicalizes/rejects per PHP; the FULL pack constraint ORDER + reason strings byte-match PHP (che_NPC능동 fullCond is EMPTY; che_선전포고 has ExistsDestNation/NearNation); che_요양 resolves but is gate-exempt-marked; che_선양 carries the ORDER BY RAND quarantine note.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.actions.MissingAiCommandDefsTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/actions): 9 missing AI-emitted che_* defs (불가침제의/선전포고/NPC능동/귀환/인재탐색/견문/해산/선양/요양) + registry registration`.

### Task FR3 — `candidateAllowed` helper (argTest gate + the constraint bridge) + gate-exempt path list
**Files:** create `logic/ai/CandidateBridge.kt`; test `logic/src/test/.../ai/CandidateBridgeTest.kt`.
Steps:
- [ ] `candidateAllowed(actionCode, RAWargs, ctx, view): Boolean` (decision #10, M2/M9): **the AI emits RAW args; the bridge runs the command's `isArgValid/argTest` (canonicalize + reject on a missing required key with `'인자가 올바르지 않습니다.'`) EXACTLY ONCE as the FIRST gate (BaseCommand.php:377 order: isArgValid → Constraint::testAll → testPostReqTurn), THEN `evaluateConstraints(def.buildConstraints(ctx.copy(args=canonicalized, mode=FULL)), ctx, view) is Allow`.** FULL mode (PRECHECK's `Unknown` phantom-denies). **`parseArgs` BECOMES the argTest analogue (rejecting on missing keys) — do NOT canonicalize in the AI-emit layer AND again in the command (no double-canonicalization, M2).** `testPostReqTurn` term-stack tail AFTER, FRESH ctx per candidate. **This MUST be the IDENTICAL call `ReservedTurnHandler.handle` makes (A13).** Provide the gate-EXEMPT list (G14): `do집합`/`do요양` (built+returned directly) + the `사망`/`재야유저`/`do예약턴` reserved-returns — these BYPASS `candidateAllowed`.
- [ ] **Reproduce the CheBallyeong typo reject (M9, R-BRIDGE §4): with the `destGenaralID` typo key present and `destGeneralID` absent, argTest returns false → `candidateAllowed` returns false → `do부대전방발령` null-returns (the PHP latent always-null bug, port verbatim). Kotlin currently has NO argTest gate (it silently null-coerces) — this gate is the fix.**
- [ ] Pin the deny-log string from the golden (G12, UNCERTAIN): the NATION reserved-fail path logs `"{failString} <1>{date}</>"` where `failString = getFailString() = "{failReason} {commandName} 실패."` (reason-first, trailing period, no date). Do NOT trust the current Kotlin `denyLog` (`"{name} 실패: {reason} <1>{date}</>"`) — re-derive in G-GATE. The GENERAL reserved path honors WITHOUT logging (decision #4).
- [ ] Test: candidateAllowed FULL-mode Allow/Deny matches `ReservedTurnHandler`'s call on a fixture; **the argTest gate runs BEFORE constraints and rejects a missing-key (the CheBallyeong typo → false → null-return); the AI emits RAW args and canonicalization happens ONCE (no double-canonicalize — a fixture asserting the emitted args are unchanged while the canonicalized ctx differs);** fresh ctx per candidate (no memo bleed); the gate-exempt paths bypass the helper; PRECHECK `Unknown` would phantom-deny (assert FULL is used).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.CandidateBridgeTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): CandidateBridge candidateAllowed (argTest gate FIRST + single canonicalization of RAW args + FULL-mode evaluateConstraints + term tail, fresh ctx, gate-exempt list, CheBallyeong typo reject) + deny-log golden note`.

**F-BRIDGE gate:** every family calls `candidateAllowed` to decide null-return; G-GATE asserts the gate boolean drives the priority loop branch identically.

## AREA F-DISPATCH — GeneralAI (the 2 LIVE master orders + pre-loop branches + reserved-honor asymmetry + the quarantined instant stub)

> **Port target = PHP `GeneralAI.php:3708-3790` (chooseGeneralTurn), `:3640-3683` (chooseNationTurn), `:3685-3705` (chooseInstantNationTurn — QUARANTINED, decision #3).** Research §"dispatcher spines". **The merged `priority` ORDER (F-POLICY) IS the `do*` dispatch order IS the log order IS the draw order — this is THE spine. The reserved-honor asymmetry (decision #4) and the two DELIBERATELY-DIVERGENT live loops are FROZEN here.** Depends on F-POLICY, F-INSTANCE, F-FACADE, F-BRIDGE. **Owns the dispatch loop body — families register `do<한글>` methods by ACTION-NAME (m12), never edit the loop.**

### Task FD1 — `ChosenCommand` + `chooseGeneralTurn` (the general spine: prologue draw + pre-loop branches + reserved-honor NO-gate + priority loop + do중립 fallback)
**Files:** create `logic/ai/GeneralAI.kt`; test `logic/src/test/.../ai/ChooseGeneralTurnTest.kt`.
Steps:
- [ ] Define `ChosenCommand(actionCode: String, args: Map<String,Any?>, reason: String)` (the AI return; `reason` = the firing `do<한글>` name for the gate). The dispatch is a `LinkedHashMap<String, (LastTurn?) -> ChosenCommand?>` keyed by ACTION-NAME (the leaf families register their bodies into THIS map by name — m12; the loop body is owned here, frozen).
- [ ] Port `chooseGeneralTurn` (`:3708-3790`) VERBATIM: (1) `updateInstance()` (calcGenType FIRST draw — F-INSTANCE). (2) the npcmsg prologue draw `:3719` — ONLY if `env['npcmsg']` truthy (`&&` short-circuit, side-effect NO return), `nextBool(npcMessageFreqByDay*turnterm/(60*24))`; **a side-effect-only draw — port the `&&` short-circuit exactly (if npcmsg falsy, ZERO draws here).** (3) the gate-exempt reserved-returns FIRST (G14): `사망`(injury/death bookkeeping) / `재야유저` / `do예약턴` return directly, BYPASSING `candidateAllowed`. (4) the pre-loop branches in PHP order: `do선양`(officer_level==12 && can선양 — quarantined-unreachable in 1010, F-QUAR) / `do집합`(gate-exempt, npc==5 draws — see FD3) / `do거병`((npc==2||3)&&nationID==0) / `do국가선택`(nationID==0&&can국가선택) / `do방랑군이동`(officer_level==12&&!capital). (5) **the reserved-honor: the GENERAL path honors a non-휴식 reserved command WITHOUT re-validating (NO `candidateAllowed` gate) and WITHOUT logging (decision #4) — do NOT add a check; the `ReservedTurnHandler` execution gate is the backstop.** (6) the `do요양` step-6 (injury>cureThreshold, direct `che_요양` build, gate-exempt, `:3773`). (7) **the priority loop:** `for actionName in generalPolicy.priority: dispatch[actionName]?.invoke(null)?.let { return it }` — first non-null wins; each `do<한글>` is gated internally by `candidateAllowed` (except the gate-exempt list). (8) `do중립` TERMINAL fallback (never null).
- [ ] **HARD WARNING (m8) in the SETUP contract:** calcGenType's conditional FIRST draw (band gate `weaker >= stronger*0.8`) shifts EVERY later draw; F-SEED flavor table (G8) MUST be GREEN before FD1; the prologue npcmsg `&&` short-circuit is the SECOND conditional-draw trap.
- [ ] Test: the dispatch order EQUALS `generalPolicy.priority` (a crafted policy with a custom valid-name priority — B1 — fires `do*` in the override order); first-non-null short-circuits (a stubbed earlier `do*` returning non-null prevents later draws); the npcmsg draw fires ONLY when npcmsg truthy (the `&&` short-circuit — ZERO draws when falsy); the reserved-honor general path returns the reserved command with NO gate and NO log (decision #4); the gate-exempt reserved-returns bypass `candidateAllowed`; `do중립` is the guaranteed fallback.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.ChooseGeneralTurnTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): GeneralAI.chooseGeneralTurn (priority-loop spine = merged-priority order, npcmsg && prologue draw, gate-exempt reserved-returns, reserved-honor NO-gate/NO-log asymmetry, do중립 fallback) + ChosenCommand`.

### Task FD2 — `chooseNationTurn` (the nation spine: promotion/tax/bill side-effects + GATED reserved-honor + the 4-guard priority loop) + the QUARANTINED `chooseInstantNationTurn` stub
**Files:** edit `logic/ai/GeneralAI.kt`; test `logic/src/test/.../ai/ChooseNationTurnTest.kt`.
Steps:
- [ ] Port `chooseNationTurn` (`:3640-3683`) VERBATIM: (1) `updateInstance` → `categorizeNationGeneral` → `categorizeNationCities` (F-FACADE, cities-before-generals). (2) the npcType≥2 step-1 side-effects: `choosePromotion`/`chooseNonLordPromotion` (MAY draw — L-RATES) ONLY on month∈{3,6,9,12}; `chooseTexRate`/`chooseGoldBillRate`(month 12)/`chooseRiceBillRate`(month 6) (NO draws); the `use_auto_nation_turn` KV delta (ChangeRecorder — decision #12). (3) **the GATED reserved-honor (decision #4 — DISTINCT from the general path): the NATION path DOES gate via `hasFullConditionMet()`; on deny it emits a `getFailString()` fail-log THEN FALLS THROUGH to the loop** (port the fall-through + the fail-log; deny-log string re-derived in G-GATE per FR3 note). (4) **the 4-guard priority loop** `for actionName in nationPolicy.priority`: guard-A `property_exists(nationPolicy,'can'+actionName)` else `trigger_error+continue` (R-NATIONPOL §1 — typo drops HERE at the consumer loop); guard-B `nationPolicy.{'can'+actionName}` flag false → continue; guard-C **the npcType<2 availableInstantTurn restriction** `npcType<2 && !(availableInstantTurn[actionName] ?? false)` → continue (R-NATIONPOL §2-A, `?? false`); then `dispatch[actionName]?.invoke(lastTurn)?.let { return it }`. (5) the neutral fallback.
- [ ] **Port the QUARANTINED `chooseInstantNationTurn` (`:3685-3705`) as a structural stub (decision #3, B3, R-SEAM §3): DELIBERATELY DIVERGENT from `chooseNationTurn` — gate-FIRST, NO reason strings, the 2-guard loop (guard-A `key_exists(actionName, availableInstantTurn)` via `key_exists` NOT `?? false` — R-NATIONPOL §2-B; guard-B `can`-flag), `reservedCommand` threaded by-ref. Mark `@ParityQuarantine("R-SEAM-no-call-site")`, EXCLUDE from G-GATE, NOT wired into F-SEAM, log to the phase backlog with the sibling `chooseNationTurn` byte-match proof. Do NOT collapse the two live loops.** (m7: the `do$X(NationCommand $reservedCommand)` param typing — pin a Kotlin-typeable nullable/unused-param contract since it never reaches the gate; this is moot for the gate but needed if the stub is to compile.)
- [ ] Test: the nation dispatch order EQUALS `nationPolicy.priority` after the merge (B1 valid-name override REPLACES); the 4 guards each skip correctly (guard-A typo→continue; guard-C npcType<2 restricts to the 12 availableInstantTurn — R-NATIONPOL §2; npcType≥2 bypasses guard-C → full 20-entry eligible); the GATED reserved-honor fail-logs THEN falls through (DISTINCT from the general NO-gate path); promotion side-effects fire only on month∈{3,6,9,12}; `use_auto_nation_turn` recorded as a ChangeRecorder delta; **`chooseInstantNationTurn` is `@ParityQuarantine`, uses `key_exists` not `?? false`, and is NOT referenced by F-SEAM (a test asserts no live wiring).**
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.ChooseNationTurnTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): GeneralAI.chooseNationTurn (categorize order, npcType>=2 promotion/tax/bill side-effects + use_auto_nation_turn delta, GATED reserved-honor fall-through, 4-guard priority loop incl npcType<2 availableInstantTurn) + chooseInstantNationTurn QUARANTINED stub (key_exists, by-ref, @ParityQuarantine, not wired)`.

**F-DISPATCH gate:** the dispatch order = the merged priority order; the leaf families register their `do<한글>` by name; G-GATE asserts the fired-do* reason sequence + the draw stream. The two LIVE loops stay DELIBERATELY DIVERGENT; the instant stub is OFF the gate.

## AREA F-SEAM — the daemon interpose (ReservedTurnHandler widen + AI hook + ProcessNationCommand port + officer≥5 nation pass + AiTurnAdapter)

> **Port target = PHP `TurnExecutionHelper.php:260-364` (the turn loop: nation-pass-then-general-pass, both under one `processBlocked()` gate), `:72-109` (processNationCommand).** R-SEAM §1/§2/§4. Resolves B2/B3/m7. **CROSS-MODULE (`:app:game-engine`) — SEQUENTIAL, never co-edited by a leaf.** Depends on F-DISPATCH. **This is the ONLY place that wires the AI into the live loop; `chooseInstantNationTurn` is NOT wired (decision #3/B3).**

### Task FM0 — pin the real seam signatures BEFORE any code (B2)
**Files:** create `.context/p5-research/H-SEAM.md` (a 1-page pin note); no gradle test.
Steps:
- [ ] **READ + PIN (B2, R-SEAM §1) the REAL current signatures:** `ReservedTurnHandler.handle(generalId: Int, actionCode: String, year: Int, month: Int, date: String): HandledTurn` (`ReservedTurnHandler.kt:93`) — the action enters as a FLAT `String actionCode`, resolved via `registry.resolve(actionCode)` (`:103`), seed 6th component = `definition.key` short-class-name (`:137`), NOT any arg payload. `TurnDaemonLifecycle.reservedActionOf: (Int)->String` (`:24,56`) returns ACTION CODE ONLY — `argJson` is DROPPED at the seam. `ReservedTurn` DOES carry `argJson` (`ReservedTurnRepository.kt:39-43,80-100,180-201`) read from the `general_turn.arg`/`nation_turn.arg` jsonb. **PIN the contract decision (R-SEAM §1): widen `reservedActionOf` from `(Int)->String` to carry `(actionCode, argJson)`, thread `ReservedTurn(actionCode,argJson)` through `handle()` into the resolver draft ctx. RNG-seed parity is UNAFFECTED (seed uses class name, never arg) → behavior-additive on the resolver side only.**
- [ ] **PIN the pass-order (B2, R-SEAM §2):** PHP `TurnExecutionHelper.php:299-348` per-general inside `if(!processBlocked())`: NATION pass FIRST (`:301`, only if `hasNationTurn ⇐ nation!=0 && officer_level>=5`, `:260`) → `if AI && use_auto_nation_turn(default 1): chooseNationTurn` (`:306`) → re-seed `'nationCommand'` 6-comp RNG (`:310-317`) → `processNationCommand` (`:318`) → `turn_last_{officer_level}` KV (`:322`) → `setRawCity(null)` (`:323`); THEN GENERAL pass (`:326`) → `if AI: chooseGeneralTurn` (`:332`, `if cmd!==newCmd autorunMode=true`) → re-seed `'generalCommand'` RNG (`:340-347`) → `processCommand` (`:348`). **Both passes RE-SEED their OWN per-command RNG — NOT one shared stream, and NOT the `'GeneralAI'` decision stream.** `preprocessCommand` ran earlier on `'preprocess'` RNG (`:280-287`); after the block `pullNationCommand` then `pullGeneralCommand` rotate the rings (`:350-351`).
- [ ] **PIN the nation-resolve GAP (B3, R-SEAM §4): NO nation-command resolve path exists in the daemon today** — `ReservedTurnHandler` ports ONLY `processCommand` (general). `processNationCommand` (`TurnExecutionHelper.php:72-109`) must be PORTED fresh, reusing the GREEN general `processCommand` while-loop (`:111-167`: full-condition/addTermStack/run/getAlternativeCommand, same `getFailString`/`getTermString`) as the structural template. Same `ChangeRecorder` single-dirty-source.
- [ ] Commit: `docs(p5-research): H-SEAM — pin real ReservedTurnHandler.handle sig + (actionCode,argJson) widen contract + nation-FIRST pass order + processNationCommand port gap (R-SEAM)`.

### Task FM1 — widen the seam to carry `(actionCode, argJson)` + the general-pass AI hook
**Files:** edit `app/game-engine/.../turn/ReservedTurnHandler.kt` + `app/game-engine/.../turn/TurnDaemonLifecycle.kt`; create `app/game-engine/.../turn/AiTurnAdapter.kt`; test `app/game-engine/src/test/.../turn/AiHookTest.kt`.
Steps:
- [ ] WIDEN `reservedActionOf` from `(Int)->String` to `(Int)->ReservedTurn` (or `(actionCode,argJson)` pair), thread `argJson` through `handle()` into the resolver draft ctx (R-SEAM §1; foundation-level interface widening — F-SEAM owns it, no leaf co-edits these two files). **RNG-seed parity unaffected** (assert the 6-component seed still uses `definition.key`, never the arg).
- [ ] Build `AiTurnAdapter` (the single seam owner): constructs the per-general `"GeneralAI"` `RandUtil` (F-SEED, ONCE per general, threaded by-ref) + `AiInstanceState` + `AiWorldView` over the `WorldStateViewAdapter` (FULL mode), supplies the read-only ctx to `GeneralAI`.
- [ ] Interpose the GENERAL-pass AI hook in `ReservedTurnHandler` (R-SEAM §2 `:332-336`): for AI-controlled generals, `val newCmd = ai.chooseGeneralTurn(reserved); if (newCmd != reserved) { autorunMode = true; cmd = newCmd }` BEFORE resolve. **The AI is READ-ONLY over GAME ENTITIES** (the chosen command's resolve runs the EXISTING delta path); the meta-KV deltas route through `ChangeRecorder` (decision #12). NO new entity write path.
- [ ] Test: `argJson` threads end-to-end (a targeted reserved command's arg reaches the resolver); the AI hook replaces the reserved command for AI generals + sets `autorunMode` only when `cmd` changed; non-AI generals are untouched; the seed still keys on `definition.key` (parity unaffected); the AI writes NO game-entity row inline (only meta-KV deltas via ChangeRecorder).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :app:game-engine:test --tests 'opensamguk.engine.turn.AiHookTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(engine/turn): widen reservedActionOf→(actionCode,argJson) + AiTurnAdapter (per-general GeneralAI RandUtil + read-only view) + general-pass AI hook (chooseGeneralTurn, autorunMode on change, no entity write)`.

### Task FM2 — `ProcessNationCommand` port + the officer≥5 nation-pass running BEFORE the general pass
**Files:** create `app/game-engine/.../turn/ProcessNationCommand.kt`; edit `app/game-engine/.../turn/TurnDaemonLifecycle.kt`; test `app/game-engine/src/test/.../turn/NationPassOrderTest.kt`.
Steps:
- [ ] Port `ProcessNationCommand` (R-SEAM §4, `TurnExecutionHelper.php:72-109`) reusing the GREEN general `processCommand` while-loop as template: full-condition → addTermStack → run → getAlternativeCommand; the `'nationCommand'` 6-component seed `serializeSeed(hidden,'nationCommand',year,month,genId,cmd.getRawClassName())`; the `turn_last_{officer_level}` KV write (`:322`) + `setRawCity(null)` (`:323`); SAME `ChangeRecorder` single-dirty-source (the ONE daemon-write rule — JDBC delta only, NO EntityManager).
- [ ] ADD the nation-pass to `TurnDaemonLifecycle.runTick` BEFORE the general pass (R-SEAM §2), under a SINGLE `processBlocked()` gate at the top: `if (hasNationTurn ⇐ nation!=0 && officer_level>=5)` → `if AI && use_auto_nation_turn: nationCmd = ai.chooseNationTurn(reservedNationCmd)` → re-seed `'nationCommand'` RNG → `ProcessNationCommand` → KV/setRawCity. **chooseNationTurn ONLY — `chooseInstantNationTurn` is NOT wired (decision #3/B3).** Then the general pass (FM1). The `'nationCommand'` RNG is constructed ONLY when `hasNationTurn` (a parity-sensitive conditional). After the block, `pullNationTurn` then `pullGeneralTurn` rotate the rings.
- [ ] Test: the nation pass runs BEFORE the general pass for an officer_level≥5 AI general (assert ordering via a recording sink); `hasNationTurn` false (nation==0 or officer_level<5) skips the whole nation block AND its RNG construction; `processBlocked()` (block≥2) skips BOTH passes; the `'nationCommand'` and `'generalCommand'` seeds are DISTINCT streams (different 2nd component); `turn_last_{officer_level}` recorded as a ChangeRecorder delta (no EntityManager); `chooseInstantNationTurn` is never invoked from the lifecycle.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :app:game-engine:test --tests 'opensamguk.engine.turn.NationPassOrderTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(engine/turn): ProcessNationCommand port (nationCommand 6-comp seed, processCommand while-loop template, turn_last KV delta, ChangeRecorder-only) + officer>=5 nation pass BEFORE general pass under single processBlocked gate (chooseNationTurn only, instant NOT wired)`.

**F-SEAM gate:** the live daemon now runs nation-pass-then-general-pass with the AI interpose; G-GATE replays a due-general turn through this seam and asserts the chosen command + the downstream delta. **chooseInstantNationTurn stays OFF the gate (R-SEAM §3).**

## AREA F-QUAR — the ORDER BY RAND quarantine + the chooseInstantNationTurn quarantine (decisions #3/#6)

> **Resolves G4 + the decision #3/#6 quarantines. Lands with F-BRIDGE/GenFound (the `che_선양`/`che_임관` defs). No new RNG draws — the substitute consumes ZERO DRBG draws, exactly like the MySQL-side `ORDER BY RAND()`.**

### Task FQ1 — `ORDER BY RAND()` deterministic-substitute quarantine + register entries
**Files:** edit `logic/ai/families/GenFoundFamily.kt` (the substitute helper) + `.context/p5-research/QUARANTINE-REGISTER.md`; test `logic/src/test/.../ai/QuarantineSubstituteTest.kt`.
Steps:
- [ ] Document + implement the `ORDER BY RAND()` substitute (decision #6, G4, R-GATE §1): `do선양` (`:3324`) and `do국가선택`-오랑캐 (`:3345`) are MySQL-side `RAND()`, NOT on the DRBG stream → **0 draws consumed (cursor UNAFFECTED).** Substitute = deterministic `min(no)`/`min(nation)` over the SAME WHERE-filtered candidate set (insertion = general.no order, G13). **Neither path is reachable in 1010** (R-GATE §1: 0 npc==5, 0 npc==9 of 678; `can선양` needs npc==5, 오랑캐임관 needs npc==9; the 2 officer_level==12 rulers 하진/장각 are both npc==2). **DO NOT fabricate an id, weaken a test, or seed npc 5/9 into 1010.** The quarantine proof is the GT1 census + the crafted-fixture sibling-byte-match (G4 §4): on a crafted npc==5 ruler fixture, the substitute picks a VALID member of the candidate set (only the non-id bytes are byte-matched; the id is asserted "valid member only").
- [ ] Register BOTH quarantines in `QUARANTINE-REGISTER.md`: (1) `ORDER BY RAND() → min(no)` substitute (0-draw, unreachable-in-1010, crafted-fixture proof); (2) `chooseInstantNationTurn` no-gate-path (`@ParityQuarantine("R-SEAM-no-call-site")`, sibling `chooseNationTurn` byte-match proof). Both logged to the phase backlog.
- [ ] Test: the substitute consumes ZERO DRBG draws (a recording rng stub asserts cursor unchanged across the pick); it picks the `min(no)` member of the candidate set deterministically; on the crafted npc==5 fixture the picked id is a valid member.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.QuarantineSubstituteTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai): ORDER BY RAND() deterministic min(no) substitute (0-draw, cursor-unaffected) + quarantine register (선양/오랑캐 unreachable-in-1010, chooseInstantNationTurn no-call-site) with crafted-fixture proof`.

**F-QUAR gate:** GT1 emits the live npc census as the quarantine proof artifact (aborts if non-zero); the substitute is asserted 0-draw.

---

# LEAF FAMILIES — the `do<한글>` command-selection methods (PARALLEL fan-out after Tier 0)

> **Each family owns ONE `ai/families/*.kt` file, consumes the foundations, and registers its `do<한글>` bodies into the F-DISPATCH map BY ACTION-NAME (m12) — none co-widen the dispatch loop, the policy, the bridge, or the registry.** Every draw comes off the ONE shared `"GeneralAI"` stream (F-SEED), in the per-method order pinned in the catalog §5 + the "per-decision RNG draw order" contract above. Every `che_*`-emitting method gates on `candidateAllowed` (F-BRIDGE) EXCEPT the gate-exempt list (`do집합`/`do요양` + the reserved-returns). **A family that emits a `che_*` whose def is not yet in `CommandRegistry` is BLOCKED on F-BRIDGE landing that def (G5) — F-BRIDGE lands all 9 + the che_출병 repair BEFORE Wave 1.** TDD red→green; the draw stream is finalized in G-GATE.

## AREA L-DEPLOY — NationDeployFamily (11 × che_발령)
**Files:** create `logic/ai/families/NationDeployFamily.kt`; test `logic/src/test/.../ai/families/NationDeployFamilyTest.kt`. ← F-FACADE, F-BFS, F-SEED, F-BRIDGE.
Steps:
- [ ] Port the 11 do발령 methods emitting `che_발령` (catalog §5.A/B; `GeneralAI.php` lines per R-BRIDGE §1: 부대전방발령 391 / 부대후방발령 / 부대구출발령 / 부대유저장후방발령 / 유저장후방발령 / 유저장구출발령 476 / 유저장전방발령 / 유저장내정발령 / NPC후방발령 / NPC구출발령 / NPC전방발령 / NPC내정발령). General-pick `choice` BEFORE city-pick `choice`/`choiceUsingWeight` (array-literal L→R). Insertion = BFS/DB order (F-BFS/F-FACADE PK-ascending).
- [ ] **`do부대전방발령` (M9, R-BRIDGE §4 — port the latent bug VERBATIM): emits the `destGenaralID` typo (extra `a`, `:384`) — the ONLY method with the typo; the other 10 emit correct `destGeneralID`. Its draws (a VARIABLE number of `choice($nextCityCandidate)` per ambiguous BFS hop, name-order) STILL HAPPEN, THEN `candidateAllowed`'s argTest rejects the missing `destGeneralID` → returns false → `do부대전방발령` ALWAYS returns null and falls through. Do NOT "fix" the typo — fixing it shifts every downstream draw.**
- [ ] `do유저장구출발령`/`doNPC구출발령` draw a `choice` per qualifying lostGeneral INSIDE the loop (draws = #qualifying + 1, even for discarded args — catalog §5.A). `chiefTurn`/`leaderTurn` compares use `cutTurn` (F-HELPERS).
- [ ] Test: general-pick BEFORE city-pick draw order; **do부대전방발령 emits the typo, consumes its variable BFS draws, then null-returns (the gate's argTest rejects — F-BRIDGE);** 구출발령 draws = #qualifying+1; candidate insertion = PK-ascending/BFS order; each method's emitted `(che_발령, RAW args)` byte-matches PHP.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.NationDeployFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): NationDeployFamily 11× do발령 (che_발령; general-pick-before-city-pick draw order; do부대전방발령 destGenaralID typo verbatim → always-null; 구출발령 per-lostGeneral draws)`.

## AREA L-REWARD — NationRewardFamily (포상/몰수, usort + count-idx weight)
**Files:** create `logic/ai/families/NationRewardFamily.kt`; test `logic/src/test/.../ai/families/NationRewardFamilyTest.kt`. ← F-FACADE, F-SEED, F-BRIDGE, F-HELPERS.
Steps:
- [ ] Port do유저장포상/do유저장긴급포상/doNPC포상/doNPC긴급포상 (che_포상; `:1298,1406,1499,1621`) + doNPC몰수 (che_몰수; `:1751`). **ONE `choiceUsingWeightPair`, weight = `count - idx` (positional rank AFTER a STABLE `usort` — NO secondary comparator, F-SEED); `max(warCnt,civilCnt)-idx` for doNPC포상; `takeAmount` for 몰수 (catalog §5.C/§7). A non-stable sort or a tie-secondary reorders ties → different idx → different weight → different single draw.** The `dedicationList` filter `npc != 5` + the **dead unused-append** `$nationGenerals[]=$this->general` (`:4216/4263` — port verbatim, latent bug; the `array_filter` is on `$this->nationGenerals`, the local append is unused — F-HELPERS). `getOutcome` = half-away (F-HELPERS). The two 긴급 methods set `reqUpdateInstance=true` AFTER the gate (decision #2 dirty trigger).
- [ ] Test: STABLE usort preserves insertion order on ties (no secondary comparator); the `count-idx` weight (and `max(warCnt,civilCnt)-idx` for doNPC포상); ONE `choiceUsingWeightPair` draw; getOutcome half-away (cite H-HELPERS, not asserted bare); the dead-append does NOT change the filtered candidate set; the 긴급 methods set reqUpdateInstance AFTER the gate.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.NationRewardFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): NationRewardFamily 포상/긴급포상/몰수 (stable usort + count-idx weight, single choiceUsingWeightPair, getOutcome half-away, dead-append verbatim, reqUpdateInstance-after-gate)`.

## AREA L-DIPLO — NationDiploFamily (불가침/선포/천도 SELECTION + boolean + draw stream only; RESULT→P6)
**Files:** create `logic/ai/families/NationDiploFamily.kt`; test `logic/src/test/.../ai/families/NationDiploFamilyTest.kt`. ← F-FACADE, F-BFS, F-SEED, F-BRIDGE, F-HELPERS.
Steps:
- [ ] Port do불가침제의 (`:1832`)/do선전포고 (`:1965`)/do천도 (`:1987,2103`) — **SELECTION + the boolean gate + the draw stream ONLY (decision #11); the `che_*` state-mutation/logs are P6. The G-GATE downstream-delta/log assertion EXCLUDES these three (m10).**
- [ ] **do불가침제의 (R-GATE §3): NO RNG draws** — pure deterministic selection + ONE stable `arsort` over `recv_assist` candidates → top pick; the 8-month cooldown (`joinYearMonth` diplomatMonth gate, `:1830`, F-HELPERS); writes `resp_assist_try` (ChangeRecorder delta, decision #12) below the gate. Its golden pins logs/target/side-effect bytes, NOT a draw stream.
- [ ] **do선전포고 (M3, catalog §5.D — confirm Kotlin emits 3 draws, NOT the TS 2): up to 3 draws — (A) `nextBool(trialProp**6)` (may consume 0 OR 1 draw when trialProp≥1/≤0) → (B) the fallback `nextBool(1/count(lowTargetNations))` in the EMPTY-`nations` path (TS DROPS THIS — PHP WINS, 3 vs 2) → (C) `choiceUsingWeight(nations)` weight `1/sqrt(power+1)`.** **VERIFY FIRST: confirm the Kotlin port emits A+B+C (3); if it emits only A+C (the TS pattern), fix BEFORE this task closes.** Sets `reqUpdateInstance=true`. isNeighbor neighbor partition (F-BFS).
- [ ] **do천도 (catalog §5.M): `arsort` score-desc → top-quartile gate → `choice(candidates)` next-hop ONLY if `dist>1` (TS DROPS the cooldown/sticky/BFS-restriction — PHP WINS).** Writes `last천도Trial` (ChangeRecorder delta). Multi-path (`:1987` vs `:2103`).
- [ ] Test: do불가침제의 ZERO draws + stable arsort pick + `resp_assist_try` delta + 8-month cooldown gate; **do선전포고 emits A+B+C = 3 draws (the empty-nations fallback B is present — NOT the TS 2-draw shape); A consumes 0-or-1 by trialProp boundary;** do천도 arsort + dist>1 choice-restriction; all three are SELECTION-only (no downstream che_* delta asserted — m10).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.NationDiploFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): NationDiploFamily 불가침제의(0-draw arsort + resp_assist_try delta)/선전포고(3 draws A+B+C, empty-nations fallback present)/천도(arsort + dist>1 restriction + last천도Trial delta) — SELECTION+boolean+draw-stream only, RESULT→P6`.

## AREA L-GENDOM — GenDomesticFamily (내정/금쌀/징병)
**Files:** create `logic/ai/families/GenDomesticFamily.kt`; test `logic/src/test/.../ai/families/GenDomesticFamilyTest.kt`. ← F-INSTANCE, F-SEED, F-BRIDGE.
Steps:
- [ ] Port do일반내정/do전쟁내정/do긴급내정/do금쌀구매/do징병 (catalog §5.K/L). **`do전쟁내정` has TWO `nextBool(0.3)` (decision #9, the biggest collapse-into-one trap): `:2271` conditional (ONLY if `nation.rice < baserice`) THEN `:2279` ALWAYS-drawn (no guard) — port BOTH, never collapse.** `do일반내정` one conditional `nextBool(0.3)` + terminal `choiceUsingWeightPair`. `do긴급내정` up to 2 conditional `nextBool`. **`do금쌀구매` ZERO draws (fully deterministic ladder — ANY accidental RNG breaks parity; the che_모병/che_징병 cost-probes at `:2391/2396` are non-RNG cost estimates).**
- [ ] **do징병 (catalog §5.L): conditional `nextBool(remainPop/maxPop)` → `choiceUsingWeight(availableArmType)` (4-entry FOOTMAN/ARCHER/CAVALRY/WIZARD insertion order — throw-unreachable, G7) → `choiceUsingWeight(pickScore)`; `round(crew-49,-2)` = PhpRound half-away (NOT the TS deterministic-armType — PHP `choiceUsingWeight` WINS).** 
- [ ] Test: do전쟁내정 fires BOTH 0.3 draws (the conditional `:2271` only when rice<baserice, the unconditional `:2279` always — assert both branches); do일반내정 conditional+terminal; **do금쌀구매 ZERO draws (a recording rng stub asserts cursor unchanged);** do징병 the 3-draw shape with `choiceUsingWeight` (4-entry insertion order) + PhpRound(crew-49,-2).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.GenDomesticFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): GenDomesticFamily 일반/전쟁(TWO 0.3 draws)/긴급 내정 + 금쌀구매(ZERO draws) + 징병(choiceUsingWeight 4-entry + PhpRound(crew-49,-2))`.

## AREA L-GENWAR — GenWarMoveFamily (전투/워프/이동/헌납/집합/방랑)
**Files:** create `logic/ai/families/GenWarMoveFamily.kt`; test `logic/src/test/.../ai/families/GenWarMoveFamilyTest.kt`. ← F-FACADE, F-BFS, F-SEED, F-BRIDGE.
Steps:
- [ ] Port do전투준비/do소집해제/do출병/doNPC헌납/do후방워프/do전방워프/do내정워프/do귀환/do집합/do방랑군이동 (catalog §5.M/N; lines per R-BRIDGE §1).
- [ ] **do출병 (m1, catalog §5.M): the `nextBool(0.7)` at `:2720` is pulled via `&&` (`rice<baserice && npc>=2 && nextBool(0.7)`) BEFORE four non-RNG early-returns (`:2729/2732/2735/2739`) — pin the EXACT statement order; reordering the guards (looks like a harmless optimization) drops/moves the draw.** Then `choice(attackableCities)` (THROWS if none — guard traced, m3). FULL pack via che_출병 repair (F-BRIDGE).
- [ ] **doNPC헌납 (m2, catalog §5.M): the `nextBool((genRes/reqRes)-0.5)` at `:2841` is drawn PER-RESOURCE inside `foreach(resourceMap)` rice-then-gold (`:2790-2796`) → VARIABLE 0–2 draws, NOT a single draw** (same variable-loop class as 구출발령/거병).
- [ ] **do내정워프 (catalog §5.M): `nextBool(0.6)` ALWAYS → `nextBool(warpProp)` (may consume 0 draws when warpProp≥1/≤0 — the prob-boundary trap) → `choiceUsingWeight`.** do소집해제 `nextBool(0.75)` ALWAYS. **do집합 is GATE-EXEMPT (no `hasFullConditionMet`) but NOT draw-free (m4): npc==5 draws `nextRangeInt(2,4)` (`:3116-3118`) AND writes `killturn` as a ChangeRecorder delta.**
- [ ] **m3 (empty-path guards): `choice([])` THROWS (`nextInt(-1)`) — trace the empty-input path for do출병/do거병(L-GENFOUND)/do방랑군이동 and document the guard that makes the throw unreachable (or the upstream catch); an uncaught throw mid-priority-loop would abort the whole AI turn.**
- [ ] Test: do출병 the `:2720` draw fires BEFORE the 4 early-returns (a general failing the train/atmos/crew gate STILL consumes the draw when rice<baserice && npc>=2 — m1); **doNPC헌납 variable per-resource (the 2-resource = 2-draw case AND the rice-skips-gold case — m2);** do내정워프 the warpProp 0-draw boundary; do소집해제 always-0.75; **do집합 gate-exempt but the npc==5 `nextRangeInt(2,4)` draw fires + `killturn` recorded as a ChangeRecorder delta, not inline (m4);** the `choice([])` empty paths are guarded/unreachable (m3).
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.GenWarMoveFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): GenWarMoveFamily 전투준비/소집해제/출병(0.7-before-early-returns)/헌납(per-resource variable 0-2)/워프트리오(warpProp boundary)/귀환/집합(gate-exempt, npc==5 nextRangeInt + killturn delta)/방랑군이동 + choice([]) guards`.

## AREA L-GENFOUND — GenFoundFamily (거병/건국/해산/선양/국가선택/사망대비/중립)
**Files:** create `logic/ai/families/GenFoundFamily.kt`; test `logic/src/test/.../ai/families/GenFoundFamilyTest.kt`. ← F-BFS, F-SEED, F-BRIDGE, F-QUAR.
Steps:
- [ ] Port do거병/do해산/do건국/do선양/do국가선택/doNPC사망대비/do중립 (catalog §5.E/F/H/I/O; lines per R-BRIDGE §1).
- [ ] **do거병 (catalog §5.E): up to 4 draws — `nextBool(0.5)`=nextBit if cityLevel∉{5,6} → per-dist-3-candidate `nextBool()`=nextBit (VARIABLE count, BFS-order-dependent) → `nextFloat1()*70` → `nextBool(0.0075*more)`.** searchDistance(dist≤3) BFS (F-BFS name-order). The `choice([])` empty path guarded (m3).
- [ ] **do건국 (catalog §5.I, m6): `choice(availableNationType)` (13-elem → nextInt(12)) THEN `choice(array_keys(GetNationColors()))` (33-elem LIST → nextInt(32) over INDICES). Order type-THEN-color, do NOT swap. ASSERT `GetNationColors().size == 33` against the GREEN `domain/GetNationColors.kt` (m6) — the count IS the parity target.**
- [ ] **do국가선택 (catalog §5.F): the `ORDER BY RAND()` 오랑캐 branch (`:3345`) is QUARANTINED (F-QUAR, min(nation) substitute, unreachable in 1010) → `nextBool(0.3)` → {early-임관-period `nextBool(pow(...))` float-exact OR post-period `nextBool()`=nextBit} → sibling-or `nextBool(0.2)` → `choice(paths)`.** do선양 uses the `ORDER BY RAND()` min(no) substitute (F-QUAR, unreachable in 1010).
- [ ] **do중립/doNPC사망대비 (catalog §5.H/O): `||`-short-circuit `nextBool` (ONLY if the `che_인재탐색` gate passes) + a `choice($candidate)` that ALWAYS draws even with 1 element. do중립 is the TERMINAL fallback (never null).**
- [ ] Test: do거병 the 4-draw shape incl. the variable per-candidate nextBit (BFS-order); **do건국 type-THEN-color order + `GetNationColors().size == 33` (m6) + nextInt(12)/nextInt(32);** do국가선택 the 오랑캐 quarantine substitute (0-draw) + the 0.3/pow/0.2 chain; do선양 min(no) substitute (F-QUAR, 0-draw); do중립 `||`-short-circuit + always-draw choice + never-null fallback.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.GenFoundFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): GenFoundFamily 거병(4 draws, variable BFS nextBit)/해산/건국(type-then-color, GetNationColors==33)/선양(min(no) substitute)/국가선택(오랑캐 quarantine + 0.3/pow/0.2 chain)/사망대비/중립(|| short-circuit, terminal fallback)`.

## AREA L-RATES — RatesPromoFamily (tax/bill/promotion)
**Files:** create `logic/ai/families/RatesPromoFamily.kt`; test `logic/src/test/.../ai/families/RatesPromoFamilyTest.kt`. ← F-FACADE, F-SEED, F-INSTANCE, F-HELPERS.
Steps:
- [ ] Port chooseTexRate/chooseGoldBillRate/chooseRiceBillRate/choosePromotion/chooseNonLordPromotion + calcCityDevelRate/calcNationDevelopedRate (catalog §5.P/Q). **The rate helpers (tax/bill) make NO draws — pure deterministic over getOutcome (half-away, F-HELPERS).**
- [ ] **choosePromotion (decision #9, the phantom-draw trap): exactly ONE `nextBool(0.1)` per OCCUPIED chief slot, ZERO per empty slot; the `:4102` `nextBool(newChiefProb)` is structurally present but NEVER draws (prob always 1 or 0 → short-circuits). A naive port that always calls it adds a phantom draw — port the short-circuit so it consumes 0 draws.** `chooseNonLordPromotion` up to 5 `choice` per EMPTY chief slot (redraw-on-reject).
- [ ] Test: the tax/bill helpers ZERO draws (cursor unchanged, getOutcome half-away cited from H-HELPERS); **choosePromotion exactly ONE `nextBool(0.1)` per occupied slot + ZERO per empty slot + the `:4102` newChiefProb NEVER draws (decision #9 — assert cursor advances by occupied-slot count only);** chooseNonLordPromotion up-to-5 redraw-on-reject per empty slot.
- [ ] Run: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.ai.families.RatesPromoFamilyTest' 2>&1 | tail -40`.
- [ ] Commit: `feat(logic/ai/families): RatesPromoFamily tax/bill (0-draw, getOutcome half-away) + choosePromotion (1× nextBool(0.1) per occupied slot, :4102 newChiefProb phantom NEVER draws) + chooseNonLordPromotion (up-to-5 redraw)`.

**LEAF FAMILIES gate:** each family's emitted `(che_*, RAW args)` + reason + draw stream is asserted draw-for-draw by G-GATE; the diplomacy families' downstream delta is EXCLUDED (m10).

---

# TIER 2 — THE GATE (serial close; depends on ALL)

## AREA G-GATE — capture_ai (live 1010 + crafted fixtures) + AiReplayGateTest (draw-for-draw selection + downstream log + long-sim)

> **The P5 gate (design §11 P5, line 266): fixed-seed full-game (scenario 1010) replay — NPC selection + downstream log turn-for-turn parity.** Clone the P4 `capture_battle`/`capture_conquercity` draw-recording seam + the `RandUtilDrawRecorder`. Three dimensions (a) AI-selection draw-for-draw, (b) downstream log/delta turn-for-turn (diplomacy families EXCLUDED — m10), (c) long-sim timeline non-divergence. **Depends on ALL foundations + all leaf families.**

### Task GT1 — the npc-census proof artifact + the live 1010 AI capture (`capture_ai.php`)
**Files:** create `tools/php-golden/capture_ai.php` + `tools/php-golden/manifest_ai.json`; commit goldens to `logic/src/test/resources/golden/p5/`; no gradle test here (the replay is GT3).
Steps:
- [ ] **EMIT the npc census as the quarantine PROOF artifact FIRST (M7, R-GATE §1) against the INSTALLED 1010 fixture (not the JSON): `SELECT npc, COUNT(*) FROM general GROUP BY npc ORDER BY npc` — EXPECT only `(npc=2, cnt=678)`; `SELECT COUNT(*) FROM general WHERE npc=5` EXPECT 0; `SELECT COUNT(*) FROM general WHERE officer_level=12 AND npc=9 AND nation` EXPECT 0; `SELECT no,name,nation,npc FROM general WHERE officer_level=12` EXPECT 2 rows both npc=2. ABORT the capture if any non-zero — the G4 ORDER BY RAND quarantine depends on it.** Commit the census output as `npc-census-1010.json` (the proof artifact).
- [ ] **PIN the hiddenSeed from the LIVE capture (m5, R-GATE §2): read `UniqueConst::$hiddenSeed` AFTER install and commit the EXACT 32-char lowercase hex into `manifest_ai.json` as the fixture INPUT (assert format `^[0-9a-f]{32}$`). NEVER hardcode a placeholder/example hex or reuse `UniqueConst.php`'s current literal as canonical — it is fresh-random per install (ResetHelper.php:96); a byte-0 desync desyncs EVERY GeneralAI per-general DRBG.** Re-pin after every re-install (buildScenario regenerates UniqueConst.php).
- [ ] `capture_ai.php`: drive due-general autorun over installed scenario_1010, recording per due general per turn the full ordered AI draw STREAM `{seq, method, args, result, stateIdxBefore, bufferIdxBefore}` tagged by the firing `do<한글>` reason + the chosen `(actionCode, RAW args)` + the resulting `ChangeRecorder` delta + the per-turn log (reuse the P4 `RandUtilDrawRecorder` seam). `manifest_ai.json`: scenario 1010, the pinned hiddenSeed, per-table ignore-list, draw-stream + reason + chosen-command + log match targets, `quarantine[]` = {선양/오랑캐 ORDER BY RAND, chooseInstantNationTurn}.
- [ ] **ENUMERATE the family coverage (M7, R-GATE §3): the live 1010 short window covers general-domestic (일반/긴급/전쟁 내정, 징병, 금쌀구매), do소집해제, do전투준비 (post-declaration only), do요양/do중립/do예약턴 + dispatch side-effects, and nation promotion/tax/bill IFF the window spans months 3/6/9/12. UNDER-COVERED (need crafted fixtures — GT2): L-DIPLO (불가침제의/선전포고/천도) + L-GENFOUND (거병/건국/해산/방랑군이동/국가선택/선양) entirely + the war-state branches of do출병 + front-warps. Document which the gate window actually exercises.**
- [ ] Commit: `feat(tools/php-golden): capture_ai.php (live 1010 due-general autorun draw-stream + reason + chosen-command + delta, RandUtilDrawRecorder) + manifest_ai.json (live-pinned hiddenSeed ^[0-9a-f]{32}$, quarantine list) + npc-census proof artifact (678/0/0, aborts if non-zero)`.

### Task GT2 — the crafted-fixture captures for the under-covered families (`capture_ai_crafted.php`)
**Files:** create `tools/php-golden/capture_ai_crafted.php`; commit `logic/src/test/resources/golden/p5/ai-crafted-{diplo,genfound}-NN.json`; no gradle test here.
Steps:
- [ ] **Build crafted-fixture sibling-byte-match captures (M7, R-GATE §3) for the under-covered branches — DO NOT rely on the long-sim alone:** seed `recv_assist`+`resp_assist`+`resp_assist_try`+supplyCities to reach **do불가침제의** (0-draw — pins logs/target/side-effect bytes); a richer nation + ≥1 valid peaceful neighbor to reach **do선전포고** draws A/B/C; a multi-city nation + a prior 천도 reservation to reach **do천도**'s choice + last천도Trial; a 재야 general at a level-5/6 city to reach **do거병** (searchDistance + nextFloat1()*70 + nextBool(0.0075*more)); a 방랑군 lord (officer_level==12, !capital) to reach **do건국**(choice 13-elem + 33-color)/**do해산**/**do방랑군이동**; a 재야 general to reach **do국가선택**(0.3/pow/0.2 chain); an at-war fixture to reach **do출병**. For the QUARANTINED npc==5 ruler (do선양) the crafted fixture byte-matches only the NON-id bytes (the ORDER BY RAND id is "valid member only").
- [ ] **m10: state in the manifest that for the diplomacy-emitting families (do불가침제의/do선전포고/do천도) the downstream delta/log is EXPLICITLY EXCLUDED from the gate (the che_* state-mutation/logs are P6, no P2–P4 green resolver) — the crafted golden asserts ONLY the AI SELECTION + the boolean gate + the draw stream for those families.**
- [ ] Commit: `feat(tools/php-golden): capture_ai_crafted.php (sibling-byte-match goldens for under-covered L-DIPLO/L-GENFOUND/do출병 — recv_assist/방랑군/재야/at-war fixtures; diplomacy downstream-delta EXCLUDED per m10; 선양 non-id-bytes only)`.

### Task GT3 — `AiReplayGateTest` (draw-for-draw selection + downstream log + long-sim timeline)
**Files:** create `logic/src/test/.../golden/AiReplayGateTest.kt` + `tools/php-golden/compare-ai/`; run the full gate.
Steps:
- [ ] Clone the P4 `BattleReplayGateTest` matched-count-gate-with-quarantine pattern. **Seed `world_state.hidden_seed` with the EXACT manifest hex (m5); assert `^[0-9a-f]{32}$`. Assert the Kotlin census matches the proof artifact: `world.generals.count{it.npc==5}==0` and `world.generals.none{it.officerLevel==12 && it.npc==9}` (M7 — the G4 quarantine validity).**
- [ ] Dimension (a) AI-selection draw-for-draw: replay each captured due-general turn on the same per-general `"GeneralAI"` seed; assert the chosen `do<한글>` reason, the emitted `(actionCode, RAW args)`, and the ordered draw stream (value-for-value AND `stateIdx`/`bufferIdx` cursor-for-cursor) match at the FIRST divergent draw. **The draw-COUNT assertion is gate-EXEMPT for the documented 0-draw methods (do금쌀구매, do불가침제의, tax/bill helpers) — assert ZERO draws, not a stream (m: gate-exempt draw tests).**
- [ ] Dimension (b) downstream log/delta turn-for-turn: the executed command's `ChangeRecorder` delta + the per-turn log byte-equal (feeds the EXISTING P2–P4 resolve gates — a correct choice ⇒ a correct downstream by construction). **EXCLUDE the diplomacy-emitting families (do불가침제의/do선전포고/do천도) from the delta/log assertion (m10) — they have no P2–P4 green resolver; assert only selection+boolean+draw-stream.** Re-derive the NATION reserved-fail deny-log string from the golden (FR3 note: `"{failString} <1>{date}</>"`, reason-first — do NOT trust the current Kotlin `denyLog`).
- [ ] Dimension (c) long-sim timeline non-divergence: N turns of full-game 1010 replay stay byte-identical PHP↔Kotlin; report the FIRST turn that desyncs. `compare-ai/` reuses the compare-command-logs matched-count + ignore-list pattern; draw-stream value+cursor diff + reason/command/log-sequence diff; diplomacy-delta EXCLUDED.
- [ ] Run the full gate: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :logic:test --tests 'opensamguk.logic.golden.AiReplayGateTest' 2>&1 | tail -60`. Then the full P5 check: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :common:test :logic:test :infra:test :app:game-engine:test 2>&1 | tail -40` (grep BUILD SUCCESSFUL + test counts; Docker-unavailable ⇒ IT skipped not failed).
- [ ] Commit: `test(logic/golden): AiReplayGateTest — draw-for-draw selection (reason+RAW-args+stream+cursor) + downstream log/delta (diplomacy EXCLUDED m10, deny-log re-derived) + long-sim timeline non-divergence; npc-census + hiddenSeed format asserted`.

**G-GATE close criteria:** (a) every captured due-general AI selection + draw stream byte-matches at the first divergent draw (0-draw methods assert ZERO draws); (b) the downstream delta/log byte-equals for the non-diplomacy families; (c) the long-sim stays byte-identical for N turns; the npc-census proof holds (0 npc 5/9); the two quarantines (ORDER BY RAND, chooseInstantNationTurn) are logged with proof. **Any residual UNCERTAIN (the deny-log string G12; the exact long-sim window depth) is golden-gated — resolved by the live capture, never asserted bare.**

---

# Residual UNCERTAINs (golden-gated — resolved by the capture, NEVER asserted bare)

1. **The NATION reserved-fail deny-log string (G12, FR3):** `getFailString()` shape (`"{reason} {commandName} 실패."` reason-first) is re-derived from the live golden in GT3 — do NOT trust the current Kotlin `denyLog` (`"{name} 실패: {reason} <1>{date}</>"`).
2. **The long-sim window depth (R-GATE §3):** whether a longer 1010 autorun organically reaches war/diplo state (covering do출병/do선전포고 without a crafted fixture) depends on the gate's month-span + atmos/tech build-up — the proven harness uses short pristine windows + crafted fixtures (P4 practice). The window depth is pinned by GT1's coverage enumeration, gated by GT3's long-sim.
3. **do선양 / 오랑캐임관 byte-match (G4, F-QUAR):** UNREACHABLE in 1010 (0 npc 5/9). Only the crafted-fixture NON-id bytes are byte-matched; the ORDER BY RAND id is asserted "valid member only". Quarantine holds as long as the GT1 census stays 0/0.
4. **`chooseInstantNationTurn` (decision #3, B3):** no PHP call-site → no golden → OFF the gate. The structural stub (if written) is `@ParityQuarantine`-proven by sibling `chooseNationTurn` byte-match, logged to the backlog, NOT wired into F-SEAM.

