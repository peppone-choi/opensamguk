# Han NAME-87055 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Complete the approved B2a name-only correction of damaged county label 87055.

**Architecture:** Extend the existing stable-ID normalization registry and regenerate its derived artifacts. Keep historical affiliation adjudication separate from a damaged glyph correction. Refresh strict content hashes because a name-only tile-byte change still invalidates the water binding.

**Tech Stack:** Python unittest, deterministic JSON builders, existing Kotlin map consumers.

**Spec:** Meta handoff `reports/opensamguk/tasks/2026-09-05-handoff-han-map-track.md`, sections 6–7 (under `/Users/apple/Desktop/개인프로젝트/opensamguk-meta`), and repository `docs/superpowers/specs/2026-09-05-water-transport-and-naval-topology-design.md` for hash/geometry invariants. User approved the spec and requested all remaining scope.

## Global Constraints

- `87055` 소속은 `PARENT-0017 安平國`; 글자 수정과 재배속은 별개 변경이다.
- The 172 parent roster, province IDs, city IDs, ownership, terrain, coordinates and adjacency remain unchanged.
- Keep existing frozen V2 and V3 runtime city catalogs unchanged; this physical place is not a V3 runtime city.
- No invented water edges, ports, barriers or fleet capacity. Preserve two reviewed water zones and zero edges/barriers/ports.
- Retain strict source-pair drift rejection, idempotence and pinned base/manifest hashes.
- Never commit raw CHGIS/RTK14 inputs or external source corpora. Use `apply_patch` for source edits and existing builders for generated artifacts.
- No server/DB changes are part of this task. No parallel Gradle writer.

### Task 1: Correct NAME-87055 and rebind derived artifacts

**Files:**
- Modify: `tools/map/build_tile_grid.py`, the existing `CANONICAL_PLACE_NAME_NORMALIZATIONS` registry.
- Test: `tools/map/tests/test_han_place_name_normalization.py`.
- Rebind exact expected tile digest after semantic comparison: `tools/map/tests/test_han_tiles_contract.py`, `infra/src/test/kotlin/opensamguk/infra/seed/HanStrategicTopologyJsonTest.kt`, `app/game-api/src/test/kotlin/opensamguk/gameapi/controller/MapPreviewControllerTest.kt`. Keep their equality assertions strict; parent serializes Gradle verification.
- Regenerate: `data/map/han-tiles.json` and strict derived water/adjudication/manifest artifacts whose source hashes change; regenerate only impacted administrative/disconnection outputs via their existing tools.
- Document: `docs/superpowers/research/2026-09-05-han-name-87055.md` with source evidence and exact geometry/identity comparison.

**Interfaces:** Consumes `normalize_han_place_names(document)` and `materialize_han_place_names.py`; produces canonical `남련현` / `南䜌县` for physical ID `87055` in every existing materialized collection, without creating a new runtime city.

- [ ] Read the approved handoff and actual primary-source occurrences at meta `references/sources/shiliao/corpus/hhs-001A.txt:44`, `hhs-110.txt:304`, `hs-015.txt:489`. Historical spelling is 南䜌; do not use the quotation's affiliation to silently reparent. Korean reading reference: https://en.wiktionary.org/wiki/䜌 (련); the literal display label is 남련현.
- [ ] Add failing behavior tests for all four collection shapes (`cities.name`, `provinceRecords.displayName`, `jurisdictionRecords.displayName`, `legacyGameplay.cities.name`), idempotence, unknown source-pair rejection and unchanged metadata. Minimal core test:

```python
def test_recovers_nanryeon_without_changing_affiliation(self):
    document = {"cities": [{"id": "87055", "name": "남?현", "nameCh": "南?县", "parentId": "PARENT-0017"}]}
    expected = {"cities": [{"id": "87055", "name": "남련현", "nameCh": "南䜌县", "parentId": "PARENT-0017"}]}
    self.assertEqual(expected, normalize_han_place_names(document))
    self.assertEqual(expected, normalize_han_place_names(expected))
```

- [ ] Run `python3 -m unittest tools.map.tests.test_han_place_name_normalization -v`; record RED for the missing normalization, not an import/setup error.
- [ ] Add only this registry entry:

```python
"87055": {
    "sourceName": "남?현",
    "sourceNameCh": "南?县",
    "displayName": "남련현",
    "nameCh": "南䜌县",
},
```

- [ ] Run `python3 tools/map/materialize_han_place_names.py`. Compare parsed pre/post documents to prove that only this ID's display/nameCh fields changed; record unchanged terrain/ownership arrays and ID/parent maps. Do not regenerate the source tile grid from unavailable external inputs.
- [ ] Rebind `data/curated/han/water-topology-adjudications-v1.json` to the measured new tile hash, then run `python3 tools/map/build_han_water_topology.py --write`. Regenerate any impacted existing audit/materialization outputs with their checked-in tools. Do not loosen validators or rewrite frozen fixtures merely to pass; report unexpected semantic drift as blocked.
- [ ] Run focused tests GREEN, then map and scenario unittest discovery. Run `materialize_han_place_names.py --check`, `build_han_water_topology.py --check`, `audit_han_water_topology.py --check`, `tools/scenario/build_han_world.py --target han-world-v3 --check`, `tools/scenario/apply_han_world.py --map han-world-v3 --check`, and `tools/scenario/audit_han_supply_disagreements.py --map han-world-v3 --check`. Record actual counts/skips and unchanged geometry/route-node sets; no production claims from these gates.
- [ ] Record evidence, changed artifacts/hashes, tests and remaining B1/B2 scope in research and the task report. Self-review, then return for independent review before commit. Parent owns review artifact, commit/PR/merge and operations.
