/**
 * v2-lab 네임스페이스 플레이스홀더 (OPENSAM-35 / 0A-a).
 *
 * 0A는 전부 "격리"이고 확장점 개설은 하나도 없다(계획 §5). 실제 v2 기능 페이지는 이 티켓 범위 밖이므로
 * 여기서는 네임스페이스가 존재하고 게이트(layout.tsx)가 걸린다는 사실만 렌더한다.
 */
export default function V2LabPage() {
    return (
        <main>
            <h1>v2-lab</h1>
            <p>v2 실험 네임스페이스. 기능 페이지는 아직 없습니다.</p>
        </main>
    );
}
