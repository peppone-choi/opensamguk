/**
 * v2-lab namespace placeholder (OPENSAM-35 / 0A-a).
 *
 * 0A is entirely about isolation and creates no extension points (plan §5). Actual
 * v2 feature pages are outside this ticket, so this renders only the namespace's
 * existence and its layout gate.
 */
export default function V2LabPage() {
  return (
    <main>
      <h1>v2-lab</h1>
      <p>v2 실험 네임스페이스. 기능 페이지는 아직 없습니다.</p>
    </main>
  );
}
